# Timeline Canvas Refactor Plan

**Status:** Phase 0 contracts implemented; Phase 1 shell-mount/diagnostics implemented; Phase 2 cache-boundary and Phase 4 legacy-retirement slices are mostly retired for the old DOM body; Phase 3 typed move, keyboard, geometry, transition operation and preview/drop-clear parity, generic keyframe transaction kernel execution, path-value keyframe create/update in the kernel, Mask/Text path-keyframe compatibility routing through the kernel, fade transaction execution through `useClipFade`, selected clip-bar keyframe tick-drag transactions, expanded curve-editor keyframe/Bezier drag transactions, and first-pass clip/track/empty context-menu command-model extraction have broad coverage with side-effecting menu-handler follow-up and explicit parity coverage still open; Phase 5 history restore-side migration plus top-level video/audio, nested-video, composition-audio, top-level-image, nested-image, direct add-image, image clipboard-paste data-only image source handling, media relink/reload image data-only source handling, direct browser video/audio add data-only handling, timeline video/audio paste data-only handling, media relink/reload video/audio data-only handling, download completion video/audio data-only handling, 3D user-action media-owned URL ownership, Vector user-action data-only handling, first thumbnail job/resource/bitmap admission gates, primary lazy video/audio admission, interactive lazy image admission, interactive WebCodecs runtime-provider admission, legacy WebCodecs helper provider admission, split media-runtime data-only cleanup, shared image hydrator admission for composition/background/slot, background/slot/composition video/audio runtime admission, RAM preview run-job/image/video-provider/CPU-cache/GPU-cache admission, export run/output/preview/image/precise-video/audio/runtime-binding/WebCodecs-provider/parallel-decoder admission, Vector runtime canvas admission, video-bake proxy admission, legacy JPEG proxy-frame cache admission, decoded AudioBuffer cache admission, proxy VideoFrame cache admission, FFmpeg frame-renderer export-prep run-id forwarding, primary lazy-media, background-layer, slot-deck, thumbnail, render-target, composition-render, RAM-preview runtime reporting, export runtime reporting, export/preview parity smoke coverage, media-scoped project-load/relink model/gaussian sequence URL ownership, durable primary `MediaFile.url` ownership, media-owned cached thumbnail URL restore, manager-owned file-backed lazy video/audio element URLs, AddComp composition-audio data-only placeholders, split no-runtime composition-audio cleanup, playback/export composition-audio mixdown-on-demand helper, interactive lazy image preview fallback, export image hydration, shared cancellable image hydration for composition/background/slot paths, data-only image support for RAM preview and `useLayerSync`, first nested shell/primitive restore dedupe, nested spatial source dedupe, async vector restore starter dedupe, recursive nested runtime patching, stale Vector guard hardening, project-load post-relink nested reload delegation, neutral nested composition loader extraction, recursive nested keyframe ID collector convergence, shared nested segment build/apply scheduling, nested boundary/thumbnail helper coverage, shared nested keyframe merge/session-guard policy, neutral restore-hooks policy surface for runtime-ready/relink behavior, `serializationUtils.loadState()` nested delegation to the neutral loader, legacy gaussian-avatar restore URL ownership, top-level restore dispatcher extraction, NativeHelper restore URL ownership cleanup, NativeHelper placement primary URL ownership cleanup, composition-audio null-mixdown generating-state cleanup, restore-scoped vector helper extraction, stale top-level image blob URL hardening, worker wire contract extraction, and the real-media worker-positive smoke hook are implemented. The full DOM `TimelineClip` body has been deleted and committed at `0f202a6b`; current work is post-retirement parity and follow-up architecture hardening. Full `npm run build`, `npm run lint`, and `npm run test` passed for the content committed as `0f202a6b`, but fresh full browser/default-on/torture verification is still required before final readiness. The latest menu, restore, media-scoped URL ownership, primary media URL ownership, cached thumbnail URL ownership, lazy video/audio element URL ownership/admission, AddComp/Split composition-audio placeholder cleanup, split media-runtime data-only cleanup, playback/export composition-audio mixdown-on-demand, direct browser video/audio add data-only migration, timeline video/audio paste data-only migration, media relink/reload video/audio/image data-only migration, download completion video/audio data-only migration, 3D user-action media-owned URL migration, Vector user-action data-only migration, thumbnail job/resource/bitmap admission, interactive lazy image preview fallback/admission, shared image hydrator admission, interactive runtime-provider admission, legacy WebCodecs helper provider admission, background/slot/composition runtime admission, RAM preview allocator admission, export allocator admission, Vector runtime canvas admission, video-bake proxy admission, legacy JPEG proxy-frame cache admission, decoded AudioBuffer cache admission, proxy VideoFrame cache admission, FFmpeg export-prep run-id forwarding, top-level/nested/add/paste image source hydration, shared image runtime hydrator, nested shell/primitive dedupe, nested spatial source dedupe, vector restore starter dedupe, recursive nested runtime patching, stale Vector guard hardening, project-load nested reload delegation, neutral nested loader extraction, recursive nested keyframe collector, segment scheduling, boundary/thumbnail helper coverage, nested keyframe merge policy, restore-hooks policy, loadState nested delegation, transition preview/drop-clear, keyframe transaction kernel, path-value keyframe kernel, Mask/Text path-keyframe routing, fade transaction/hook migration, selected keyframe tick-drag transaction routing, expanded curve-editor transaction routing, top-level restore dispatcher, NativeHelper URL cleanup, NativeHelper placement URL ownership cleanup, composition-audio null-mixdown state cleanup, vector restore extraction, stale image blob hardening, legacy-avatar, worker contract, worker-positive smoke hook, AI node runtime canvas admission, active audio/stem preview element admission, and post-push context-menu no-op guard slices have targeted checks only. Effective plan progress is about 95% risk-weighted / about 98% code-breadth; use `docs/plans/timeline-canvas-refactor-handoff.md` for the current working snapshot.
**Current correction:** the old "side-effecting menu-handler follow-up" wording above is superseded by the current post-`0bec2cd3` clip-menu/worker-default checkpoint: clip context-menu side effects now use central command descriptors with stale-target no-op coverage.
**Scope:** full timeline clip refactor for speed, maintainability, and clean ownership  
**Current handoff:** `docs/plans/timeline-canvas-refactor-handoff.md`
**Parallelism target:** up to 6 agents working on independent streams  
**Primary rule:** timeline visuals must not create, own, or warm playback media runtime

**Latest runtime-admission checkpoint:** Commit `8841c778` moved
composition-audio, thumbnail, and scrubbing allocation closer to the target
ownership model. `compositionAudioMixdownCache.ts` now gates
composition mixdown playback `HTMLAudioElement` creation through the interactive
runtime coordinator, reports/release clip-scoped playback elements, reports
completed mixdown `AudioBuffer` cache entries as retained runtime bindings with
heap/duration diagnostics, releases those buffer resources on LRU eviction and
cache clear, and exposes a clip-runtime release helper used by deleted-clip
cleanup, nested content-hash refresh, and media-delete cleanup. Thumbnail
generation now admits detached generation videos before `document.createElement('video')`,
admits generation canvases before `document.createElement('canvas')`,
and admits bitmap decode jobs plus retained bitmap resources before
`fetch(...)` / `createImageBitmap(...)`. `ScrubbingCache` background preload
videos are now background-policy admitted before hidden video creation, released
on source/session clear, and `CacheManager.handleDeviceLost()` destroys the
whole cache instead of dropping it after only composite-resource release.
Composition mixdown runtime release now lives in store-cycle-safe resource
helpers, while the timeline store mutation helper is isolated from the
cache/mixer path so Timeline store slices can import cleanup without re-entering
`useTimelineStore` during initialization.
Focused checks passed:
`npm run test -- tests/unit/compositionAudioMixdownCache.test.ts tests/unit/audioScrubSync.test.ts tests/unit/compositionAudioMixer.test.ts tests/stores/mediaStore/fileManageSlice.test.ts`
(`147` tests), `npm run test -- tests/unit/thumbnailCacheService.test.ts tests/unit/thumbnailBitmapCache.test.ts`
(`24` tests), `npm run test -- tests/unit/scrubbingCache.test.ts tests/unit/cacheManagerRuntimeReporting.test.ts`
(`14` tests), `npm run test -- tests/stores/timeline/clipSlice.test.ts`
(`139` tests), touched-file ESLint,
`npx tsc -p tsconfig.app.json --noEmit --pretty false`, and final full
`npm run build`, `npm run lint`, and `npm run test` (`363` files / `3878`
tests). Current LOC watermark from `npm run swarm:status` at
`2026-06-06T11:56:23Z`: clean tree, `0` tracked changed files, `0` new
untracked files, `+0/-0`.

**Committed post-`8841c778` export/menu descriptor checkpoint:** Composition
mixdown writeback during export now checks export `source-buffer` admission
before mutating timeline state, and audio-only export creates/reports/releases
an export run id so `AudioExportPipeline` admission is active outside video
exports. Track/Empty context menus now produce pure command descriptors with
explicit executor helpers instead of closure `action` fields. This slice is
committed at `0bec2cd3`; its pre-commit LOC watermark from
`npm run swarm:status` at `2026-06-06T12:03:59Z` was `11` tracked changed
files, `0` new untracked files, tracked `+382/-123` net `+259`.
Focused checks passed: `npm run test -- tests/unit/audioExportPipeline.test.ts tests/unit/compositionAudioMixdownCache.test.ts`
(`26` tests), `npm run test -- tests/unit/trackContextMenu.test.ts tests/unit/timelineEmptyContextMenu.test.ts tests/unit/TrackContextMenu.test.tsx tests/unit/TimelineEmptyContextMenu.test.tsx`
(`15` tests), `npx tsc -p tsconfig.app.json --noEmit --pretty false`, and
`npm run test -- tests/unit/audioExportPipeline.test.ts tests/unit/compositionAudioMixdownCache.test.ts tests/unit/exportRuntimeReporting.test.ts`
(`33` tests), and final full `npm run build`, `npm run lint`, and
`npm run test` (`363` files / `3883` tests).

**Post-`0bec2cd3` hidden-video cleanup checkpoint:** Background-layer
and slot-deck video hydrators now register owner-scoped pending video disposers,
route stale `canplaythrough`, `error`, and pre-ready dispose through
`engine.cleanupVideo(video)`, remove stale listeners, and call
`engine.cleanupVideo(video)` for bound video teardown after successful warmup.
Current LOC watermark from `npm run swarm:status` at `2026-06-06T12:17:33Z`:
`6` tracked changed files, `0` new untracked files, tracked `+301/-45` net
`+256`. Focused checks passed:
`npm run test -- tests/unit/layerPlaybackManagerWarmDeck.test.ts tests/unit/slotDeckManager.test.ts`
(`12` tests), touched-file ESLint for the two managers and tests, and
`npx tsc -p tsconfig.app.json --noEmit --pretty false`.

**Current post-`0bec2cd3` thumbnail ownership checkpoint:** Legacy media-panel
thumbnail helper output now converts raw generated/reused thumbnail blob URLs
into media-owned thumbnail URLs via `createThumbnailMediaObjectUrl(...)` before
writing `MediaFile.thumbnailUrl`. `processImport(...)`, `ensureFileThumbnail`,
and `refreshFileUrls(...)` now pass/consume the media id for managed thumbnail
ownership, and `mediaThumbnailOwnership.test.ts` covers temporary blob URL
revocation plus manager tracking. Current LOC watermark from
`npm run swarm:status` at `2026-06-06T12:22:22Z`: `12` tracked changed files,
`1` new untracked file, tracked `+370/-60` net `+310`, plus `+85` new-file LOC.
Focused checks passed:
`npm run test -- tests/unit/mediaThumbnailOwnership.test.ts tests/unit/importPipeline.test.ts tests/stores/mediaStore/fileManageSlice.test.ts tests/unit/projectMediaPersistence.test.ts`
(`163` tests), touched-file ESLint for thumbnail/import/media-store files plus
the current hidden-video files, and
`npx tsc -p tsconfig.app.json --noEmit --pretty false`.

**Current post-`0bec2cd3` clip-menu/worker-default checkpoint:**
Clip context-menu side effects now route through a pure command descriptor union
and one central executor. `TimelineContextMenu` builds descriptors for Show in
Explorer, proxy generation, thumbnail regeneration, audio proxy/analysis
regeneration, display toggles, clipboard, timeline mutations, stem separation,
transcription, and label colors; `executeClipContextMenuCommand(...)` keeps
disabled/stale descriptors inert when clips or target ids are missing. The
product default for `timelineCanvasWorker` is now `true`, and
`scripts/run-timeline-canvas-verification.mjs` runs the unforced default-on live
worker smoke by default unless `--skip-worker-default-on` is explicit. Current
LOC watermark from `npm run swarm:status` at `2026-06-06T13:47:01Z`: `19`
tracked changed files, `1` new untracked file, tracked `+1050/-371` net `+679`,
plus `+85` new-file LOC. Focused checks already run before the final-only-test
instruction: `npx tsc -p tsconfig.app.json --noEmit --pretty false` and
`npm run test -- tests/unit/clipContextMenu.test.ts tests/unit/TimelineContextMenu.test.tsx`
(`34` tests). Remaining agent-confirmed work: live/default-on worker proof,
Torture coverage through the local ignored
`fixtures/torture-media/manifest.local.json`, and final broad gates.

**Final browser verification checkpoint:** Full bridge verification passed in
`fixtures/timeline-canvas-reports/run-20260606-134318Z/report.json` after the
runner was hardened for the worker-default-on target. The run created a real MP4
live fixture with `150` video clips and `150` audio clips, then passed live
composition verification, export-preview parity, forced worker prewarm,
synthetic worker, thumbnail worker, live worker compatibility, live worker
positive, unforced default-on live worker, Torture, synthetic large-project,
thumbnail-before-playback reload, scrub/playback/path, playhead smoothness,
Blade, and marquee checks. Fast export produced `1141079` bytes and the summary
had no verification failures. The local ignored Torture manifest used MP4-only
sources for this proof so the FAST/MP4Box export gate tests rendering instead of
failing on WebM container parsing. Final broad build/lint/test gates still need
to be rerun after this checkpoint before a normal push.

**Latest post-push menu/image reload checkpoint:** After commit/push
`0f202a6b`, a six-agent audit found concrete remaining Phase 3/5 follow-ups.
This slice fixed the smallest high-signal items: `TimelineEmptyContextMenu.tsx`
now ignores capture-phase contextmenu events inside the menu; disabled/stale
clip context-menu thumbnail, delete-gap, transcription, and label-color actions
stay inert instead of closing or mutating missing targets; and
`fileManageSlice.updateTimelineClips(...)` reloads image clips as data-only
`source.imageUrl` state instead of allocating `new Image()` / storing
`source.imageElement`. Focused checks passed:
`npm run test -- tests/stores/mediaStore/fileManageSlice.test.ts tests/unit/TimelineContextMenu.test.tsx tests/unit/TimelineEmptyContextMenu.test.tsx tests/unit/clipContextMenu.test.ts tests/unit/timelineEmptyContextMenu.test.ts tests/unit/trackContextMenu.test.ts tests/unit/TrackContextMenu.test.tsx`
(`164` tests), touched-file ESLint, and
`npx tsc -p tsconfig.app.json --noEmit --pretty false`. Later slices migrated
the clip context-menu side effects into command descriptors, turned the worker
renderer default on, and closed the named allocator leftovers. Remaining open
work from that audit is fresh live/default-on proof, Torture coverage when a
local manifest exists, and final broad gates.

**Latest cleanup/parity checkpoint:** Shared deleted-clip runtime cleanup now lives in `src/stores/timeline/deletedClipResources.ts` and is used by both manual clip removal and edit-kernel deletes, so video/audio elements, vector runtimes, AI-node runtime canvases, and clip-owned blob URLs are cleaned from one path. Phase 3 stale-target parity for selected clip-bar keyframe tick drags and expanded curve-editor keyframe drags is now covered: stale typed targets clear the active transaction session and do not fall back to legacy move callbacks. Focused checks passed: `npm run test -- tests/unit/TimelineTrack.test.tsx tests/stores/timeline/clipSlice.test.ts tests/unit/timelineEditOperations.test.ts tests/unit/aiNodeRuntime.test.ts` (233 tests), touched-file ESLint for the cleanup/parity slice, and `npx tsc -p tsconfig.app.json --noEmit --pretty false`. Latest LOC status from `npm run swarm:status` at `2026-06-06T09:05:18Z`: `242` tracked changed files, `81` new untracked files, tracked `+23490/-17015` net `+6475`, plus `+30534` LOC in new files. This checkpoint is superseded by the current clip-menu/worker-default checkpoint; remaining release work is live/default-on proof, Torture coverage if fixtures exist, and final broad build/lint/test/browser gates.

**Latest smoke persistence/playhead/context-menu checkpoint:** Synthetic timeline smokes now guard both active-composition autosave and project continuous-save/beforeunload persistence, so temporary smoke media/timelines should not persist as Relink artifacts. `Timeline.tsx` now keeps the visible playhead on the high-frequency internal playback clock during playback and clamps tiny forward-playback DOM regressions, fixing the user-visible playhead flicker/backtrack while preview continues correctly. Synthetic playhead smokes now restore by default unless `createSynthetic:false` or `restoreTimelineAfterRun:false` is explicit; this closes the no-arg playhead smoke regression that could leave the visible browser on Smoke clips. `TimelineContextMenu.tsx` thumbnail regeneration, label-target resolution, proxy generation, and audio-proxy regeneration now delegate to `clipContextMenu.ts`; thumbnail regeneration uses media-owned/source URLs instead of requiring `clip.source.videoElement`, matching the data-only video-source migration. Focused browser proof passed: `runTimelineCanvasPlayheadSmoothnessSmoke` returned `backtrackCount=0`, `maxBacktrackPx=0`, `forwardDistancePx=57.63`, restored the live comp to `781` clips / `3` tracks, and a four-smoke bridge group (`large`, `marquee`, `blade`, `playhead`) all returned `success=true` and restored to the same live shape. Current safe live test surface is `Subcomposition 1` (`1780705391680-ecu7panot`) with `781` clips, `426.18s`, `Video 1:300`, `Audio 1:381`, and `Audio 2:100`; `Random 100 Video Clips` is not the current live verification target unless rebuilt/restored. Focused checks passed: `npm run test -- tests/unit/clipContextMenu.test.ts tests/unit/timelineCanvasSmokeHandlers.test.ts` (29 tests), touched-file ESLint for the smoke/context-menu slice, direct browser playhead smoke, four-smoke bridge group, post-smoke bridge state check, `node --check scripts/run-timeline-canvas-verification.mjs`, `node scripts/run-timeline-canvas-verification.mjs --help`, and `npx tsc -p tsconfig.app.json --noEmit --pretty false`. Latest LOC status from `npm run swarm:status` at `2026-06-06T09:54:08Z`: `244` tracked changed files, `81` new untracked files, tracked `+23708/-17054` net `+6654`, plus `+30972` LOC in new files. This checkpoint is superseded by the current clip-menu/worker-default checkpoint.

**Historical live-project verification checkpoint (superseded for current target):** The 2026-06-06 08:45 UTC proof previously ran on `Random 100 Video Clips` (`1780703769030-jqptxgu3c`) before it was later observed empty after the smoke/open cycle. Treat those runner, worker-positive, and export/preview results as historical evidence only. The current safe live verification target is `Subcomposition 1` (`1780705391680-ecu7panot`); do not use `Random 100 Video Clips` unless it is rebuilt or explicitly restored. `scripts/run-timeline-canvas-verification.mjs` has a reproducible live fallback path (`--live-import-path`, `--live-import-paths`, `--skip-live-import-fallback`), a default-on probe mode (`--check-worker-default-on`), hard assertions for live worker re-open steps, and a post-`openComposition` poll so the runner waits until timeline clips are actually present before judging a live comp. It also fixes `--live-min-video-clips 0` / `--live-min-audio-clips 0` parsing. Focused live runner proof `fixtures/timeline-canvas-reports/run-20260606-084514Z/report.json` passed with reload first stats in `2921ms`, ready in `4181ms`, no reload/live failures, live `384` video clips, scrub/playback/playback-path success, and zero DOM clip bodies. Direct real-media worker-positive proof on `Random 100 Video Clips` passed with `781` clips, `3` video media sources, `222` thumbnail URLs requested, `31` warmed thumbnail bitmaps, `workerTrackCount=1`, `workerEligibleTrackCount=1`, `workerFallbackTrackCount=2` only for whitelisted `audio-resource-visuals`, `workerPendingTrackCount=0`, `workerErrorTrackCount=0`, `workerResourceBytes=9728000`, estimated `60fps`, and zero slow/dropped frame estimates. Direct nonblank export/preview parity on the same historical live comp passed: FAST export blob `298942` bytes, `6` preview samples, reference/candidate `nonBlankRatio=1`, `avgRgbDelta=0.8889`, `meanLumaDelta=0.8945`, and no parity failures. `node --check scripts/run-timeline-canvas-verification.mjs` passed. This proof is superseded for LOC/current-open-work by the cleanup/parity checkpoint above.

**Latest worker-positive live smoke checkpoint:** Phase 6 now has the real-media worker-positive proof hook plus a focused browser proof on the restored 3-video fixture. `runTimelineCanvasLargeProjectSmoke` accepts `warmWorkerThumbnails`, loads cached thumbnails for current real video sources, warms decoded thumbnail bitmaps through `thumbnailBitmapCache`, reports `workerThumbnailWarmup`, can fail if too few bitmaps were prepared, and can reject fallback reasons outside an explicit `allowedWorkerFallbackReasons` whitelist. The large local-file import path in `src/services/aiTools/handlers/media.ts` now retries dev-bridge `response.blob()` `Failed to fetch` failures with byte-range requests, which let the browser import all three larger user MP4s after the original full-blob load failed. The focused comp `WorkerPositive 3 Restored Videos 53 Segments 20260606` (`1780725925034-snfho0roz`) passed a forced-worker real smoke with 106 clips, `workerTrackCount=3`, `workerEligibleTrackCount=3`, `workerFallbackTrackCount=0`, `workerPendingTrackCount=0`, `workerErrorTrackCount=0`, `workerResourceBytes=9584552`, `workerThumbnailWarmup` `31/99`, estimated `60fps`, and zero slow/dropped frame estimates. Focused checks passed: `npm run test -- tests/unit/aiToolMediaHandlers.test.ts tests/unit/timelineClipCanvasWorkerModel.test.ts tests/unit/timelineCanvasSmokeHandlers.test.ts` (37 tests), touched-file ESLint for the worker/media/test slice, and `npx tsc -p tsconfig.app.json --noEmit --pretty false`. Current worker work is not more implementation: run the full live/default-on proof with `timelineCanvasWorker` now default `true`, then final visual/parity and broad gates.

**Latest runner hardening checkpoint:** `runTimelineCanvasThumbnailReloadSmoke` no longer depends on MediaRecorder/WebM capture for the normal runner path. It now prefers an existing video media file and otherwise uses bundled `/masterselects_github.mp4`, which avoids the Chrome/GPU hang reproduced by the old synthetic video-source path. `runTimelineCanvasLargeProjectSmoke` now supports compact bridge results plus phase timings, and `scripts/run-timeline-canvas-verification.mjs` now prewarms the worker with a smaller forced-worker smoke, defaults the forced worker synthetic timeout to `180000ms`, defaults the live comp id to the current safe `Subcomposition 1` (`1780705391680-ecu7panot`), and drives the 720/8 synthetic worker proof at zoom levels that actually exercise culling. The runner now hard-asserts worker live state waits and wraps `workerFallbackLive`, `workerPositiveLive`, and `workerDefaultLive` in tool-success assertions; default-on proof also requires an unforced smoke with `workerFlag.previous === true`. Direct browser proof passed for `720` clips / `8` worker tracks with `workerTrackCount=8`, `workerEligibleTrackCount=8`, zero fallback/pending/error tracks, `workerResourceBytes=30720000`, `60fps`, zero dropped/slow frames, and partial culling at high zoom/scroll. Focused runner proof `fixtures/timeline-canvas-reports/run-20260606-070042Z/report.json` passed with reload ready in `1506ms`, worker prewarm `72` clips / `4` tracks, worker synthetic `720` clips / `8` tracks, and worker thumbnail synthetic all green. Focused checks passed: touched-file ESLint for `timelineCanvasSmoke.ts` and `run-timeline-canvas-verification.mjs`, `node --check scripts/run-timeline-canvas-verification.mjs`, `node scripts/run-timeline-canvas-verification.mjs --help`, and `npx tsc -p tsconfig.app.json --noEmit --pretty false`. Remaining runner work: full live/default-on proof still needs `workerPositiveLive`, torture-media coverage, visual parity/pixel evidence, and final broad build/lint/test gates.

**Latest bridge smoke checkpoint:** A fresh Chrome tab registered with the local AI bridge after opening `http://localhost:5173`; `GET /api/ai-tools` reported one visible client, and `getStats` showed `engineReady=true`, `renderLoop.isRunning=true`, WebGPU device present, `projectLoadProgress.phase="idle"`, and no current playback/export resources. The opened tab had no media files and an empty `Comp 1`, so real 3-video `workerPositiveLive` and nonblank export/preview parity could not be re-run in this browser state. Browser Bridge smokes that do not need user media passed: forced-worker synthetic large timeline (`180` clips / `4` tracks, `workerTrackCount=4`, `workerFallbackTrackCount=0`, `workerErrorTrackCount=0`, estimated `56fps`, `0` dropped frames), default non-worker synthetic large timeline (`180` clips / `4` tracks, `workerTrackCount=0`, estimated `56fps`, `0` dropped frames), forced-worker thumbnail-before-playback smoke with bundled `/masterselects_github.mp4` (`18` clips, `36` thumbnail draws, `workerTrackCount=2`, no fallback/error tracks), playhead smoothness smoke (no backtrack failures), and Blade tool canvas smoke (preview present before click and split changed one clip into two). The synthetic export/preview parity smoke was intentionally not counted as pass/fail for visual parity because both reference and candidate fingerprints were blank (`nonBlankRatio=0`) without real media. Post-smoke logs only showed WebGPU adapter/device timeout warnings, two high-drop-rate warnings during smoke activity, and the expected blank synthetic export warning; final stats were idle with `drops.lastSecond=0`, no preview freeze, no render stall, and `export.current=null`.

**Latest Phase 5 lifecycle cleanup checkpoint:** AI-node runtime canvases now have both global and clip-scoped cleanup. `clearTimeline()`, manual clip remove, edit-kernel deletes, media deletion, history restore, and timeline-canvas smoke restore clear AI-node runtime caches at their lifecycle boundaries; history restore also stops active timeline audio before direct timeline state replacement. Focused checks passed: `npm run test -- tests/unit/timelineEditOperations.test.ts tests/unit/aiNodeRuntime.test.ts tests/unit/audioScrubSync.test.ts tests/unit/historyRuntimeRehydration.test.ts tests/unit/timelineCanvasSmokeHandlers.test.ts` (96 tests), touched-file ESLint for the lifecycle files, and `npx tsc -p tsconfig.app.json --noEmit --pretty false`. Latest LOC status from `npm run swarm:status`: `242` tracked changed files, `80` new untracked files, tracked `+23283/-16972` net `+6311`, plus `+30144` LOC in new files. Remaining Phase 5 work is live export/playback parity gates plus final default-on decision proof.

**Latest Phase 5 stray cleanup checkpoint:** NativeHelper-resolved timeline placement files now use media-owned primary URL ownership: `timelinePlacementCommands.ts` calls `createPrimaryMediaObjectUrl(...)` when a placement command dereferences a NativeHelper file, so the recovered `MediaFile.url` is tracked by `mediaObjectUrlManager` instead of becoming an unmanaged raw blob URL. `AudioTrackSyncManager.ts` also clears `mixdownGenerating` when lazy composition-audio playback mixdown returns `null`, preventing failed/missing composition mixdowns from leaving the timeline canvas stuck in a generating state. `compositionAudioMixdownCache.ts` completed result retention is now bounded by a `12` entry LRU cap, so nested content-hash churn and cached negative results cannot retain `AudioBuffer` entries without bound for the whole session. Composition mixdown audio elements are now owner-scoped: `compositionAudioMixer.createAudioElement(...)` accepts an owner clip id and creates WAV blob URLs through `blobUrlManager` for the lazy playback path, replacing/revoking prior clip-owned audio URLs and letting existing clip deletion cleanup revoke them. Nested composition refresh clears stale mixdown runtime and revokes the clip-scoped audio URL when the nested content hash changes, while unchanged hashes preserve the current runtime. Waveform, processed-waveform, and audio-analysis preparation now use `requestCompositionAudioMixdown(...)`; the dead split-at-times eager audio-element callback was removed. `spectrogramCanvas.ts` now admits retained raster canvases through the interactive runtime coordinator, reports heap-backed `image-canvas` resources, releases resources on cache replacement/eviction/clear, and draws transient uncached rasters when admission is denied. `proxyFrameCache.ts` now admits audio-proxy `HTMLAudioElement` retention before `new Audio()`, reports it as an interactive `html-media:audio` resource, releases the report with the cached element, and revokes owned blob URLs when admission fails. `AudioTrackSyncManager.ts` now admits retained stem-layer mixer buffers through the interactive runtime coordinator, reports them as `audio-source-clock` resources with heap/duration/sample-rate/channel diagnostics, releases them on LRU eviction and cache clear, and skips cache retention when the interactive heap budget is full. `AudioTrackSyncManager.ts` also gates cloned active audio-proxy elements and stem-preview `HTMLAudioElement`s before allocation, reports them as interactive `html-media:audio` resources, releases them when proxy/stem entries are removed, and denies stem buffer preview before creating a WAV blob URL or audio element when the policy is full. `nodeGraph/aiNodeRuntime.ts` now admits the retained source/output runtime canvases before allocation, reports them as interactive `image-canvas` resources, rechecks admission when cache dimensions change, bounds the cache with entry/heap caps, releases entries when AI nodes are removed or sources disappear, and exposes `clearAINodeRuntimeCache()` for lifecycle/test cleanup. Focused checks passed: `npm run test -- tests/unit/timelinePlacementCommands.test.ts tests/unit/mediaObjectUrlManager.test.ts` (12 tests), `npm run test -- tests/unit/audioScrubSync.test.ts tests/unit/compositionAudioMixdownCache.test.ts` (13 tests), `npm run test -- tests/unit/compositionAudioMixdownCache.test.ts tests/unit/audioScrubSync.test.ts tests/unit/audioExportPipeline.test.ts` (29 tests), `npm run test -- tests/stores/timeline/clipSlice.test.ts tests/unit/timelineEditOperations.test.ts tests/unit/compositionAudioMixer.test.ts tests/unit/audioScrubSync.test.ts tests/unit/compositionAudioMixdownCache.test.ts tests/unit/audioExportPipeline.test.ts` (222 tests), `npm run test -- tests/unit/timelineSpectrogramCanvas.test.ts tests/unit/timelineRuntimeCoordinatorContracts.test.ts` (15 tests), `npm run test -- tests/unit/proxyFrameCache.test.ts tests/unit/timelineSpectrogramCanvas.test.ts tests/unit/timelineRuntimeCoordinatorContracts.test.ts` (32 tests), `npm run test -- tests/unit/audioScrubSync.test.ts tests/unit/proxyFrameCache.test.ts tests/unit/timelineSpectrogramCanvas.test.ts tests/unit/timelineRuntimeCoordinatorContracts.test.ts` (44 tests), `npm run test -- tests/unit/aiNodeRuntime.test.ts tests/unit/timelineRuntimeCoordinatorContracts.test.ts` (17 tests), `npm run test -- tests/unit/audioScrubSync.test.ts tests/unit/timelineRuntimeCoordinatorContracts.test.ts` (24 tests), touched-file ESLint for the placement/audio/spectrogram/proxy-cache/nodeGraph/active-audio files, and `npx tsc -p tsconfig.app.json --noEmit --pretty false`. Latest LOC status from `npm run swarm:status`: `242` tracked changed files, `80` new untracked files, tracked `+23147/-16970` net `+6177`, plus `+30101` LOC in new files. Remaining Phase 5 work is final lifecycle/policy polish plus live export/playback parity gates.

**Latest export allocator admission checkpoint:** Phase 5 now gates the retained export resources before heavy allocation, including the provider/decoder preflight paths. `exportRuntimeReporting.ts` has descriptor factories plus `canRetain...` / reserve helpers for run jobs, output surfaces, preview frames, clip image/video elements, audio buffers, runtime bindings, sequential WebCodecs providers, and parallel decode decoder/frame-buffer resources. `FrameExporter` asks admission before encoder/audio setup for the run job, before zero-copy/readback output surface setup, and softly before preview `VideoFrame.clone()` / `ImageData` / `createImageBitmap`. `ClipPreparation` reserves export image and dedicated PRECISE video element resources before file-backed object URLs, `new Image()`, or detached `video` creation; it also plans media-runtime source ids before `bindSourceRuntimeForOwner()`, reserves FAST sequential frame providers before `File.arrayBuffer()` / `new WebCodecsPlayer()`, and reserves parallel `native-decoder` plus decoded-frame-buffer resources before `new ParallelDecodeManager()` / `ParallelDecodeManager.initialize()`. `AudioExportPipeline` asks admission for source, processed, mix, and master audio buffer stages and treats admission denial as a hard export error instead of falling back to silence. Focused checks passed: `npm run test -- tests/unit/audioExportPipeline.test.ts tests/unit/exportRuntimeReporting.test.ts tests/unit/clipPreparation.test.ts` (26 tests), touched-file ESLint for the touched export/runtime files, and `npx tsc -p tsconfig.app.json --noEmit --pretty false`. Latest LOC status from `npm run swarm:status`: `225` tracked changed files, `76` new untracked files, tracked `+19622/-16573` net `+3049`, plus `+28168` LOC in new files. Remaining allocator work is mainly stray runtime callers outside the now-gated export/RAM/background/slot/composition paths plus final live export/playback parity gates.

**Latest RAM preview allocator admission checkpoint:** Phase 5 now gates the main RAM-preview retained-resource paths before heavy allocations. `ramPreviewSlice` asks `TimelineRuntimeCoordinator.canRetainResource(...)` for the run job before importing/configuring the engine or mutating preview state. `RamPreviewEngine` reserves data-only image resources before file-backed object URL creation and `new Image()`, reserves video runtime-binding/provider/html-media resources before `getRuntimeFrameProvider(...)`, and hard-denial aborts generation without rendering partial cached frames. `verifyVideoPositions()` now uses a non-allocating runtime-provider peek, so hidden/skipped clips are no longer able to create RAM-preview sessions during drift checks. `CacheManager` asks CPU composite-cache admission before constructing `ImageData`, `ScrubbingCache` guards direct CPU cache writes, and `RenderDispatcher` asks GPU frame-cache admission before creating cached playback textures; `ScrubbingCache` also guards GPU cache replacement/LRU/denial paths. Focused checks passed: `npm run test -- tests/unit/ramPreviewRuntimeReporting.test.ts tests/unit/ramPreviewEngineRuntimeReporting.test.ts` (10 tests), the combined runtime/export/RAM admission set `npm run test -- tests/unit/layerPlaybackManagerWarmDeck.test.ts tests/unit/slotDeckManager.test.ts tests/unit/compositionRendererRuntimeReporting.test.ts tests/unit/exportRuntimeReporting.test.ts tests/unit/clipPreparation.test.ts tests/unit/ramPreviewRuntimeReporting.test.ts tests/unit/ramPreviewEngineRuntimeReporting.test.ts` (39 tests), touched-file ESLint, and `npx tsc -p tsconfig.app.json --noEmit --pretty false`. Latest LOC status from `npm run swarm:status`: `225` tracked changed files, `76` new untracked files, tracked `+19622/-16573` net `+3049`, plus `+28168` LOC in new files.

**Latest shared image hydrator allocator admission checkpoint:** Phase 5 now gates the shared image hydration paths before they create `HTMLImageElement`s. `imageRuntimeHydrator.ts` accepts policy/owner/source resource metadata, asks `TimelineRuntimeCoordinator.canRetainResource(...)` before `new Image()`, reserves admitted resources under the caller policy, releases on cancel/stale/error, and returns an explicit denied handle without image allocation. `compositionRenderer`, `layerPlaybackManager`, and `slotDeckManager` pass resource ids that match their existing final reporting ids, so reservation and ready-state reporting do not double count. Denied composition image loads resolve/clean pending state, denied background image loads clear loading state, and denied slot image loads do not inflate `preparedClipCount`. Focused checks passed: `npm run test -- tests/unit/layerPlaybackManagerWarmDeck.test.ts tests/unit/slotDeckManager.test.ts tests/unit/compositionRendererRuntimeReporting.test.ts` (18 tests), touched-file ESLint, and `npx tsc -p tsconfig.app.json --noEmit --pretty false`. Latest LOC status from `npm run swarm:status`: `223` tracked changed files, `76` new untracked files, tracked `+18094/-16416` net `+1678`, plus `+26547` LOC in new files. This image-specific checkpoint is superseded by the later background/slot/composition video/audio admission checkpoint.

**Latest background/slot/composition non-image allocator admission checkpoint:** Phase 5 now also gates the main non-image background-layer, warm slot-deck, and non-active composition-render browser media paths before heavy allocation. `runtimeResourceReporting.reservePlannedClipRuntimeResources(...)` reserves planned runtime-binding plus HTML video/audio descriptors under the same IDs later used by final reporting. `layerPlaybackManager` and `slotDeckManager` plan runtime source/session IDs before background/slot video/audio element creation, deny cleanly before `document.createElement(...)`, and release reservations on stale/error paths. `compositionRenderer` reserves composition-render video runtime-binding/html-media resources before object URL creation and detached `video` allocation. Focused checks passed with the combined runtime set above plus touched-file ESLint and `npx tsc -p tsconfig.app.json --noEmit --pretty false`.

**Latest interactive runtime-provider admission checkpoint:** Phase 5 now gates interactive scrub WebCodecs provider creation before `new WebCodecsPlayer(...)`. `ensureRuntimeFrameProvider()` reserves an interactive runtime binding plus `video-frame-provider` resource before creating/loading the dedicated scrub provider, returns `null` without constructing a player when the frame-provider budget is full, releases a freshly opened scrub session on admission denial, releases retained coordinator resources on load failure, and clears retained runtime-provider resources when `releaseRuntimePlaybackSession(...)` runs. Focused checks passed: `npm run test -- tests/unit/mediaRuntime.test.ts` (19 tests), the combined runtime/export/RAM admission set with `mediaRuntime.test.ts` included (58 tests), touched-file ESLint, and `npx tsc -p tsconfig.app.json --noEmit --pretty false`. Latest LOC status from `npm run swarm:status`: `226` tracked changed files, `76` new untracked files, tracked `+19843/-16573` net `+3270`, plus `+28205` LOC in new files.

**Latest legacy WebCodecs helper provider admission checkpoint:** The remaining direct `new WebCodecsPlayer(...)` in `stores/timeline/helpers/webCodecsHelpers.ts` is now gated before construction. `initWebCodecsPlayer()` reserves an interactive `video-frame-provider` resource, returns `null` when the provider budget is full without constructing the helper player, releases the reservation on init failure, and patches `destroy()` so successful helper providers release their coordinator resource. Focused checks passed: `npm run test -- tests/unit/webCodecsHelpers.test.ts` (5 tests), the combined runtime/export/RAM/WebCodecs-helper admission set (63 tests), touched-file ESLint, and `npx tsc -p tsconfig.app.json --noEmit --pretty false`. Latest LOC status from `npm run swarm:status`: `227` tracked changed files, `76` new untracked files, tracked `+20010/-16574` net `+3436`, plus `+28227` LOC in new files.

**Latest Vector runtime canvas admission checkpoint:** Phase 5 now gates Lottie/Rive runtime canvas creation before `document.createElement('canvas')` or player construction. `vectorRuntimeReporting.ts` builds caller-owned `image-canvas` descriptors, asks `TimelineRuntimeCoordinator.canRetainResource(...)`, retains admitted canvases, and exposes a consistent admission error. `LottieRuntimeManager` and `RiveRuntimeManager` reserve before allocation, release on constructor/load failure, and release retained resources on `destroyClipRuntime`. `VectorAnimationRuntimeManager.prepareClipSource(...)` now accepts optional runtime ownership options; background-layer, slot-deck, composition-render, and export preparation pass their real policy/owner/resource IDs so the retained runtime canvas is charged to the caller that owns it and cleanup uses the existing owner/run clears. Focused checks passed: `npm run test -- tests/unit/vectorRuntimeReporting.test.ts tests/unit/timelineRuntimeCoordinatorContracts.test.ts tests/unit/exportRuntimeReporting.test.ts tests/unit/clipPreparation.test.ts tests/unit/layerPlaybackManagerWarmDeck.test.ts tests/unit/slotDeckManager.test.ts tests/unit/compositionRendererRuntimeReporting.test.ts` (40 tests), touched-file ESLint, and `npx tsc -p tsconfig.app.json --noEmit --pretty false`. Latest LOC status from `npm run swarm:status`: `232` tracked changed files, `78` new untracked files, tracked `+20466/-16848` net `+3618`, plus `+28675` LOC in new files.

**Latest video-bake proxy admission checkpoint:** Phase 5 now gates composition video-bake proxy videos before hidden browser media allocation. `videoBakeProxyCache.registerCompositionArtifact(...)` builds a `composition-render` `html-media/video` descriptor, asks `TimelineRuntimeCoordinator.canRetainResource(...)`, and only then creates the blob URL and hidden `video` element. Denial creates no blob URL and no video element; admitted resources are released on `remove(...)`, `clear()`, and failed ready-load cleanup. Focused checks passed: `npm run test -- tests/unit/videoBakeProxyCache.test.ts tests/unit/layerBuilderService.test.ts` (28 tests), touched-file ESLint, and `npx tsc -p tsconfig.app.json --noEmit --pretty false`. Latest LOC status from `npm run swarm:status`: `233` tracked changed files, `79` new untracked files, tracked `+20580/-16882` net `+3698`, plus `+28813` LOC in new files.

**Latest legacy JPEG proxy-frame cache admission checkpoint:** Phase 5 now reports and gates the retained legacy JPEG/`HTMLImageElement` proxy-frame cache before inserting decoded images into the service-level cache. `proxyFrameCache` aggregates retained JPEG frames into one `thumbnail` policy `image-canvas/html-image` resource per media file, uses projected heap bytes for admission, skips cache insertion when the thumbnail heap budget is full, refreshes the aggregate descriptor after LRU eviction, and releases the resource on media-specific or full cache clear. Focused checks passed: `npm run test -- tests/unit/proxyFrameCache.test.ts` (9 tests), touched-file ESLint, and `npx tsc -p tsconfig.app.json --noEmit --pretty false`. Latest LOC status from `npm run swarm:status`: `235` tracked changed files, `79` new untracked files, tracked `+20855/-16896` net `+3959`, plus `+28813` LOC in new files.

**Latest proxyFrameCache decoded AudioBuffer admission checkpoint:** Phase 5 now reports and gates retained decoded `AudioBuffer`s in `proxyFrameCache`. Decoded scrub/playback audio buffers are represented as `interactive` policy `audio-source-clock` resources with heap-byte, duration, sample-rate, and channel-count diagnostics. Admission is checked before inserting a decoded buffer into the service cache; denied buffers are returned to the immediate caller but not retained. AudioBuffer resources are released when entry-count/byte LRU eviction drops a buffer and on `clearForMedia(...)`, `clearAll()`, `clearAudioBufferCache()`, or `disposeAudioContext()`. Focused checks passed: `npm run test -- tests/unit/proxyFrameCache.test.ts` (12 tests), touched-file ESLint, and `npx tsc -p tsconfig.app.json --noEmit --pretty false`. Latest LOC status from `npm run swarm:status`: `235` tracked changed files, `79` new untracked files, tracked `+21030/-16900` net `+4130`, plus `+28845` LOC in new files.

**Latest proxyFrameCache WebCodecs VideoFrame admission checkpoint:** Phase 5 now reports and gates retained WebCodecs `VideoFrame`s in `proxyFrameCache`. Cached proxy frames are represented as one aggregate `interactive` policy `video-frame-provider` resource per media file, with decoded-frame byte estimates and dimensions growing with the retained frame count. Admission is checked before cache insertion after LRU eviction has made space; denied decoded frames are closed and returned as `null` from `getVideoFrame(...)` so the cache does not leak ownerless `VideoFrame`s. Aggregate resources refresh after per-frame LRU eviction and release on media/full cache clear. Focused checks passed: `npm run test -- tests/unit/proxyFrameCache.test.ts` (15 tests), touched-file ESLint, and `npx tsc -p tsconfig.app.json --noEmit --pretty false`. Latest LOC status from `npm run swarm:status`: `235` tracked changed files, `79` new untracked files, tracked `+21323/-16912` net `+4411`, plus `+28861` LOC in new files.

**Latest split media-runtime data-only, FFmpeg export-prep run-id, audio bake/unbake, and stem source-switch checkpoint:** Phase 5 now removes the remaining split-time retained media element cloning for ordinary media, not only composition-audio. `splitBatchOperations.ts` uses a shared data-only source stripper for new clip parts, and legacy `clipSlice.splitClip` uses the same helpers. Split-created video/audio parts preserve source metadata (`naturalDuration`, `mediaFileId`, `runtimeSourceId`, `runtimeSessionKey`, `filePath`) but do not carry `videoElement`, `audioElement`, `webCodecsPlayer`, or `nativeDecoder`; lazy/runtime paths hydrate when the part is actually needed. `FFmpegFrameRenderer.initialize()` now passes its `runtimeRunId` into `prepareClipsForExport(...)`, so the existing export admission gates apply to FFmpeg video/GIF/browser GIF/image-sequence frame preparation, and failed initialization releases the reported job/resources. Audio edit bake/unbake now also keeps baked/restored audio clip sources data-only: it preserves `File`, `mediaFileId`, duration, waveform refs, and source metadata without storing `HTMLAudioElement`. Stem source switching follows the same rule: it disposes stale audio elements, writes only file/media/duration/source metadata, and lets lazy/audio sync hydrate the active source. Focused checks passed: `npm run test -- tests/unit/timelineEditOperations.test.ts tests/stores/timeline/clipSlice.test.ts` (190 tests), `npm run test -- tests/unit/ffmpegFrameRendererRuntimeReporting.test.ts tests/unit/clipPreparation.test.ts tests/unit/exportRuntimeReporting.test.ts tests/unit/exportLayerBuilder.test.ts` (27 tests), `npm run test -- tests/stores/timeline/audioEditBakeSlice.test.ts` (2 tests), `npm run test -- tests/stores/timeline/stemSeparationSlice.test.ts` (10 tests), touched-file ESLint, and `npx tsc -p tsconfig.app.json --noEmit --pretty false`. Latest LOC status from `npm run swarm:status`: `231` tracked changed files, `76` new untracked files, tracked `+20267/-16796` net `+3471`, plus `+28355` LOC in new files.

**Latest primary lazy media/image allocator admission checkpoint:** Phase 5 now gates the main interactive lazy allocation paths before they create browser resources. `lazyMediaElements.ts` creates a planned `html-media` descriptor and asks `TimelineRuntimeCoordinator.canRetainResource(...)` before `getLazySource(...)`, file-backed object URL creation, video/audio DOM element creation, and clip-source mutation. `lazyImageElements.ts` now plans source identity before allocation, asks admission before `new Image()` or file-backed `URL.createObjectURL(...)`, leaves denied clips data-only/retryable, and replaces stale same-clip image records when the underlying image URL changes. `LayerBuilderService` returns zero layers for an over-budget data-only image instead of mutating `clip.source.imageElement`. Focused checks passed: `npm run test -- tests/unit/lazyMediaElements.test.ts tests/unit/layerBuilderService.test.ts tests/unit/timelineRuntimeCoordinatorContracts.test.ts` (47 tests), touched-file ESLint, and `npx tsc -p tsconfig.app.json --noEmit --pretty false`. Latest LOC status from `npm run swarm:status`: `223` tracked changed files, `76` new untracked files, tracked `+17856/-16418` net `+1438`, plus `+26393` LOC in new files. Remaining allocator work is tracked by the newer background/slot/composition, RAM-preview, and export admission checkpoints.

**Latest thumbnail allocator admission checkpoint:** Phase 5 now has concrete thumbnail admission gates instead of reporting-only runtime accounting. `TimelineRuntimeCoordinator.canRetainResource(...)` predicts whether a resource can be retained under a policy's hard budget without mutating the retained-resource ledger. `thumbnailCacheService` asks before starting cached thumbnail DB-load jobs or thumbnail generation jobs, so over-budget jobs do not touch IndexedDB, create detached videos, or mark the source generating. Detached thumbnail generation video and canvas resources also ask for admission before being retained; denied video resources are cleaned up and reset to retryable `none`, while denied canvases return `false` before frame work. `thumbnailBitmapCache` builds the decoded thumbnail bitmap descriptor before caching, asks the coordinator for admission, and closes over-budget decoded `ImageBitmap`s instead of retaining them or firing `onReady`. Export allocator admission was completed in a later checkpoint; broader remaining allocator ownership is now stray runtime callers plus final live parity proof. Focused checks passed: `npm run test -- tests/unit/thumbnailCacheService.test.ts tests/unit/thumbnailBitmapCache.test.ts tests/unit/timelineRuntimeCoordinatorContracts.test.ts` (31 tests), touched-file ESLint, and `npx tsc -p tsconfig.app.json --noEmit --pretty false`. Latest LOC status from `npm run swarm:status`: `223` tracked changed files, `76` new untracked files, tracked `+17705/-16418` net `+1287`, plus `+26071` LOC in new files.

**Latest Vector user-action data-only checkpoint:** Phase 5 now covers Vector user actions outside restore. Direct Lottie/Rive add writes file plus source metadata only and does not store `textCanvas`; vector paste and media relink/reload follow the same data-only rule and do not call `prepareClipSource(...)` during the user action. `LayerBuilderService` and `ExportLayerBuilder` no longer require `clip.source.textCanvas` for vector clips; they consume the runtime canvas returned by `vectorAnimationRuntimeManager.renderClipAtTime(...)` and leave preparation to the vector runtime/export preparation path. Focused checks passed: `npm run test -- tests/unit/vectorUserActionDataOnly.test.ts tests/unit/clipboardPasteDataOnly.test.ts tests/stores/mediaStore/fileManageSlice.test.ts tests/unit/layerBuilderService.test.ts tests/unit/exportLayerBuilder.test.ts` (163 tests), touched-file ESLint, and `npx tsc -p tsconfig.app.json --noEmit --pretty false`. Latest LOC status from `npm run swarm:status`: `223` tracked changed files, `76` new untracked files, tracked `+17352/-16420` net `+932`, plus `+25963` LOC in new files.

**Latest 3D user-action media-owned URL checkpoint:** Phase 5 now covers the first 3D user-action URL ownership cleanup. Direct model and gaussian-splat load fallbacks use a media-owned primary URL when a `mediaFileId` exists, while preserving the old clip-scoped fallback only for clips with no media identity. Model paste now reuses `mediaFile.url` or creates a primary media URL instead of an unmanaged `URL.createObjectURL(mediaFile.file)`. `updateTimelineClips()` prefers media-owned URLs when reloading single model, gaussian-avatar, and gaussian-splat clips; model/gaussian sequence frame URL reuse remains unchanged. NativeHelper-backed external timeline drop recovery now writes a managed primary media URL when it reattaches a browser `File`. Focused checks passed: `npm run test -- tests/unit/spatialClipUrlOwnership.test.ts tests/unit/clipboardPasteDataOnly.test.ts tests/stores/mediaStore/fileManageSlice.test.ts` (123 tests), touched-file ESLint, and `npx tsc -p tsconfig.app.json --noEmit --pretty false`. Latest LOC status from `npm run swarm:status`: `221` tracked changed files, `75` new untracked files, tracked `+17208/-16324` net `+884`, plus `+25805` LOC in new files.

**Latest download completion video/audio data-only checkpoint:** Phase 5 now covers YouTube/native download completion for downloaded video and linked audio clips. `completeDownload()` no longer stores `HTMLVideoElement`, `HTMLAudioElement`, WebCodecs players, or clip-owned video/audio blob URLs in clip sources. It reuses import metadata for duration when available, only falls back to a temporary metadata probe if duration is missing, writes downloaded video and linked audio as `{ type, naturalDuration, mediaFileId }`, and warms source thumbnails with `thumbnailCacheService.generateForSourceUrl(...)` through the media-owned URL. Focused checks passed: `npm run test -- tests/unit/completeDownloadDataOnly.test.ts`, the combined migrated user-action run `npm run test -- tests/unit/addVideoClip.test.ts tests/unit/clipboardPasteDataOnly.test.ts tests/stores/mediaStore/fileManageSlice.test.ts tests/unit/completeDownloadDataOnly.test.ts tests/unit/lazyMediaElements.test.ts` (129 tests), touched-file ESLint, and `npx tsc -p tsconfig.app.json --noEmit --pretty false`. Latest LOC status from `npm run swarm:status`: `218` tracked changed files, `74` new untracked files, tracked `+17135/-16321` net `+814`, plus `+25666` LOC in new files.

**Latest media relink/reload video/audio data-only checkpoint:** Phase 5 now covers manual and batch media reload for browser video/audio clips through `fileManageSlice.updateTimelineClips()`. Reloaded video/audio clips now get the replacement `File`, clear `needsReload`/`isLoading`, and store only `{ type, naturalDuration, mediaFileId }` source metadata; reload no longer creates `HTMLVideoElement`s, `HTMLAudioElement`s, or reload-time clip media blob URLs. Video thumbnail regeneration now calls `thumbnailCacheService.generateForSourceUrl(...)` using the media-owned primary source URL instead of holding a clip-owned video element. Focused checks passed: `npm run test -- tests/stores/mediaStore/fileManageSlice.test.ts` (118 tests), touched-file ESLint, and `npx tsc -p tsconfig.app.json --noEmit --pretty false`. Latest LOC status from `npm run swarm:status`: `217` tracked changed files, `73` new untracked files, tracked `+17097/-16271` net `+826`, plus `+25537` LOC in new files.

**Latest timeline video/audio paste data-only checkpoint:** Phase 5 now covers timeline paste/duplicate for browser video/audio sources. `clipboardSlice.pasteClips()` already created pasted video/audio clips as data-only placeholders; the async media reload phase now preserves that boundary instead of creating pasted `HTMLVideoElement`s, `HTMLAudioElement`s, WebCodecs players, or video/audio blob URLs. Pasted clips now regain the actual `File`, `mediaFileId`, duration, `isLoading:false`, and `needsReload:false`, leaving element ownership to `lazyMediaElements`. The remaining `URL.createObjectURL(mediaFile.file)` in `clipboardSlice.ts` is the model paste path, not video/audio. Focused checks passed: `npm run test -- tests/unit/clipboardPasteDataOnly.test.ts tests/unit/addVideoClip.test.ts tests/unit/lazyMediaElements.test.ts` (10 tests), touched-file ESLint, and `npx tsc -p tsconfig.app.json --noEmit --pretty false`. Latest LOC status from `npm run swarm:status`: `217` tracked changed files, `73` new untracked files, tracked `+17032/-16207` net `+825`, plus `+25524` LOC in new files.

**Latest direct browser video/audio add data-only checkpoint:** Phase 5 now covers the first non-restore user-action hydration path for browser video/audio adds. `loadVideoMedia()` still uses a temporary local video element for metadata/native scale probing, but it no longer stores `source.videoElement`, no longer stores a `WebCodecsPlayer`, no longer registers that temporary element through `blobUrlManager`, and releases the temporary element after probing. Thumbnail generation now uses `thumbnailCacheService.generateForSourceUrl(...)` with a separate source URL that is revoked after the thumbnail job when it is not media-owned. Linked video audio now keeps `source: { type:'audio', naturalDuration, mediaFileId }` and waveform metadata only. `loadAudioMedia()` still probes metadata with a temporary local audio element, then releases it and stores a data-only audio source. The old exported `primeImportedVideoFrame()` helper was deleted because no product path uses imported clip pre-cache ownership anymore. Focused checks passed: `npm run test -- tests/unit/addVideoClip.test.ts tests/unit/lazyMediaElements.test.ts` (9 tests), touched-file ESLint, and `npx tsc -p tsconfig.app.json --noEmit --pretty false`. Latest LOC status from `npm run swarm:status`: `217` tracked changed files, `72` new untracked files, tracked `+16995/-16125` net `+870`, plus `+25383` LOC in new files.

**Latest composition-audio mixdown-on-demand checkpoint:** Phase 5 now has the first shared lazy contract for composition audio after the AddComp/Split placeholder cleanup. `compositionAudioMixdownCache.ts` de-dupes composition mixdown requests by composition id plus nested content hash, returns existing buffers without re-mixing, caches null/no-audio results behind the current bounded LRU policy, and patches timeline clips with generated buffer/waveform state. `AudioTrackSyncManager` now requests an element only when active playback needs composition audio, skips visual-composition fallback when an active linked composition-audio clip already owns playback, and preserves data-only clip sources until the lazy element is actually attached. `AudioExportPipeline` now asks the helper for a mixdown buffer before audio extraction and skips empty data-only placeholder files when a composition has no audio. Focused checks passed: `npm run test -- tests/unit/compositionAudioMixdownCache.test.ts tests/unit/audioExportPipeline.test.ts tests/unit/audioScrubSync.test.ts tests/unit/addCompClipNestedRestore.test.ts tests/stores/timeline/clipSlice.test.ts tests/unit/timelineEditOperations.test.ts` (244 tests), touched-file ESLint, and `npx tsc -p tsconfig.app.json --noEmit --pretty false`. Latest LOC status from `npm run swarm:status`: `213` tracked changed files, `72` new untracked files, tracked `+16809/-15999` net `+810`, plus `+25359` LOC in new files.

**Historical fixed-comp verification checkpoint:** The earlier 2026-06-04 browser/bridge target was `Random 3 Videos - 50 Cuts Test 164025` (`1780584025151-mx0znx977`). It is no longer the current live verification target; use `Subcomposition 1` (`1780705391680-ecu7panot`) unless another target is explicitly rebuilt/restored. After an earlier synthetic large-smoke polluted the active timeline, the same comp id was rebuilt through the AI tools from the three real Media Library videos: 50 video segments plus 50 linked audio clips, distributed as 9 Anduril / 23 Bolt / 18 FPV source segments across 3 video tracks and 2 audio tracks. Synthetic verification smokes now restore the active timeline by default where they can structurally mutate it: large-project, thumbnail-reload, marquee, Blade, playhead when requested, and synthetic RAM preview. The runner passes `restoreTimelineAfterRun:true` for all synthetic mutation smokes it owns, and reopens the live comp before final stats, so temporary synthetic load tests no longer leave the browser on Smoke clips. The user-reported playhead flicker was reproduced by `runTimelineCanvasPlayheadSmoothnessSmoke`; root cause was clock-driven playback positions briefly moving backward while the DOM playhead read the high-frequency internal clock. `PlayheadState` now clamps forward clock-driven positions monotonically, and `usePlaybackLoop` rejects backward positions during normal forward playback. Direct live-comp proof passed with `forwardDistancePx=28.81`, `backtrackCount=0`, `maxBacktrackPx=0`, 73 samples, and restore back to 100 clips. The latest mixed live/synthetic runner proof `fixtures/timeline-canvas-reports/run-20260604-160121Z/report.json` passed with live `50` video + `50` audio clips, reload first stats at `1506ms`, ready at `3170ms`, Large-240 at `60fps` with `0` dropped/slow frames, thumbnail reload, playhead, Blade, and marquee all passing. A focused default Large-720 proof `fixtures/timeline-canvas-reports/run-20260604-160241Z/report.json` passed with `60fps`, `0` dropped frames, `0` slow frames, max frame delta `17.51ms`, and the fixed comp still at `100` clips afterwards. Current LOC status from `npm run swarm:status`: `164` tracked changed files, `44` new untracked files, tracked `+7270/-12508` net `-5238`, plus `+11794` LOC in new files. Focused checks passed: `npm run test -- tests/unit/playheadState.test.ts tests/unit/timelineCanvasSmokeHandlers.test.ts`, touched-file ESLint, `npx tsc -p tsconfig.app.json --noEmit --pretty false`, live-only runner, Large-240 runner, and focused Large-720 runner.

**Earlier worker-readiness checkpoint:** Phase 6 worker hardening first established a real contract instead of the old raw worker post shape. `timelineClipCanvasWorkerModel.ts` built structured-clone-safe worker draw messages from main-thread resolved `x/width` geometry and selected/hovered state only; it returned explicit eligibility/fallback reasons before anything could cross the worker boundary. At that earlier point, worker mode required runtime support plus eligibility and intentionally fell back on the main-thread canvas path for thumbnail video clips, source timing/reverse visuals, composition decorations, audio waveform/spectrogram resources, fade visuals, passive decorations, drag preview, active drag, trim, and waveform-enabled tracks. Those fallback classes have since been narrowed by the worker-runtime, forced-worker, and worker-resource checkpoints below. Focused checks passed at that checkpoint: `npm run test -- tests/unit/timelineClipCanvasWorkerModel.test.ts tests/unit/timelineCanvasDiagnostics.test.ts`, touched-file ESLint, and `npx tsc -p tsconfig.app.json --noEmit --pretty false`. Historical LOC status from `npm run swarm:status`: `165` tracked changed files, `46` new untracked files, tracked `+7374/-12534` net `-5160`, plus `+12109` LOC in new files.

**Latest worker-runtime checkpoint:** Phase 6 worker mode now has an acknowledged runtime protocol and recoverable fallback instead of fire-and-forget drawing. Worker draw messages include `requestId`; the worker posts `ready`, per-draw `drawn` acks with `drawMs`, `drawnClipCount`, and backing-store `resourceBytes`, and explicit `error` messages for failed init/draw. `TimelineClipCanvas` no longer marks the worker ready immediately after `postMessage(init)`: it queues the latest draw until `ready`, ignores stale draw acks, handles `worker.onerror`, `worker.onmessageerror`, post/init failures, and ready timeout, terminates the worker on failure, records a runtime fallback reason, and remounts the `<canvas>` with a generation key so the main-thread path never tries to draw into a transferred OffscreenCanvas. `timelineCanvasDiagnostics` now exposes `workerPendingDraw`, `workerDrawMs`, `workerResourceBytes`, and `workerError`, and totals aggregate pending tracks, draw report counts, draw-ms total/max, resource bytes, and runtime error counts. Focused checks passed: `npm run test -- tests/unit/timelineClipCanvasWorkerModel.test.ts tests/unit/timelineCanvasDiagnostics.test.ts`, touched-file ESLint, `npx tsc -p tsconfig.app.json --noEmit --pretty false`, and focused live runner `fixtures/timeline-canvas-reports/run-20260604-163643Z/report.json` on `1780584025151-mx0znx977` with reload first stats `2767ms`, ready `3289ms`, 100 input clips, `domClipBodyCount=0`, and no verification failures. Current LOC status from `npm run swarm:status`: `165` tracked changed files, `48` new untracked files, tracked `+7665/-12550` net `-4885`, plus `+13048` LOC in new files. Remaining worker follow-ups: force-flag browser smokes for worker-positive synthetic solids and forced live-comp fallback, plus transferable thumbnail/waveform/spectrogram resource support.

**Latest forced-worker smoke checkpoint:** `runTimelineCanvasLargeProjectSmoke` now supports temporary `forceTimelineCanvasWorker` and optional synthetic `waveformsEnabled` overrides, restoring both timeline state and the feature flag even on failure. It can now assert `minWorkerTrackCount`, `minWorkerEligibleTrackCount`, `maxWorkerFallbackTrackCount`, and `requiredWorkerFallbackReasons`. `scripts/run-timeline-canvas-verification.mjs --include-worker-smokes` adds official worker blocks for solid clips, thumbnail-strip video clips, and live-comp fallback. Historical runner proof `fixtures/timeline-canvas-reports/run-20260604-164402Z/report.json` passed: synthetic 720 clips / 8 tracks reported `workerTrackCount=8`, `workerEligibleTrackCount=8`, `workerFallbackTrackCount=0`, `workerDrawReportCount=8`, `workerDrawMsMax=0.4ms`, `workerResourceBytes=26809728`, 57.33 fps, 2 estimated dropped frames, 0 legacy DOM clip bodies, and restored `timelineCanvasWorker=false`; forced live fallback on `1780584025151-mx0znx977` reported `workerTrackCount=0`, `workerFallbackTrackCount=5`, fallback reasons including `thumbnail-visuals`, `waveforms-enabled`, `source-timing-visuals`, `passive-decorations`, and `audio-resource-visuals`, 56 fps, `domClipBodyCount=0`, restored to 100 clips, and no verification failures. Latest direct browser worker-thumbnail proof targeted the visible bridge tab with `runTimelineCanvasThumbnailReloadSmoke(forceTimelineCanvasWorker:true)`: success, `thumbnailDrawCount=18`, `workerTrackCount=1`, `workerEligibleTrackCount=1`, `workerFallbackTrackCount=1`, `workerResourceBytes=1132080`, `domClipBodyCount=0`, and `timelineCanvasWorker` restored to `false`. This smoke found and fixed two worker handoff bugs: switching from a main-thread 2D canvas to a worker now remounts a fresh canvas before `transferControlToOffscreen()`, and worker thumbnail preparation now actively decodes missing `ImageBitmap`s instead of relying on the main-thread draw path to do it. Focused checks passed: `npm run test -- tests/unit/timelineCanvasSmokeHandlers.test.ts tests/unit/TimelineClipCanvasWorkerRuntime.test.tsx tests/unit/timelineClipCanvasWorkerModel.test.ts tests/unit/thumbnailBitmapCache.test.ts`, touched-file ESLint including the runner, `npx tsc -p tsconfig.app.json --noEmit --pretty false`, direct browser worker probes, and the focused worker runner.

**Latest worker-component checkpoint:** `TimelineClipCanvas` now has a focused React component test with a fake Worker and fake OffscreenCanvas transfer. The test covers the real lifecycle contract that model tests and bridge smokes cannot see: the component posts only `init` before `ready`, queues the first structured draw message while the worker is still cold, sends that draw after `ready`, records `drawn` acks into `timelineCanvasDiagnostics`, then handles `onmessageerror` by terminating the worker, remounting a fresh `<canvas>`, and reporting a main-thread fallback with `worker-messageerror`. This found and fixed a real initial-draw bug: the draw effect previously returned before building the pending draw when `workerReadyRef` was false, so the first `ready` event could have no queued draw to send. Focused checks passed: `npm run test -- tests/unit/TimelineClipCanvasWorkerRuntime.test.tsx tests/unit/timelineClipCanvasWorkerModel.test.ts tests/unit/timelineCanvasDiagnostics.test.ts`, touched-file ESLint, and `npx tsc -p tsconfig.app.json --noEmit --pretty false`.

**Latest worker-resource checkpoint:** Phase 6 now has prepared transferable/resource paths for waveform columns, spectrogram rasters, bounded thumbnail strips, passive badge/progress decorations, active trim/source-extension visuals, fade curve geometry, normal active drag/drag-preview geometry, Slip/Slide tool geometry, and composition visuals instead of a geometry-only worker payload. `timelineClipCanvasWorkerModel.ts` accepts per-clip prepared resources, clones waveform/spectrogram/fade/composition numeric data into fresh `Float32Array`s, returns the matching transfer list, and accepts fresh per-draw thumbnail-strip `ImageBitmap`s without transferring cache-owned thumbnail bitmaps. `TimelineClipCanvas` prepares waveform/spectrogram resources, fresh normal/reversed thumbnail strips, passive status/transcript/analysis resources, active trim body/source-extension ghost rects, shared fade curve geometry, worker-visible clip snapshots with drag/preview/Slide `startTime/duration/inPoint/outPoint`, Slip-shifted `inPoint/outPoint`, and composition resources for outlines, nested boundaries, segment thumbnail composites, segment rects, mixdown waveform, and generating-audio labels. Source-timed/trimmed/reversed/composition video clips can enter worker mode when their visible source range and visual resources are represented by prepared transferables; reversed transcript and analysis ratios are mirrored before transfer, and active trim/fade/ordinary drag/Slip/Slide/composition visuals no longer force fallback when prepared visuals exist. The old `clip-drag-gesture-active` fallback was removed: Slide posts resolved worker geometry, and Slip prepares thumbnail/source-dependent resources from the shifted source range. The worker draws transferred thumbnail strips, composition segment composites/boundaries/mixdown/outline, spectrogram rasters, waveform columns, trim source-extension ghosts, fade fills/curves/markers, and passive badge/progress/transcript/analysis overlays, closes transferred thumbnail strip bitmaps after draw, and reports thumbnail counts plus resource bytes in its `drawn` ack. Smoke assertions and the verification runner now accept `maxWorkerResourceBytes`, `maxWorkerPendingTrackCount`, and `maxWorkerErrorTrackCount`, so worker-positive smokes can fail on resource-budget, pending-draw, and worker-error regressions instead of only counting drawn tracks. Focused checks passed: `npm run test -- tests/unit/timelineCanvasSmokeHandlers.test.ts tests/unit/timelineClipCanvasWorkerModel.test.ts tests/unit/TimelineClipCanvasWorkerRuntime.test.tsx` (43 tests), `node scripts/run-timeline-canvas-verification.mjs --help`, touched-file ESLint, `npx tsc -p tsconfig.app.json --noEmit --pretty false`, and the direct browser worker-thumbnail smoke above. Historical LOC status from `npm run swarm:status`: `169` tracked changed files, `49` new untracked files, tracked `+9695/-12697` net `-3002`, plus `+15392` LOC in new files.

**Latest worker-proof checkpoint:** The focused forced-worker browser proof now passes against the real fixed comp and worker-positive synthetic paths. The runner/bridge was hardened for reload-heavy proof runs: AI bridge tab ids are stable per browser tab through `sessionStorage`, bridge presence is refreshed every 3s and on targeted tool execution, and `scripts/run-timeline-canvas-verification.mjs` re-picks the fresh visible tab before each worker smoke. The forced thumbnail worker cold path was also hardened: `runTimelineCanvasThumbnailReloadSmoke(forceTimelineCanvasWorker:true)` warms source thumbnail `ImageBitmap`s after cache eviction, and worker thumbnail strip prep tolerates null slots instead of dropping an otherwise drawable strip. Focused runner proof `fixtures/timeline-canvas-reports/run-20260604-192921Z/report.json` passed with no verification failures: reload first stats `2773ms`, ready `3837ms`, live `50` video + `50` audio clips, `domClipBodyCount=0`, worker synthetic `720` clips / `8` tracks at `60fps` with `0` dropped frames, `0` slow frames, `workerTrackCount=8`, `workerFallbackTrackCount=0`, `workerPendingTrackCount=0`, `workerErrorTrackCount=0`, and `workerResourceBytes=26809728`; worker-thumbnail synthetic passed with `18` clips, `2` worker tracks, `36` thumbnail draws, `0` fallback tracks, `0` pending/error tracks, and `workerResourceBytes=3289392`; forced live fallback passed on the 100-clip comp with `60fps`, `0` dropped/slow frames, `workerTrackCount=0`, `workerFallbackTrackCount=5`, fallback reasons `source-timing-visuals` and `thumbnail-visuals`, and no worker pending/errors. At this historical checkpoint the product worker flag was still off; the current branch now sets `flags.timelineCanvasWorker` to `true` and the runner executes the unforced default-on live proof by default. Focused checks passed: `npm run test -- tests/unit/timelineCanvasSmokeHandlers.test.ts tests/unit/TimelineClipCanvasWorkerRuntime.test.tsx tests/unit/timelineClipCanvasWorkerModel.test.ts tests/unit/thumbnailBitmapCache.test.ts` (47 tests), touched-file ESLint, `node scripts/run-timeline-canvas-verification.mjs --help`, `npx tsc -p tsconfig.app.json --noEmit --pretty false`, and the focused forced-worker runner. Full verification follow-up passed in `fixtures/timeline-canvas-reports/run-20260604-193416Z/report.json` with no verification failures: reload first stats/ready `1251ms`, live `Random 3 Videos - 50 Cuts Test 164025` (`1780584025151-mx0znx977`) at `50` video + `50` audio clips and `domClipBodyCount=0`, live playback/scrub/playback-path success, export-preview parity success, Large-720 at `60fps` with `0` dropped/slow frames, worker synthetic `720` clips / `8` tracks at `58.67fps` with `0` fallback/pending/error tracks and `workerResourceBytes=26809728`, worker-thumbnail synthetic `36` draws on `2` worker tracks, forced live fallback with expected `source-timing-visuals`/`thumbnail-visuals` fallback reasons and no pending/errors, playhead backtrack `0`, thumbnail reload/RAM/spectral smokes passing, and final stats `engineReady=true`. Full gates passed after that run: `npm run build`, `npm run lint`, and `npm run test` (`343` files, `3602` tests). Current LOC status from `npm run swarm:status`: `170` tracked changed files, `49` new untracked files, tracked `+9727/-12702` net `-2975`, plus `+15476` LOC in new files.

**Latest context-menu command-model checkpoint:** The first clip/track/empty context-menu command-model extraction is implemented. `TimelineContextMenu.tsx` now delegates pure target/link/lock/effect-label/color-placement derivation to `utils/clipContextMenu.ts` instead of keeping that logic inline with DOM, store, thumbnail, proxy, explorer, and transcription side effects. `TrackContextMenu.tsx` now delegates add/duplicate/delete command labels, disabled state, delete impact text, and color swatch actions to `utils/trackContextMenu.ts`. `TimelineEmptyContextMenu.tsx` now delegates gap/view command construction to `utils/timelineEmptyContextMenu.ts`. These models compute their command state from plain inputs and injected callbacks. New DOM-free tests cover the clip, track, and empty menu models without React rendering, DOM nodes, `File`, media elements, or store state, while existing React integration tests remain as parity guards. Targeted checks passed: `npm run test -- tests/unit/clipContextMenu.test.ts tests/unit/trackContextMenu.test.ts tests/unit/timelineEmptyContextMenu.test.ts tests/unit/TimelineContextMenu.test.tsx tests/unit/TrackContextMenu.test.tsx tests/unit/audioRegionContextMenu.test.ts`, touched-file ESLint, and `npx tsc -p tsconfig.app.json --noEmit --pretty false`. Follow-up menu work remains for extracting side-effecting proxy/thumbnail/explorer/transcription handlers.

**Latest restore-boundary checkpoint:** `serializationUtils.loadState()` now restores top-level video/audio clips, direct nested video clips, and sub-nested video clips as data-only sources instead of creating blob URLs, `HTMLVideoElement`s, `HTMLAudioElement`s, or WebCodecs players during timeline restore. The old unreachable top-level element-creating video/audio branches plus their local restore-time WebCodecs cache/warmup helpers have been deleted. When a browser `File` is present for a video/audio clip, restore records `mediaFileId`, persisted natural duration, and `absolutePath`/`filePath`, marks the clip as not loading, asks `restoreSourceThumbnails()` to load cached video thumbnails, and leaves runtime hydration to the normal lazy media/render adapters. Composition-audio clips now also restore data-only: load keeps the persisted waveform and an audio source descriptor with natural duration, but does not create an `HTMLAudioElement`, object URL, `mixdownBuffer`, or background `compositionAudioMixer.mixdownComposition()` job. `projectLoad.ts:reloadNestedCompositionClips()` also dropped its now-unreachable video/audio element branches after the data-only short-circuit; the old still-image blob branch from this checkpoint has since been superseded by the latest data-only nested image restore checkpoint. `serializationUtils.loadState()` now delegates direct and deeper nested tree construction to `nestedCompositionLoader.ts:loadNestedClips(..., { restoreHooks })`, and direct nested audio is data-only with explicit no-object-URL/no-audio-element/no-mixdown coverage. `restoredMediaSource.ts` now owns the shared data-only video/audio descriptor helper used by both `serializationUtils` and the `projectLoad` post-relink nested video/audio branch. `tests/unit/projectMediaPersistence.test.ts` now covers the post-relink branch through `loadProjectToStores`, proving nested video/audio rebuild as data-only and no restore-time video/audio elements while allowing the media relink itself to create blob URLs. `nestedRestore.ts` is the first shared nested-restore module slice: it centralizes nested ids, motion clip restore, persisted video/audio state, node graph cloning, and nested media clip shell fields for both `serializationUtils.loadState()` and `projectLoad.ts:reloadNestedCompositionClips()`. At that checkpoint, non-video nested image/model/vector runtime assets and playback/export mixdown-on-demand were still on separate paths; nested image and playback/export composition-audio mixdown have since migrated, while model/vector resource policy remains separate follow-up work. Targeted checks passed for the latest nested-restore consolidation: `npm run test -- tests/unit/serializationNestedRestore.test.ts tests/unit/projectMediaPersistence.test.ts`, touched-file ESLint, and `npx tsc -p tsconfig.app.json --noEmit --pretty false`.

**Latest export/preview parity checkpoint:** The AI bridge now exposes `runTimelineCanvasExportPreviewParitySmoke`, backed by `frameFingerprint.ts`. The smoke captures a compact reference fingerprint from the live preview, subscribes to `exportPreviewFrame` during `debugExport`, fingerprints export preview `ImageBitmap`s synchronously before `endExport()` can close them, compares the closest export sample against the reference with concrete RGB/luma/nonblank thresholds, asserts the export blob is non-empty, and verifies export runtime resources are `0` before and after. It restores the active timeline by default. The runner now has `--skip-export-preview-parity`, runs this smoke on the fixed live comp before synthetic/torture mutations, and uses stable live sample candidates `[30, 42, 10, 2, 0.35]` because early frames on the fixed comp can be blank or stale while later real-video frames are stable. Focused live proof passed on `Random 3 Videos - 50 Cuts Test 164025` (`1780584025151-mx0znx977`): selected sample `30s`, reference nonblank `1`, FAST blob `48818` bytes, 6 export preview samples, `avgRgbDelta=15.1914`, `meanLumaDelta=5.0315`, comparison passed, export resources before/after `0`, and restore returned to 100 clips. Runner proof `fixtures/timeline-canvas-reports/run-20260604-162856Z/report.json` passed with reload first stats `1518ms`, ready `2903ms`, live 50 video + 50 audio clips, `domClipBodyCount=0`, no verification failures, and export-preview parity passed. Focused checks passed: `npm run test -- tests/unit/aiToolFrameFingerprint.test.ts tests/unit/aiToolPolicy.test.ts`, touched-file ESLint including `scripts/run-timeline-canvas-verification.mjs`, `npx tsc -p tsconfig.app.json --noEmit --pretty false`, direct live parity smoke, and focused runner. Current LOC status from `npm run swarm:status`: `165` tracked changed files, `48` new untracked files, tracked `+7385/-12534` net `-5149`, plus `+13043` LOC in new files.

**Earlier verification-runner checkpoint:** `npm run timeline:canvas:verify -- --base-url http://127.0.0.1:5173` passes against the live dev app with direct bridge smokes in the same run. The earlier compact report is `fixtures/timeline-canvas-reports/run-20260604-134604Z/report.json` at 289 lines / 6.6 KB. The synthetic large-project smoke creates 720 clips across 8 video tracks, verifies select-all selects all 720 clips, reports `domClipBodyCount=0` and `legacyClipBodyCount=0`, observes real draw culling with partial cull steps, and samples 60 fps with 0 estimated dropped frames. The thumbnail-before-playback reload smoke generates cached thumbnails for a synthetic video source, evicts memory, reloads canvas rows from cache before playback, and passes with `thumbnailClipCount=20`, `thumbnailDrawCount=86`, and no legacy DOM clip bodies. Direct `simulateScrub`, `simulatePlayback`, and `simulatePlaybackPath(play_scrub_stress_v1)` pass; the path stress still reports playback diagnostics as `bad` because of two preview-freeze events during the stress path, so this remains a follow-up signal even though the tool call succeeds. The synthetic marquee selection, spectral playback, and RAM preview smokes pass; RAM preview still uses the direct `RamPreviewEngine` fallback because the timeline UI keeps `RAM_PREVIEW_FEATURE_ENABLED=false` and cancels store-driven RAM preview. Local reports are written under ignored `fixtures/timeline-canvas-reports/`. Torture-media video/nested/export smokes are still skipped until `fixtures/torture-media/manifest.local.json` exists.

**Latest live-playhead checkpoint:** The user-reported playback playhead flicker was reproduced with a real library media clip, not synthetic solids. `runTimelineCanvasPlayheadSmoothnessSmoke` now supports `useExistingMediaFile: true`; using `Anduril Unveils Roadrunner  Roadrunner-M.mp4` first failed with `backtrackCount=39` over 83 sampled frames while preview playback stayed fine. Root cause was React committing stale inline `left` values while the rAF loop also advanced the DOM playhead. `Timeline.tsx` now keeps playback metrics in a ref, removes React `left` writes during playback, and lets only the rAF loop mutate the playhead position until playback stops or dragging starts. The same real-media smoke now passes with 84 sampled frames, `backtrackCount=0`, `maxBacktrackPx=0`, `forwardDistancePx=98.64`, average frame delta 16.67 ms, and `domClipBodyCount=0`.

**Latest tool-pointer parity checkpoint:** The user-reported missing cut behavior on canvas-rendered clips was traced to `TimelineTrack`: the canvas row hit-tested the clip but always forwarded the left click to the drag/select path, so the already-working Blade dispatcher and split operation never ran. Canvas row mousemove now dispatches `dispatchTimelineClipPointerMove()` and updates `timelineToolPreview`, restoring the Blade cut indicator. Canvas row left-click now dispatches `dispatchTimelineClipPointerClick()` before drag, applies the typed split operation through `applyTimelineEditOperation(..., { source: 'ui' })`, and preserves normal select/drag clicks for the select tool. Focused checks passed: touched-file ESLint, `npm run test -- tests/unit/TimelineTrack.test.tsx tests/unit/timelineToolPointerDispatcher.test.ts`, and `npx tsc -p tsconfig.app.json --noEmit --pretty false`.

**Latest Blade browser-smoke checkpoint:** normal `blade` previews now render a track-local red blade line through `TimelineToolOverlayLayer`; `blade-all-tracks` keeps the existing section-height line. The verification bridge now exposes `runTimelineCanvasBladeToolSmoke`, which switches to `blade`, dispatches real row `mousemove`/`mousedown` events, asserts the hover preview targets the hit clip/time, asserts the click splits the clip, and can restore the previous timeline/tool state afterwards. The verification runner includes the synthetic one-clip version in the canvas batch. The user-facing browser proof was then rerun against the real Media Library file `Anduril Unveils Roadrunner  Roadrunner-M.mp4` with `useExistingMediaFile:true`, targeted at the fresh visible bridge tab using explicit JSON args: `synthetic=null`, `mediaSetupName=Anduril Unveils Roadrunner  Roadrunner-M.mp4`, `rowFound=true`, `previewToolId=blade`, preview time about `2.999s` for a `3s` split, real linked video/audio clip count `2 -> 4`, and `restore.enabled=false`, leaving the visible timeline on real Anduril split clips instead of Smoke clips. Focused checks passed: touched-file ESLint, `npm run test -- tests/unit/timelineCanvasSmokeHandlers.test.ts tests/unit/timelineToolOverlayLayer.test.ts tests/unit/aiToolPolicy.test.ts`, and `npx tsc -p tsconfig.app.json --noEmit --pretty false`.

**Historical live test composition checkpoint:** Earlier browser/bridge timeline checks used the user-visible real-media composition `Random 3 Videos - 50 Cuts Test 164025` (`1780584025151-mx0znx977`). It was created from the three Media Library videos and contains 50 contiguous random video segments plus 50 linked audio clips: 9 Anduril, 23 Bolt, and 18 FPV video segments, with audio distributed across two audio tracks. It is historical evidence only now; the current safe live target is `Subcomposition 1` (`1780705391680-ecu7panot`) unless a target is explicitly rebuilt/restored. Initial playback smoke on this comp confirmed canvas rendering with 50 thumbnail draws, 50 waveform clips, and `domClipBodyCount=0`, but also exposed a real performance signal: the 10s playback simulation advanced only about 6.3s, with long stall samples and preview-freeze diagnostics marked `bad`.

**Latest live test-comp diagnostics checkpoint:** The first follow-up against `Random 3 Videos - 50 Cuts Test 164025` fixed two diagnostics/smoke contamination issues. `simulatePlayback` now leaves playback paused by default even when playback was already running before the smoke, restores the old speed, and only resumes the previous play state when callers pass `restorePlaybackState:true`; the public tool schema documents this flag. The handler now uses the shared finite-duration parser for `durationMs`/`settleMs`, so stringified bridge numbers are accepted. `timelineCanvasDiagnostics` now treats reported tracks that still exist in the canonical store as active even when their canvas has not redrawn for more than 30s; non-store reports remain separated as orphaned diagnostics. `TimelineClipCanvas` unregisters draw diagnostics on canvas unmount/track change, and `TimelineTrack` unregisters the whole track diagnostics on track unmount/track change, so composition switches do not leave old renderer entries in the module-level diagnostics map. Live bridge proof on the 50-cut comp: `simulatePlayback` with explicit JSON `durationMs:1500` returned `requestedDurationMs=1500`, `actualDurationMs=1626`, `deltaSeconds=1.463`, `diagnosticsStatus=ok`, `previewFreezeEvents=0`, `endedPlaying=false`, and post-run stats stayed `isPlaying=false`. Canvas totals before and after the run stayed at `trackCount=3`, `inputClipCount=100`, `thumbnailDrawCount=50`, `waveformClipCount=50`, `domClipBodyCount=0`, `staleTrackCount=0`, `orphanedTrackCount=0`, `storeTrackCount=3`, and `storeInputClipCount=100`. Focused checks passed: `npm run test -- tests/unit/aiToolPlaybackHandlers.test.ts tests/unit/timelineCanvasDiagnostics.test.ts tests/unit/TimelineTrack.test.tsx`, touched-file ESLint, and `npx tsc -p tsconfig.app.json --noEmit --pretty false`.

**Latest real-comp verification-runner checkpoint:** `scripts/run-timeline-canvas-verification.mjs` now has a dedicated real-media live composition gate in addition to the synthetic and torture-media smokes. The runner accepts `--live-composition-id`, `--live-playback-ms`, `--live-min-video-clips`, `--live-min-audio-clips`, `--skip-live-composition`, `--require-live-composition`, and `--no-restore-live-composition`; by default it uses `TIMELINE_CANVAS_LIVE_COMP_ID` or the current local 50-cut comp id `1780584025151-mx0znx977`. The live block opens the comp, asserts minimum video/audio clip counts, samples pre/post `getStats`, runs direct `simulateScrub`, `simulatePlayback`, and `simulatePlaybackPath` unless playback is skipped, verifies playback ends paused, checks `domClipBodyCount`, stale, and orphaned track counts, and reopens the live comp before final stats so the browser does not stay on a synthetic smoke timeline. Compact reports now include `liveComposition`, `playbackContexts.live/torture/synthetic`, `summary.liveCompositionSuccess`, and global scrub/playback/path success now counts the live context. Final `getStats`, `getPlaybackTrace`, and `getRuntimeDiagnostics` are hard `assertToolSuccess` gates. Live-only runner proof passed with `node scripts/run-timeline-canvas-verification.mjs --base-url http://127.0.0.1:5173 --skip-large --skip-torture --skip-export --skip-ram --skip-spectral --skip-marquee --skip-playhead --skip-thumbnail-reload --require-live-composition`; latest report `fixtures/timeline-canvas-reports/run-20260604-150046Z/report.json` shows `liveCompositionSuccess=true`, `scrubSuccess=true`, `playbackSuccess=true`, `playbackPathSuccess=true`, 50 video clips, 50 audio clips, `simulatePlayback` diagnostics `playbackStatus=ok` with `previewFreezeEvents=0`, final `inputClipCount=100`, `domClipBodyCount=0`, `staleTrackCount=0`, and `orphanedTrackCount=0`. The live playback path tool succeeds but still reports `playbackStatus=bad`, so longer real-media playback diagnostics remain a follow-up signal rather than hidden runner noise. Focused checks passed: runner `--help`, `npx eslint scripts/run-timeline-canvas-verification.mjs`, and live-only verification runs.

**Latest real-comp playback-path checkpoint:** `simulatePlaybackPath(play_scrub_stress_v1)` is now media-range aware instead of timeline-duration only. The 50-cut comp has a 180s composition duration but the last playable video clip ends at about 159.57s, so the old hard-coded `clipStart + 180s` stress scrub could land in empty timeline and create artificial end-of-media stalls. The path preset now uses the playable video end, clamps each scrub target with enough tail room for the following play step, reports `unclampedTargetTime`/`targetWasClamped`, and skips play steps that have no playable media left. It also marks its output as `diagnosticMode: "playback_scrub_stress"` so the runner treats it as a crash/scrub-stability stress path, not as the linear playback health verdict. DOM playhead scrubs now fall back to store-driven scrub if browser geometry produces non-finite mouse coordinates, and playback diagnostics now count `proxy-image-frame`, `proxy-image-frame-nearest`, and `not-ready-scrub-cache` as responsive preview fallback paths only when freeze/stale thresholds stay healthy. Current live-only proof: `fixtures/timeline-canvas-reports/run-20260604-151550Z/report.json` passed with `liveCompositionSuccess=true`; normal `simulatePlayback` on `Random 3 Videos - 50 Cuts Test 164025` reported `playbackStatus=ok`, `previewFreezeEvents=0`, `healthAnomalies=0`, `coldVideos=0`, `warmingUpVideos=0`, `worstReadyState=4`, `deltaSeconds=1.540`, `stalledFrames=1`, and `endedPlaying=false`. The stress path succeeded, ended paused, and showed the 3m target clamped from `180` to `157.0667` against `playableEndTime=159.5667`; its `playbackStatus=bad` remains a stress-path signal, not a live playback failure. Canvas totals stayed `inputClipCount=100`, `thumbnailClipCount=50`, `waveformClipCount=50`, `domClipBodyCount=0`, `staleTrackCount=0`, and `orphanedTrackCount=0`. Focused checks passed: `npm run test -- tests/unit/aiToolPlaybackHandlers.test.ts tests/unit/playbackDebugStats.test.ts`, touched-file ESLint, `npx tsc -p tsconfig.app.json --noEmit --pretty false`, runner `--help`, and live-only verification.

**Latest reload instrumentation checkpoint:** `getStats` now includes a normalized `projectLoadProgress` snapshot, and `scripts/run-timeline-canvas-verification.mjs` polls `getStats` after `reloadApp` instead of sleeping blindly for 2.5s. Compact reports now expose `reload.firstStatsMs`, `reload.readyMs`, per-sample project-load phase/percent, FPS, and canvas totals. On the 50-cut comp the latest live-only runner proof `fixtures/timeline-canvas-reports/run-20260604-152235Z/report.json` measured first browser bridge response at `1526ms`, `projectLoadProgress.phase="ui"`/`58%` at first response, all 100 timeline clips present by `1879ms` during cached thumbnail restoration, `ready` at `2235ms`, FPS back to 58 by `2754ms`, and idle/overlay cleared at `3270ms`. This run did not reproduce the earlier 8s/40% hang; it gives us a concrete baseline and will make future regressions visible. Focused checks passed: `npm run test -- tests/unit/aiToolStats.test.ts`, touched-file ESLint, runner `--help`, live-only verification, and `npx tsc -p tsconfig.app.json --noEmit --pretty false`.

**Latest smoke cleanup checkpoint:** Real-media timeline canvas smokes now have an explicit timeline restore contract. `runTimelineCanvasPlayheadSmoothnessSmoke` captures the current tracks, clips, layers, selection, keyframes, markers, duration, playhead, zoom/scroll, RAM-preview flags, drag/tool previews, and playback state before it builds a temporary media timeline, then restores those fields after collecting the smoke snapshot. `useExistingMediaFile: true` restores by default unless `restoreTimelineAfterRun:false` is passed; synthetic runner smokes still keep their existing behavior unless restore is requested explicitly. The handler also no longer falls back to synthetic clip mutation when an existing-media smoke cannot find a video `MediaFile`. Focused checks passed: touched-file ESLint, `npm run test -- tests/unit/timelineCanvasSmokeHandlers.test.ts`, and `npx tsc -p tsconfig.app.json --noEmit --pretty false`.

**Latest implementation checkpoint:** six Phase 0 contract packets landed through the swarm. `ClipInteractionShell` now mounts inside the existing canvas DOM overlay for hovered/dragged/trimmed canvas clips plus track-local active `clipFade`, open clip context-menu, selected keyframe, audio-region, spectral-region, video-bake, and stem-job states with `pointer-events: none`, while selected-only clips still stay canvas-rendered. Active trim handles now render through `ClipTrimHandles` and dispatch back into the existing trim command path; active fade shells now render `ClipFadeHandles`, share fade-curve geometry with `TimelineClipCanvas`, use duration-positioned fade handles, and dispatch into the existing fade command path. Selected keyframe tick shells now render `ClipKeyframeTicks` through the shell and dispatch grouped keyframe moves through `TimelineTrack`. Audio-region shells now render selection/gain visuals, operation overlays, edit-stack controls, and context-menu commands through `ClipAudioRegionControls`, and own move, resize, gain, fade-in, fade-out, preview, and commit interactions through store actions. Spectral-region shells now render selection and image-layer overlays, Mask/Resyn/Img toolbar commands, Ctrl/Meta drag selection, selection clear, and image-layer drop through `ClipSpectralRegionControls`; passive spectral display now renders real tile-backed spectrogram rasters in `TimelineClipCanvas`, and the DOM `ClipSpectrogram` component has been retired. Video-bake shells now render clip bake-region overlays and Bake/Unbake/Remove controls through `ClipVideoBakeControls`, reusing the existing region overlay model and store actions. Stem-only shells now render active job progress and completed-stem switcher controls through `ClipStemControls`, including source/stem prewarm and `setClipSourceToStem`. Canvas-mode DOM overlays now render only `ClipInteractionShell`; they no longer mount a legacy `TimelineClip` body in any active shell state, and diagnostics report `domClipBodyCount: 0` for canvas-mode tracks. `getStats` now exposes `timelineCanvas` diagnostics with canonical store track/clip totals, active renderer totals, last reported renderer totals, stale track counts, missing track IDs, canvas draw counts, thumbnail/waveform counts, DOM overlay counts, shell counts, and active shell slot counts. Phase 2 has moved visible thumbnail DB warmup into `timelineThumbnailDbWarmup`, missing visible thumbnail generation into `timelineThumbnailGenerationWarmup`, waveform artifact reads into `timelineWaveformArtifactWarmup`, spectrogram artifact reads into `timelineSpectrogramArtifactWarmup`, source waveform generation demand into `timelineSourceWaveformWarmup`, and bounded spectrogram rasterization into `utils/spectrogramCanvas`, all with in-flight coalescing and component-side visible-demand collection. Thumbnail and waveform generation warmups block while playback or drag preview is active. Missing visible thumbnail generation now resolves from `MediaFile.url` before falling back to hydrated preview video URLs, runs through `thumbnailCacheService.generateForSourceUrl()`, and uses a small global generation cap. Warmup schedulers now use bound browser timers so visible thumbnail generation cannot crash with `Illegal invocation` on load. Thumbnail cache notifications now include frame/second-index events; `TimelineClipCanvas` and the DOM thumbnail hook filter redraws to visible source-second ranges. Decoded thumbnail `ImageBitmap`s now live in a service-level `thumbnailBitmapCache` with source/URL close APIs; source thumbnail evictions close decoded bitmaps before revoking blob URLs. `TimelineClip.tsx` decomposition now includes extracted presentation, stem-display, audio-region display, coverage badges, media-label color resolution, waveform render geometry, audio-region context-menu model, active audio/video region overlay geometry, source-extension ghost geometry, and spectral selection/image-layer overlay geometry. The dead `passiveVisualsSuppressed` renderer option and its test were retired after the canvas overlay stopped mounting legacy clip bodies.

**Latest retirement checkpoint:** `TimelineClip.tsx` has been deleted. `TimelineTrack` no longer accepts or calls a full clip renderer prop; the track path always renders `TimelineClipCanvas` plus `ClipInteractionShell` and the old `timelineCanvasClips` rollback flag was removed. New-track clip-drag previews now render through a lightweight `.timeline-clip-preview` element instead of a full `TimelineClip`. Composition-switch clip overlays now render `TimelineClipCanvas` rows with switch-specific canvas animation wrappers, so `Timeline.tsx` no longer imports or calls the deleted `TimelineClip` component. The former `TimelineClip` hook/component passive DOM stack was removed, including old thumbnail filmstrip, waveform, transcript/analysis overlay, passive badge, media-label, source-extension ghost, and selected-region hitarea modules plus their app-orphaned tests. `TimelineClip.css` is now a shell/overlay stylesheet for stem controls, parent links, lightweight previews, trim/fade handles, audio/spectral/video-bake controls, and keyframe ticks; old `.timeline-clip` body, thumbnail, waveform, proxy/download, badge, and transcript/analysis overlay CSS was removed. The canvas/shell path restored the regressions found during retirement: hover shells now start trim/fade gestures, hovered clips expose keyframe tick groups, clip double-click routes back to the existing MIDI/composition/media reveal handler, and visible audio clips schedule processed waveform or spectrogram artifact generation through timeline services. The now-unused passive-visual suppression option was removed from the former `TimelineTrackProps.renderClip`, `Timeline.renderClipForSection()`, and `TimelineClipProps`. The internal always-true `passiveMediaEnabled` and `passiveDecorationsEnabled` compatibility gates were then removed from the former `TimelineClip` path, thumbnail planning, audio artifact warmups, visual chrome, and passive visual layers; code refs in `src` and `tests` are gone. `serializationUtils.restoreSourceThumbnails()` no longer calls `thumbnailCacheService.generateForSource()` during timeline load; it only loads already-cached source thumbnails, leaving missing thumbnail generation to the visible scheduler. Silent project-load auto-relink now passes `{ generateThumbnails: false }` through both `applyRelinkMatch()` and the stored-handle `updateTimelineClips()` fallback, while manual reload/relink keeps direct generation. Unused IndexedDB project actions (`saveProject`, `loadProject`, `getProjectList`, `deleteProject`) were removed from `mediaStore/slices/projectSlice.ts`; current project loading/saving stays on `ProjectFileService`. The remaining IndexedDB startup restore body was extracted to `mediaStore/legacyStartupRestore.ts`, leaving `projectSlice.ts` as a thin compatibility delegator plus project UI/runtime actions.

**Latest post-retirement cleanup checkpoint:** stale DOM-body target assumptions have been removed from the active timeline paths. `TimelineTrack`, `useMidiClipDraw`, and `useMarqueeSelection` now use `timelineActiveTargets` so shell controls and lightweight previews are treated as active targets without depending on the deleted `.timeline-clip` body. `usePickWhipDrag` no longer contains the unreachable legacy DOM query path. Guided DOM target resolution now understands `.clip-interaction-shell` plus shell trim/fade handle attributes, so guided actions can address active canvas shells without reviving full clip bodies. Orphan `.timeline-clip` body styles were removed from timeline interaction/keyframe/track CSS; remaining `.timeline-clip-preview` styles are only for lightweight drag previews.

**Latest bridge-target cleanup checkpoint:** AI bridge drag/perf instrumentation no longer depends on deleted `.timeline-clip[data-clip-id]` bodies. `measureClipDragInteraction` first targets active `.clip-interaction-shell` or `.timeline-clip-preview` elements, then falls back to store clip geometry plus the owning `.track-clip-row` so dispatched events enter the current canvas hit-test path. The real clip-drag recorder now recognizes shell/preview targets, and preview diagnostics collect `.timeline-clip-canvas`, shell, and lightweight preview elements instead of retired `.clip-waveform` / `.waveform-canvas` selectors. Focused checks passed: touched-file ESLint and `npx tsc -p tsconfig.app.json --noEmit --pretty false`.

**Latest spectrogram checkpoint:** passive spectral rendering is now fully canvas-backed. `TimelineClipCanvas` loads visible spectrogram artifacts through `timelineSpectrogramArtifactWarmup`, retries transient missing artifacts instead of storing permanent misses, maps processed spectrogram source ranges into the processed tile duration, and treats audio-track clips plus waveform/spectrogram refs as audio-like even when source metadata is incomplete. `utils/spectrogramCanvas` draws the full CSS destination span while capping raster pixel work and bounding the raster cache by entry count and byte budget.

**Latest cache test checkpoint:** `timelineSpectrogramArtifactWarmup` now has unit coverage matching the waveform artifact warmup path: unique preferred-ref collection, cached fast path, in-flight load coalescing by spectrogram ref id, and missing-result publication without duplicate loads. This replaces part of the deleted DOM `ClipSpectrogram` test coverage at the new service boundary.

**Latest cache invalidation checkpoint:** Phase 2 media cache invalidation now has executable memory cleanup instead of only a plain-data contract. Timeline audio memory caches expose ref-id eviction APIs for waveform pyramids, spectrogram tile sets, loudness envelopes, beat/onset maps, and frequency/phase summaries. `timelineCacheInvalidation` executes `createMediaCacheInvalidationPlan()` actions against thumbnail generation aborts, source thumbnail clears/evicts, decoded thumbnail bitmap closes, audio cache ref eviction, and `ClipAudioAnalysisJobService.cancelClip()`. `deleteMediaFilesEverywhere()` now collects matched active and saved-composition clips before deletion, includes clip-owned source/processed audio refs in cache invalidation, cancels clip analysis jobs, and uses the same source-replacement invalidation boundary for `reloadFile()`, `reloadAllFiles()`, and direct `updateTimelineClips()` relink/update callers. The AI `deleteMediaItem` handler now routes media files through deep deletion instead of the lightweight `removeFile()` path. Targeted checks passed: `npx tsc -p tsconfig.app.json --noEmit --pretty false`, touched-file ESLint, and `npm run test -- tests/unit/timelineCacheInvalidation.test.ts tests/unit/timelineCacheSchedulerContracts.test.ts tests/unit/aiToolMediaHandlers.test.ts`.

**Latest relink/source-identity checkpoint:** Phase 2 now recomputes or clears source identity during media source replacement. `reloadFile()`, `reloadAllFiles()`, file-backed relink, model/gaussian sequence relink, and stored-handle project-load fallback use a shared source-replacement patch that calculates a fresh `fileHash` for browser `File` replacements and clears stale thumbnail, waveform, proxy, audio-proxy, and media audio-analysis fields. Native metadata-only relinks explicitly clear the old hash instead of preserving a stale source identity. Relink now invalidates old source caches before replacing the media item, then reloads matching timeline clips without a duplicate invalidation pass; reloaded clips clear old `sourceAnalysisRefs` and `processedAnalysisRefs` while preserving user audio state. `updateTimelineClips()` accepts the replacement `fileHash` and passes it to thumbnail generation, and gaussian-splat reloads no longer preserve a stale `gaussianSplatFileHash` from the old clip source. `thumbnailCacheService` now versions per-source DB loads so an old `loadCachedForSource(mediaId, oldHash)` promise cannot repopulate memory after `clearSource(mediaId)`. Targeted checks passed: touched-file ESLint, `npm run test -- tests/stores/mediaStore/fileManageSlice.test.ts tests/unit/thumbnailCacheService.test.ts tests/unit/timelineCacheInvalidation.test.ts tests/unit/timelineThumbnailDbWarmup.test.ts tests/unit/timelineThumbnailGenerationWarmup.test.ts`, and `npx tsc -p tsconfig.app.json --noEmit --pretty false`.

**Latest non-waveform audio artifact checkpoint:** Phase 2 now has visible read warmups for the remaining non-waveform analysis artifacts. `timelineAudioAnalysisArtifactWarmup` collects effective processed-over-source refs for loudness envelopes, beat grids, onset maps, frequency summaries, and phase correlation artifacts, dedupes and sorts by kind/ref id, checks memory caches first, and coalesces persistent loads with the existing scheduler lanes. `timelineFrequencyPhaseCache` now exposes individual `loadTimelineFrequencySummary()` and `loadTimelinePhaseCorrelation()` APIs to match the existing loudness and beat/onset loaders. `TimelineClipCanvas` triggers this warmup from the geometry-aware visible audio artifact clip set, so drag/trim/viewport bounds match the active artifact generation path, and the warmup primes service caches without creating media elements or adding component-local drawing state. Targeted checks passed: touched-file ESLint, `npm run test -- tests/unit/timelineAudioAnalysisArtifactWarmup.test.ts tests/unit/timelineWaveformArtifactWarmup.test.ts tests/unit/timelineSpectrogramArtifactWarmup.test.ts tests/unit/timelineAudioArtifactGenerationWarmup.test.ts tests/unit/timelineCacheSchedulerContracts.test.ts`, and `npx tsc -p tsconfig.app.json --noEmit --pretty false`.

**Latest history boundary checkpoint:** Phase 5 history restore now prefers `HistoryTimelineEditState` instead of replaying runtime-bearing `snapshot.timeline` clips/layers. `historyTimelineRestoreState` converts sidecar tracks, clips, layers, keyframes, markers, and master audio state back to timeline-store shape, reuses only compatible current runtime sources by clip/runtime ref, and restores deleted or non-resident media clips as data-only lazy-reload entries. New `StateSnapshot.timeline` payloads are now derived from the runtime-free sidecar with plain placeholder files, so newly captured history no longer keeps old `HTMLVideoElement`, `File`, decoder, or layer-source runtime payloads for the timeline restore path; old snapshots without sidecars retain the legacy fallback. `historyRuntimeRehydration` now syncs restored compatible live sources into the interactive runtime coordinator under scoped `history-rehydrate:*` owners, releases only its previous history-rehydrate resources, and leaves data-only lazy reload clips plus unrelated lazy/playback/slot resources untouched. `runtimeResourceReporting` also tolerates source-less mock media elements, so reporting cannot crash on legacy test/runtime shells. Regression coverage asserts runtime-free sidecar capture, data-only legacy timeline capture, undo reuse of current compatible runtime instead of old snapshot runtime, deleted-clip restore as lazy data, service-level history rehydrate resource replacement, and store-level undo reporting of rehydrated runtime resources. Targeted checks passed: touched-file ESLint, `npm run test -- tests/unit/historyRuntimeRehydration.test.ts tests/stores/historyStore.test.ts`, and `npx tsc -p tsconfig.app.json --noEmit --pretty false`.

**Latest runtime coordinator checkpoint:** Phase 5 primary lazy media now reports into a live `timelineRuntimeCoordinator` resource ledger without changing allocation ownership. The coordinator contract now supports `retainResource()`, `releaseResource()`, and scoped `clearResources()`, computes per-policy resource usage/budget pressure from retained plain `RenderResourceDescriptor`s, and exposes bridge-safe stats through `getBridgeStats()`. `lazyMediaElements` registers interactive `html-media` descriptors when it creates primary lazy video/audio elements, refreshes diagnostics while clips stay desired, releases exact resource ids during detach/prune/releaseAll, and detaches any existing same-key record before replacing it so drag-preview/store-frame overwrites do not leak old elements. `getStats`/`getStatsHistory` now include `timelineRuntimeCoordinator` beside `timelineCanvas`. Focused tests cover retain/release budget accounting, lazy video/audio registration, idle prune cleanup with owned object URL revocation, and AI stats/history plain-data exposure. Targeted checks passed: touched-file ESLint, `npm run test -- tests/unit/lazyMediaElements.test.ts tests/unit/timelineRuntimeCoordinatorContracts.test.ts tests/unit/aiToolStats.test.ts tests/stores/historyStore.test.ts tests/unit/historyTimelineEditStateContracts.test.ts`, `npx tsc -p tsconfig.app.json --noEmit --pretty false`, and `git diff --check` with only existing CRLF warnings.

**Latest lazy video/audio object URL ownership checkpoint:** Phase 5 file-backed lazy video/audio element URLs are now manager-owned instead of raw `URL.createObjectURL()` allocations hidden inside `lazyMediaElements`. `mediaObjectUrlManager` exposes a per-clip/per-kind lazy media element key, `getLazySource()` creates or reuses that managed URL when a media id is available, and the actual `video.src` / `audio.src` assigned to the element is the managed URL. Detach/prune/releaseAll now revoke that exact managed URL through the manager, with a fallback direct revoke only for clips that truly have no media id. The lazy key stays separate from durable primary media URLs so releasing one lazy element does not revoke shared `MediaFile.url` ownership. Latest LOC status from `npm run swarm:status`: `208` tracked changed files, `70` new untracked files, tracked `+16392/-15951` net `+441`, plus `+25029` LOC in new files. Targeted checks passed: `npm run test -- tests/unit/lazyMediaElements.test.ts tests/unit/mediaObjectUrlManager.test.ts` (13 tests), touched-file ESLint, and `npx tsc -p tsconfig.app.json --noEmit --pretty false`.

**Latest background/slot-deck runtime checkpoint:** Phase 5 now also reports cold background-layer and warm slot-deck resources into the same coordinator ledger without changing existing ownership. `runtimeResourceReporting` centralizes descriptor creation for clip-owned runtime bindings, HTML video/audio elements, image elements, and text canvases. `slotDeckManager` reports prepared deck clips under the `slot-deck` policy even though their internal decode sessions still use the existing `background` session policy, and releases by exact slot/clip owner during deck dispose, eviction, replacement, and disposeAll. `layerPlaybackManager` reports only cold-hydrated layer-owned clips under the `background` policy and releases by exact background layer/clip owner on deactivate; slot-deck-adopted layers only pause and release the deck pin, preserving slot-deck ownership. Focused tests cover slot-deck report/release, cold background report/release, runtime-binding/html-media budget counts, and the existing warm-deck adoption boundary. Targeted checks passed: touched-file ESLint, `npm run test -- tests/unit/slotDeckManager.test.ts tests/unit/layerPlaybackManagerWarmDeck.test.ts tests/unit/timelineRuntimeCoordinatorContracts.test.ts tests/unit/aiToolStats.test.ts`, and `npx tsc -p tsconfig.app.json --noEmit --pretty false`.

**Latest thumbnail runtime checkpoint:** Phase 5 thumbnail runtime reporting now covers DB load jobs, generation jobs, detached generation videos, generation canvases, bitmap decode jobs, and retained decoded `ImageBitmap` resources under the `thumbnail` policy. `RenderResourceKind` now includes `job`, so coordinator budgets can count `usage.jobs` instead of hiding scheduler work behind unrelated resource kinds. `thumbnailCacheService` reports exact DB/generation job ids, releases them on promise completion, source clear, and global clear, and now aborts/release-tracks active generation resources when `clearAll()` runs. `thumbnailBitmapCache` reports transient decode jobs, replaces them with retained `image-canvas/image-bitmap` descriptors when decode finishes, and releases exact bitmap resources on URL/source close, invalidation, LRU eviction, and full cache clear. Focused tests cover job accounting in the coordinator contract, pending DB-load job visibility, active generation job/video visibility, decode-job-to-bitmap-resource transition, and resource release on close. Targeted checks passed: touched-file ESLint, `npm run test -- tests/unit/timelineRuntimeCoordinatorContracts.test.ts tests/unit/thumbnailBitmapCache.test.ts tests/unit/thumbnailCacheService.test.ts tests/unit/timelineThumbnailGenerationWarmup.test.ts tests/unit/timelineThumbnailDbWarmup.test.ts tests/unit/aiToolStats.test.ts`, and `npx tsc -p tsconfig.app.json --noEmit --pretty false`.

**Latest render-target runtime checkpoint:** Phase 5 render-target reporting now lives at the central `renderTargetStore` boundary instead of individual preview components. `renderTargetRuntimeReporting` emits `render-target` policy `image-canvas/html-canvas` descriptors for canvas/context-backed targets with destination/source tags, composition routing, canvas dimensions, and estimated GPU bytes. The store reports on target registration with an existing canvas, canvas binding, source/name/enabled/window/transparency updates, and releases exact owner-scoped resources on canvas clear, deactivate, and unregister. Disabled targets keep their retained canvas descriptor with a warning diagnostic instead of being released, matching the existing runtime state. Focused tests cover register-without-canvas, canvas bind, source retarget, disabled retention, canvas clear, rebinding, and unregister release. Targeted checks passed: touched-file ESLint, `npm run test -- tests/unit/renderTargetStoreRuntimeReporting.test.ts tests/unit/timelineRuntimeCoordinatorContracts.test.ts tests/unit/aiToolStats.test.ts`, and `npx tsc -p tsconfig.app.json --noEmit --pretty false`.

**Latest composition runtime checkpoint:** Phase 5 composition-render reporting now covers non-active composition sources prepared by `compositionRenderer`. `compositionRenderRuntimeReporting` emits owner-scoped `runtime-binding`, `html-media/video`, `image-canvas/html-image`, and `image-canvas/html-canvas` descriptors under the `composition-render` policy, clearing stale descriptors before replacing an owner. `compositionRenderer` now marks stale `CompositionSources`, blocks in-flight video/image/vector loads from reporting after invalidation/dispose, revokes owned blob URLs on load failure or invalidation, releases media runtime sessions, and destroys owned vector-animation runtimes without touching active-composition pass-through media. Focused tests cover video source reporting/release, stale video load cleanup after invalidation, and text canvas reporting without runtime bindings. Targeted checks passed: touched-file ESLint, `npm run test -- tests/unit/compositionRendererRuntimeReporting.test.ts tests/unit/timelineRuntimeCoordinatorContracts.test.ts tests/unit/aiToolStats.test.ts`, and `npx tsc -p tsconfig.app.json --noEmit --pretty false`.

**Latest GPU texture descriptor checkpoint:** Phase 5 now has a first-class `gpu-texture` `RenderResourceKind` for RAM preview, export, render-target manager internals, readback, and intermediate WebGPU frame resources. The descriptor records `textureId`, `textureKind`, dimensions, format/sample metadata, and GPU byte estimates while staying bridge-plain. Runtime policy contracts allow `gpu-texture` across non-interactive policies, budget accounting increments `usage.gpuTextures`, and the contract test covers validation plus GPU texture/GPU byte budget pressure. Targeted checks passed: touched-file ESLint, `npm run test -- tests/unit/timelineRuntimeCoordinatorContracts.test.ts tests/unit/aiToolStats.test.ts`, and `npx tsc -p tsconfig.app.json --noEmit --pretty false`.

**Latest RAM preview runtime checkpoint:** Phase 5 RAM preview reporting now exposes run jobs, run-scoped clip/source resources, CPU composite cache pressure, and GPU cached-frame pressure under the `ram-preview` policy. `ramPreviewRuntimeReporting` emits `ram-preview-render` jobs, runtime bindings, frame providers, HTML video resources, image/canvas resources, aggregate CPU composite-cache heap usage, and per-frame `gpu-texture` resources for GPU cached playback frames. `RamPreviewEngine` now threads a run id through frame generation, reports video/image/nested-video resources once per run, and releases RAM-preview policy runtime sessions when generation finishes. `ScrubbingCache` reports CPU composite cache updates, reports GPU cached frames with width/height/format/gpu bytes, releases resources on clear/eviction/replacement, and destroys replaced same-key textures. `CacheManager.handleDeviceLost()` releases RAM-preview cache reporting before dropping the cache reference, preventing stale coordinator entries after WebGPU device loss. Focused tests cover helper reporting/release, engine run source reporting and session release, CPU/GPU cache reporting, GPU same-key replacement, GPU LRU eviction, and device-loss cleanup. Targeted checks passed: touched-file ESLint, `npm run test -- tests/unit/ramPreviewRuntimeReporting.test.ts tests/unit/ramPreviewEngineRuntimeReporting.test.ts tests/unit/scrubbingCache.test.ts tests/unit/cacheManagerRuntimeReporting.test.ts tests/unit/timelineRuntimeCoordinatorContracts.test.ts tests/unit/aiToolStats.test.ts`, and `npx tsc -p tsconfig.app.json --noEmit --pretty false`.

**Latest export runtime checkpoint:** Phase 5 export runtime reporting now covers the main `FrameExporter` path, FAST parallel decode detail, export audio buffers, and the active FFmpeg/GIF/image-sequence frame renderer without changing rendering behavior. `exportRuntimeReporting` emits export run jobs, zero-copy/readback output-surface resources, prepared clip runtime bindings, FAST WebCodecs frame providers, PRECISE HTML video resources, export preview `ImageBitmap` resources, ParallelDecode `native-decoder` plus decoded `VideoFrame` buffer pressure, and audio source/processed/mix/master buffers under the `export` policy. `FrameExporter` creates a run id per attempt, reports prepared clip state and parallel decode snapshots with throttling, passes the run id into `AudioExportPipeline`, and releases on success, failure, or cancel. `AudioExportPipeline` now clears extractor caches through `finally`, cancels the WebCodecs audio encoder, blocks late reporting after cancel, and reports audio buffers as `audio-source-clock` resources. `FFmpegFrameRenderer` has optional run reporting enabled for FFmpeg video/GIF, browser GIF, and image-sequence exports, shares its run id with FFmpeg audio, and releases coordinator resources on cleanup/cancel; one-shot still-frame export and FFmpeg WASM internals remain explicit non-goals. Focused tests cover job/output/preview, prepared clip-state resources, parallel decode snapshots, audio-buffer reporting and cancel cleanup, FFmpeg renderer lifecycle reporting, budget counts, and run release. Remaining export runtime work is browser `debugExport` parity smoke plus final full export/playback checks. Targeted checks passed: touched-file ESLint, `npm run test -- tests/unit/ffmpegFrameRendererRuntimeReporting.test.ts tests/unit/audioExportPipeline.test.ts tests/unit/exportRuntimeReporting.test.ts tests/unit/parallelDecodeRuntimeSnapshot.test.ts tests/unit/timelineRuntimeCoordinatorContracts.test.ts tests/unit/aiToolStats.test.ts`, and `npx tsc -p tsconfig.app.json --noEmit --pretty false`.

**Latest browser smoke checkpoint:** A separate Chrome test profile loaded `http://localhost:5173` with WebGPU available and 12 timeline canvas-related elements visible. AI-bridge stats after load showed `engineReady=true`, idle render total around 0.9 ms, `dropsLastSecond=0`, 3 interactive HTML-media resources, and 140 thumbnail bitmap resources retained under the thumbnail policy. `simulatePlayback` for 2 seconds from 0s advanced by 2.049s with max stall 50 ms and no freeze; `simulatePlaybackPath(play_scrub_stress_v1)` completed in 15.4s. A 1s `debugExport` smoke without download returned a `video/mp4` blob of 968,838 bytes. Post-export stats showed `engineReady=true`, `playbackStatus=ok`, `playbackStalls=0`, `previewFreezeEvents=0`, `exportResources=0`, `gpuTextures=0`, and no WebGPU-lost symptoms. Remaining browser-console noise is outside this timeline refactor slice: Windows WebGPU adapter/device timeout warnings, NativeHelper websocket unavailable, duplicate React key `success-MasterSelects 2.1.0`, and passive-listener `preventDefault` warnings.

**Latest interactive/export/shared image checkpoint:** Phase 5 now has top-level and nested data-only image restore plus preview, export, RAM-preview, composition-render, background-layer, slot-deck, direct add-image, image clipboard-paste, and `useLayerSync` image hydration coverage. `serializationUtils.loadState()` and `nestedCompositionLoader.ts` write restored image clips with managed `source.imageUrl` instead of constructing `HTMLImageElement`s during project load or nested restore. `addImageClip.ts` now keeps the loaded image local for thumbnail/native-scale preparation and writes managed `source.imageUrl`; image clipboard paste now writes a `blobUrlManager`-owned `source.imageUrl` without constructing `Image`. `lazyImageElements.ts` owns a capped interactive `HTMLImageElement` cache, reports `image-canvas/html-image` resources through the timeline runtime coordinator, releases file-backed object URLs, notifies overlay sync on image status changes, and does not mutate `clip.source`. `LayerBuilderService`, RAM preview, composition render, nested composition render, and `useLayerSync` can render data-only image clips through bounded lazy image paths or shared prepared sources. `ClipPreparation` prepares data-only image clips into `ExportClipState.exportImageElement`, `ExportLayerBuilder` uses that state before legacy `source.imageElement`, and export cleanup revokes only export-owned image blob URLs. `imageRuntimeHydrator.ts` supplies cancellable/stale-guarded image loading for composition render, background layer playback, warm slot decks, and add-image thumbnail/native-scale preparation. This is still not the final allocator API: non-image user-action media hydration and allocator ownership remain separate. Focused checks passed: `npm run test -- tests/unit/serializationNestedRestore.test.ts tests/unit/lazyMediaElements.test.ts tests/unit/layerBuilderService.test.ts tests/unit/exportLayerBuilder.test.ts tests/unit/clipPreparation.test.ts` (82 tests), `npm run test -- tests/unit/compositionRendererRuntimeReporting.test.ts tests/unit/layerPlaybackManagerWarmDeck.test.ts tests/unit/slotDeckManager.test.ts tests/unit/lazyMediaElements.test.ts tests/unit/layerBuilderService.test.ts` (46 tests), `npm run test -- tests/unit/serializationNestedRestore.test.ts tests/unit/addCompClipNestedRestore.test.ts tests/unit/projectMediaPersistence.test.ts tests/unit/nestedRestoreRuntimeHelpers.test.ts` (102 tests), `npm run test -- tests/unit/addImageClip.test.ts tests/unit/lazyMediaElements.test.ts tests/unit/layerBuilderService.test.ts tests/unit/clipPreparation.test.ts tests/unit/compositionRendererRuntimeReporting.test.ts tests/unit/serializationNestedRestore.test.ts` (77 tests), touched-file ESLint, plus `npx tsc -p tsconfig.app.json --noEmit --pretty false`. Latest LOC status from `npm run swarm:status`: `198` tracked changed files, `70` new untracked files, tracked `+14316/-15697` net `-1381`, plus `+24929` LOC in new files.

**Latest move resolver checkpoint:** Phase 3 has a pure `moveResolution.ts` resolver that emits structured-clone-safe `ResolvedClipMove[]` plus explicit move operations without mutating the store. It models snap/resistance, fallback-track intent, linked clips/groups, selected linked-pair dedupe, lock/compatibility warnings, overlap-trim oracle data, and provisional new-track intent. `useClipDrag` now routes desktop body-drag commits through typed operations, including resolved fallback-track, overlap-trim, and selected linked-pair cases; resolver warnings and typed apply failures no longer silently fall back to legacy `moveClip()` in the desktop drag adapter. The store operation plans pure track additions, validates mapped moves against planned tracks, commits clips/tracks/resource cleanup/selection/duration/cache invalidation in one history batch, and `moveOverlapTrim.ts` applies pure legacy-equivalent trim/delete mutations. The related geometry contracts expose trim previews, fade handle IDs, keyframe diamond rect IDs, and marquee exclusion helpers; keyboard delete/blend-mode also dispatch executable operation contracts. Broader side-by-side old-vs-new parity smokes are still needed. Targeted checks passed: touched-file ESLint, `npm run test -- tests/unit/resolvedClipMove.test.ts tests/unit/resolvedClipMoveOverlapTrim.test.ts tests/unit/timelineEditOperations.test.ts tests/stores/timeline/trackSlice.test.ts`, `npm run test -- tests/unit/resolvedClipMove.test.ts tests/unit/timelineEditOperations.test.ts tests/unit/timelineEditOperationContracts.test.ts`, `npm run test -- tests/unit/timelineRenderModel.test.ts`, `npm run test -- tests/unit/timelineRenderModel.test.ts tests/unit/timelineEditOperationContracts.test.ts`, `npm run test -- tests/unit/useMarqueeSelection.test.tsx tests/unit/TimelineTrack.test.tsx`, `npm run test -- tests/unit/useTimelineKeyboard.test.tsx tests/unit/timelineEditOperations.test.ts tests/unit/timelineEditOperationContracts.test.ts`, and `npx tsc -p tsconfig.app.json --noEmit --pretty false`.

**Latest transition operation checkpoint:** Phase 3 transition apply/remove/update and preview/drop-clear now route through executable typed operation contracts instead of direct clip mutation or local-only drag state. `transitionOperations.ts` validates clip existence, same-track ordering, registry transition type, positive duration, locked tracks, and optional transition id matches; it clamps duration with the existing transition min/max and half-clip limits, writes reciprocal `transitionOut`/`transitionIn` metadata, restores incoming clip timing on remove, and updates duration atomically without remove-then-reapply. `applyTimelineEditOperation()` handles `transition-apply`, `transition-remove`, `transition-update-duration`, `transition-preview-drop`, and `transition-clear-preview`; preview writes `timelineToolPreview` ghost ranges or blocked preview state, and clear removes that state. `transitionSlice.ts` is now a compatibility wrapper around apply/remove/update, `useTransitionDrop` dispatches typed preview/drop/clear contracts with junction geometry references, and the AI transition tool defaults to the registered `crossfade` id. Targeted checks passed: touched-file ESLint, `npm run test -- tests/unit/timelineEditOperations.test.ts tests/unit/timelineEditOperationContracts.test.ts tests/unit/timelineRenderModel.test.ts`, `npm run test -- tests/unit/timelineEditOperations.test.ts tests/unit/timelineEditOperationContracts.test.ts tests/unit/timelineToolOverlayLayer.test.ts tests/unit/timelineEditReplayDescriptors.test.ts` (61 tests), and `npx tsc -p tsconfig.app.json --noEmit --pretty false`.

**Latest keyframe transaction checkpoint:** Phase 3 now has an executable generic keyframe transaction kernel for begin/update/commit/cancel operations. `TimelineEditOperation` includes the `KeyframeTransactionOperation` union, `applyTimelineEditOperation()` validates transaction clips/locked tracks, applies numeric keyframe create, move, value update, remove, easing update, bezier-handle update, rotation-interpolation update, and selection operations in one history batch, returns changed keyframe clip IDs from a before/after keyframe snapshot diff, and warns for missing or locked keyframe targets while still applying valid operations. Path-value creation was added in the later checkpoint below. Guided replay descriptors now cover Fade and Keyframe transaction types. Targeted checks passed: `npm run test -- tests/unit/timelineEditOperations.test.ts tests/unit/timelineEditOperationContracts.test.ts tests/unit/timelineEditReplayDescriptors.test.ts` (59 tests), touched-file ESLint, and `npx tsc -p tsconfig.app.json --noEmit --pretty false`.

**Latest fade transaction checkpoint:** Phase 3 Fade is no longer contract-only. `applyTimelineEditOperation()` now executes `fade-transaction-update`, `fade-transaction-commit`, and `fade-transaction-cancel` by materializing/removing the planned zero/one fade keyframes, preserving existing curve handles, cleaning selected deleted keyframes, and using one history batch across live update plus commit. `useClipFade` now dispatches fade begin/update/commit operations instead of direct `addKeyframe`/`moveKeyframe`/`removeKeyframe` calls, while still creating the legacy audio-volume effect at fade start when needed. Targeted checks passed: `npm run test -- tests/unit/timelineEditOperations.test.ts tests/unit/timelineEditOperationContracts.test.ts tests/unit/timelineEditReplayDescriptors.test.ts` (61 tests), touched-file ESLint, and `npx tsc -p tsconfig.app.json --noEmit --pretty false`.

**Latest keyframe tick-drag transaction checkpoint:** Phase 3 selected clip-bar keyframe tick drag now routes through typed keyframe transactions instead of the legacy direct `onMoveKeyframeGroup` mutation path. `ClipKeyframeTicks` emits `begin` / `update` / `commit` phases, `useClipKeyframeTickDrag` tracks the latest resolved drag time, `TimelineTrack` opens a `keyframe-transaction-begin`, sends live `keyframe-transaction-update` operations with `deferHistoryCommit:true`, and closes with `keyframe-transaction-commit`; click-without-move intentionally does not commit a history operation. The legacy callback remains only as a fallback when no `applyTimelineEditOperation` is available. Targeted checks passed: `npm run test -- tests/unit/ClipInteractionShell.contract.test.tsx tests/unit/TimelineTrack.test.tsx tests/unit/timelineEditOperations.test.ts tests/unit/timelineEditOperationContracts.test.ts` (100 tests), touched-file ESLint, and `npx tsc -p tsconfig.app.json --noEmit --pretty false`.

**Latest curve-editor transaction checkpoint:** Phase 3 expanded curve-editor drags now route through typed keyframe transactions. `CurveEditor` emits `begin` / `update` / `commit` phases for keyframe-point and Bezier-handle drags, `TimelineTrack` translates keyframe drags into paired `keyframe-move` plus `keyframe-update-value` operations, translates Bezier drags into `keyframe-update-bezier-handle`, defers live-update history commits, and skips commit when a drag never moved. The legacy direct callbacks remain fallback-only when no `applyTimelineEditOperation` is available. Path-value and Mask/Text routing were added in the later checkpoints below. Targeted checks passed: `npm run test -- tests/unit/CurveEditor.test.tsx tests/unit/TimelineTrack.test.tsx tests/unit/timelineEditOperations.test.ts tests/unit/timelineEditOperationContracts.test.ts` (87 tests), touched-file ESLint, and `npx tsc -p tsconfig.app.json --noEmit --pretty false`.

**Latest path-value keyframe kernel checkpoint:** Phase 3 path-value keyframe creation is no longer unsupported in the generic transaction kernel. `keyframe-create` with `value.pathValue` now creates a cloned path keyframe, clamps the requested time to the clip duration, updates an existing same-property/same-time keyframe instead of duplicating it, invalidates the timeline cache, and reports the changed clip through the same before/after keyframe snapshot diff as numeric transactions. Targeted checks passed: `npm run test -- tests/unit/timelineEditOperations.test.ts tests/unit/timelineEditOperationContracts.test.ts` (56 tests), touched-file ESLint, and `npx tsc -p tsconfig.app.json --noEmit --pretty false`.

**Latest Mask/Text path-keyframe routing checkpoint:** Phase 3 direct Mask/Text path-keyframe UI routing now uses the transaction kernel through compatibility store APIs. `addMaskPathKeyframe()` and `addTextBoundsPathKeyframe()` build `keyframe-transaction-commit` operations with `keyframe-create` `pathValue` payloads, updating existing same-time path keyframes through the kernel. Continuous mask vertex/edge drags use `phase:'update'` plus deferred history commits so the outer drag batch remains one undo step. `disableMaskPathKeyframes()` and `disableTextBoundsPathKeyframes()` now remove path keyframes through deferred transaction remove operations while preserving the requested static path value and recording toggle cleanup. Latest LOC status from `npm run swarm:status`: `208` tracked changed files, `70` new untracked files, tracked `+16343/-15947` net `+396`, plus `+24982` LOC in new files. Targeted checks passed: `npm run test -- tests/stores/timeline/keyframeSlice.test.ts tests/unit/timelineEditOperations.test.ts tests/unit/timelineEditOperationContracts.test.ts` (181 tests), touched-file ESLint, and `npx tsc -p tsconfig.app.json --noEmit --pretty false`.

**Latest AddComp/Split composition-audio lazy placeholder checkpoint:** Phase 5 no longer starts composition audio mixdowns when adding a composition clip. `createCompLinkedAudioClip()` now creates a data-only linked audio placeholder with silent waveform metadata, no `compositionAudioMixer.mixdownComposition(...)`, no `compositionAudioMixer.createAudioElement(...)`, and no fallback `document.createElement('audio')`. `createCompositionAudioClip()` now accepts missing `audioElement` without allocating one. Legacy `splitClip` and `split-at-times` now strip composition-audio `source.audioElement` for first and later split parts and do not schedule mixdown audio-element cloning for composition linked audio. Playback/export mixdown-on-demand was open at this checkpoint and is now covered by the later `compositionAudioMixdownCache.ts` slice. Latest LOC status from `npm run swarm:status`: `211` tracked changed files, `70` new untracked files, tracked `+16566/-15992` net `+574`, plus `+25121` LOC in new files. Targeted checks passed: `npm run test -- tests/unit/addCompClipNestedRestore.test.ts tests/stores/timeline/clipSlice.test.ts tests/unit/timelineEditOperations.test.ts` (218 tests), touched-file ESLint, and `npx tsc -p tsconfig.app.json --noEmit --pretty false`.

**Latest linked-group parity checkpoint:** Phase 3 now has explicit linked-group resolver and apply coverage. `moveResolution.ts` records skipped group followers when `includeGroups:false`/Alt-style independent drag is requested, while normal group following emits all linked-group clip moves with preserved offsets. The operation kernel path now has tests proving a linked group applies through `applyTimelineEditOperation(move-clips)`, group followers stay in place when group following is disabled, and locked follower tracks block the resolved group move before any partial typed commit. This closes the first linked clip / linked group parity proof for typed drag commits; broader UI drag smokes and remaining warning fallback removal are still separate follow-up work. Targeted checks passed: touched-file ESLint, `npm run test -- tests/unit/resolvedClipMove.test.ts tests/unit/timelineEditOperations.test.ts tests/unit/resolvedClipMoveOverlapTrim.test.ts`, and `npx tsc -p tsconfig.app.json --noEmit --pretty false`.

**Latest desktop drag fallback checkpoint:** `useClipDrag` no longer mutates through desktop `addTrack()`/`moveClip()` when a typed resolver warning or typed apply failure occurs. The hook now uses `createClipDragTypedMoveCommitOperation()` to choose either direct `move-clips` or `move-clips-resolved` for fallback-track, overlap-trim, and selected-linked-pair hard cases; unsupported/warning/empty plans log and stop instead of taking the legacy mutation path. `Timeline.tsx` no longer passes `addTrack` or `moveClip` into the desktop drag hook. The legacy store `moveClip()` API remains for mobile and direct callers, but desktop body-drag commits are typed-only. Targeted checks passed: touched-file ESLint, `npm run test -- tests/unit/useClipDragCommit.test.ts tests/unit/resolvedClipMove.test.ts tests/unit/timelineEditOperations.test.ts`, `npx tsc -p tsconfig.app.json --noEmit --pretty false`, and `rg -n "moveClip\\(|addTrack\\(|legacy path|deferred to legacy" src/components/timeline/hooks/useClipDrag.ts src/components/timeline/Timeline.tsx tests/unit/useClipDragCommit.test.ts` returned no matches.

**Latest passive decoration checkpoint:** `TimelineClipCanvas` now draws lightweight passive transcript markers, sampled focus/motion/face analysis overlays, download/transcription/analysis/proxy progress bars, and compact status badges for download errors, proxy/audio-proxy state, transcript/analysis state, reversed clips, and linked groups. These are plain canvas decorations derived from clip/media metadata; they do not revive the deleted DOM badge/overlay components or add per-clip React subscriptions. The OffscreenCanvas worker path can now carry simple passive badges/progress, bounded transcript markers, sampled focus/motion/face analysis overlays, reversed `R` badges, source-extension trim ghosts, and composition-specific visuals as prepared resources.

**Latest composition visual checkpoint:** `TimelineClipCanvas` now declares and renders composition-specific passive data: orange composition outlines, segment thumbnail strips from `clipSegments`, nested composition boundary lines, mixdown waveform display from `mixdownWaveform` or composition waveform fallback, and a lightweight mixdown generation label. Composition clips with segment thumbnails suppress the normal source-cache filmstrip, matching the former DOM planning behavior. Composition-only visuals also keep the unfinished worker path disabled until the worker payload supports those resources.

**Latest geometry checkpoint:** `TimelineTrack` now measures the real `.track-clip-row` width with `ResizeObserver` and uses that width for visible clip culling, shell viewport geometry, and `TimelineClipCanvas.viewportWidth`. The old `window.innerWidth` viewport assumption is retired from the track row path, so docked panels, resized layouts, and mobile-width timelines no longer over-warm or overdraw based on the full browser window.

**Latest decomposition checkpoint:** `TimelineClip.tsx` was first reduced from a 3364 LOC monolith into focused renderers/hooks, then deleted once `TimelineTrack`, new-track drag previews, and composition-switch overlays stopped importing it. The former passive DOM stack is gone: thumbnail filmstrips, passive waveform/spectrogram display, transcript/analysis overlays, status badges, source-extension ghosts, and selected-region hitareas now belong to `TimelineClipCanvas`, shared drawing utilities, or timeline cache services. Active behavior belongs to `ClipInteractionShell` modules for trim/fade/keyframes/audio regions/spectral regions/video bake/stems plus focused hooks and typed operations. `TimelineClipProps` and the old full-body renderer prop are gone; `TimelineTrack` no longer accepts a full clip renderer. Stem job active-phase handling, clip source timing/loop-extension helpers, trim minimum duration, timeline render overscan, and empty stem choices are now shared instead of duplicated in UI, store, trim, and canvas files. Historical decomposition checks passed before the deletion slice; the current refactor batch is intentionally waiting for end-of-refactor full checks.

**Latest debug checkpoint:** the preview "horizontal lines" artifact was reproduced through the AI bridge in both GPU readback and DOM canvas capture. The active layer was using `proxy-image-frame` while proxy mode was enabled and the editor was paused; `purgePlaybackPath` cleared the cold cache but the stale/corrupt proxy frame still rendered as stripes. Paused inspection now stays on the live HTML video path, while JPEG proxy frames are reserved for interactive scrub/drag previews, and `VideoSyncManager` follows the same proxy gating. `VideoSyncManager` also now rechecks actual GPU-ready state instead of trusting `video.played.length`, so a video that has played once but is still not GPU-ready can be warmed again. Post-change capture returned real video frames. A follow-up playback probe found `inputLayers=1` but `collectedLayerData=0` when the full WebCodecs provider had no frame and the HTML video was paused; `LayerCollector` now has a playback-only `playback-html-fallback`, and `VideoSyncManager` can start a ready HTML video when the provider is not yet frame-ready. This still needs the next bridge playback checkpoint.

## Goal

Make the timeline clip area fast and maintainable for large projects by making the canvas path the real clip UI architecture, not a partial optimization layered on top of the old DOM clip system.

The target is a strict separation:

- Canvas renders all passive clip bodies: rectangles, labels, thumbnails, status badges, transcript/analysis markers, source-extension ghosts, fade curves, waveform summaries, and artifact-backed spectrogram summaries.
- DOM renders only active interaction shells: hovered clips, active drag/trim/fade states, open menus, keyframe/audio/spectral/video/stem active controls, and tool-specific drop or pointer affordances.
- Selected-only visuals stay canvas-rendered. Do not mount selected-only DOM clip bodies.
- Playback, export, RAM preview, thumbnail/render-target generation, nested composition evaluation, and other render services own live media elements, decoder warmup, audio sync, WebGPU texture import, and runtime hydration through explicit policies.
- Cache services own thumbnails, waveform, spectrogram, loudness, beat/onset, and frequency/phase artifacts. View components request visible render data; they do not generate or hydrate media.

## Why This Refactor

The current hybrid state is useful but not clean enough:

- The canvas clip renderer reduces visible DOM work, and the old `TimelineClip` component body has been deleted. Remaining work is forward parity in canvas drawing, active shell modules, cache schedulers, context-menu command models, geometry, runtime ownership, and history state.
- Project load and playback can still touch live media elements for too many clips when runtime ownership is unclear.
- Existing generated thumbnails can now hydrate from IndexedDB before playback, and missing visible thumbnails can generate from `MediaFile.url` through a bounded background scheduler.
- Maintaining stale assumptions from the former DOM clip body in docs, tests, CSS, target resolvers, or tools makes bugs likely and makes performance regressions easy. Passive clip bodies are canvas-rendered.
- The current worker path is too small to be the architecture. It draws basic rects/labels and lacks hover, trim/source-extension geometry, in/out/reverse visuals, thumbnails, waveforms, and spectrogram resources.

## Non-Goals

- No throwaway MVP canvas rewrite.
- No silent removal of editing features.
- No playback/export rewrite unless needed to separate timeline UI from media runtime.
- No big-bang deletion before interaction parity, cache parity, diagnostics, and playback/export smoke tests prove the replacement.

## Target Architecture

### 1. Timeline Render Model

Create a compact, plain, structured-clone-safe render model consumed by the canvas and active shell layer.

It should contain only render data and references:

- clip id, track id, start, duration, in/out, speed/reverse, source type
- label, colors, selected/hovered/linked/locked/muted state
- passive badge state: proxy, audio proxy, download, stem, reload, reversed, linked, transcript, analysis, nested composition
- cache refs for thumbnails, waveform, spectrogram, loudness, beat/onset, frequency/phase, and analysis markers
- computed geometry fields where useful, or stable inputs to the shared geometry resolver

It must not contain `File`, `HTMLVideoElement`, `HTMLAudioElement`, `ImageBitmap` ownership, object URLs, DOM nodes, WebCodecs players, native decoder handles, store actions, cache-warming callbacks, or playback state.

Persistence boundary: `TimelineRenderModel` is plain render/worker-transfer data, not project schema. It is derived from `CompositionTimelineData`, media-store metadata, cache registry state, and transient UI state after load. Do not persist hover/selection, canvas geometry, object URLs, DOM nodes, `ImageBitmap`s, waveform payloads, spectrogram payloads, or cache payloads into `SerializableClip`, `ProjectClip`, or `CompositionTimelineData`.

### 2. Timeline Geometry Snapshot

Use one geometry source for canvas, overlays, hit testing, marquee, drag, trim, fade, keyframes, transitions, markers, and split targets.

The snapshot must include:

- track lane rects and row viewport rects measured from the real scroll container, not `window.innerWidth`
- clip body rects
- source-extension and trim ghost rects
- trim handle rects and linked/follower trim preview rects
- fade handle rects and fade curve geometry
- keyframe rows, ticks, and diamond rects
- transition junctions and drop zones
- marquee exclusion zones
- drag/drop targets for clips, image layers, spectral regions, audio regions, and tools
- timeline ruler/grid conversion helpers

Normalize `canvasContentWidth`/`contentWidth`: either remove it or make it part of `TimelineGeometrySnapshot`. Do not keep a prop that is computed in `TimelineTrack` but ignored by `TimelineClipCanvas`.

### 3. Canvas Renderer

Canvas becomes the single visible renderer for passive clip bodies.

Expected responsibilities:

- viewport culling and level of detail
- source thumbnail drawing from cache
- waveform summary drawing from cache
- real spectrogram summary drawing from `timelineSpectrogramArtifactWarmup` plus `utils/spectrogramCanvas`
- fade curve drawing via a shared drawing utility
- transcript and analysis markers
- nested composition mixdown waveforms and boundaries
- passive status badges and labels
- selected/hovered visual feedback where cheap
- density rendering at extreme zoom

Do not promote the current `CanvasClip` type to the final model. The target renderer input must remove `File`, DOM element, store-action, and cache-warming dependencies.

### 4. Clip Interaction Shell

Replace the generic `renderClip` callback and full `TimelineClip` overlay with a typed `ClipInteractionShell` contract.

Shell inputs:

- resolved geometry
- interaction state
- command callbacks
- active module state
- cache/render refs needed for hit areas, not payload ownership

Mount rules:

- hovered clip
- active drag or multi-drag clip
- active trim clip and linked/follower trim preview clips
- active fade clip via explicit `clipFade` state
- open clip, audio, spectral, stem, or video menu
- selected clips with visible keyframes or active keyframe focus
- active audio region context menu, selection, or gain drag
- active spectral selection, drop target, or toolbar
- active video bake region
- active stem menu/job
- tool preview state

Selected-only clip body visuals remain canvas-rendered.

### 5. Specialized Active Modules

The deleted `TimelineClip.tsx` body must not be recreated. New passive work belongs in `TimelineClipCanvas` or shared drawing utilities; active work belongs in `ClipInteractionShell` modules.

Canvas owns:

- static clip body
- fill/color/selection/hover visuals
- labels and passive badges
- source thumbnails
- passive waveform and spectrogram summaries
- fade curves
- transcript/analysis markers
- nested mixdown waveform and boundaries
- source-extension trim ghosts

Active shell modules own:

- root pointer/context dispatch
- trim and fade handles
- keyframe ticks/edit controls
- audio region selection, gain, context menu, and edit stack
- spectral selection, image-layer drop, toolbar, and context menu
- video bake controls
- stem switcher and stem job controls
- drag/drop/drop-target affordances

Subcomponent disposition:

- Waveform rendering: the DOM `ClipWaveform` component is retired; keep passive waveform work in the canvas path and shared drawing utilities, with active-only controls in shell modules.
- Spectrogram rendering: DOM fallback retired; keep passive spectral display in `TimelineClipCanvas`, `timelineSpectrogramArtifactWarmup`, and `utils/spectrogramCanvas`.
- `ClipAnalysisOverlay`: move passive markers/summary to canvas; keep active playhead-coupled editing in shell only if needed.
- `FadeCurve`: move path math into shared geometry/drawing utility; shell owns pointer handles.
- `TimelineContextMenu`: split command model from DOM/browser/media dependencies.

### 6. Runtime Boundary

Interactive playback hydrates active and near-active timeline resources under capped budgets.

Export, RAM preview, thumbnail/render-target generation, and nested composition evaluation use explicit non-interactive runtime policies and must not be collapsed into playback hydration without parity tests.

Runtime must account for:

- same-track transition overlap where two clips on one track can render at once
- background layers
- active slot decks/program output
- nested composition recursion
- masks/effects inside nested compositions
- render-target and thumbnail generation paths
- export clip preparation, seeking, layer building, audio, and cleanup
- RAM preview policy

### 7. Cache Boundary

Source and analysis caches are warmed independently from playback:

- existing generated thumbnails load from IndexedDB for visible source ids
- missing thumbnails generate through a controlled background queue, never during playback start
- decoded thumbnail bitmaps are cached and closed through an explicit LRU/invalidation path
- source waveform generation remains job-coalesced
- artifact reads for waveform, spectrogram, loudness, beat/onset, and frequency/phase gain in-flight coalescing per artifact/ref id
- cache changes notify canvas redraw without React clip remounts

## Parallel Streams

Use up to 6 agents for independent implementation streams. Agents must not edit the same files unless explicitly coordinated.

1. **Canvas renderer and timeline track shell**
   - Files: `TimelineClipCanvas.tsx`, `TimelineTrack.tsx`, canvas worker, canvas utilities, related CSS.
   - Output: render model, geometry snapshot, passive visual parity, worker-readiness contract.

2. **Post-retirement shell and passive parity**
   - Files: `ClipInteractionShell` modules, `TimelineClipCanvas.tsx`, timeline CSS, context menu command models.
   - Output: no full DOM clip body, active shell parity matrix, passive canvas parity for markers/badges/nested visuals.

3. **Interaction and edit operations**
   - Files: timeline hooks, edit operations, selection/drag/trim/fade/keyframe/transition flows.
   - Output: typed operation parity, transaction model, shared geometry hit testing.

4. **Runtime and playback/export boundary**
   - Files: layer builder, media runtime, playback loop, export preparation/seeking/building, RAM preview, composition renderer.
   - Output: runtime policy registry, diagnostics, parity test matrix.

5. **Thumbnail/audio/cache pipeline**
   - Files: thumbnail cache, thumbnail bitmap cache, waveform/spectrogram/loudness/beat/frequency caches, audio analysis jobs.
   - Output: scheduler lanes, in-flight coalescing, invalidation and deletion contract.

6. **Project load, serialization, and history**
   - Files: serialization, project load/save, history snapshots, media-store project paths.
   - Output: persisted data contract, undo state contract, runtime rehydration adapter, legacy path decision.

## Swarm Execution Protocol

Use agent swarms only with explicit phase and file ownership. The default first swarm is Phase 0 only. Later phases require Phase 0 contracts to be merged first.

### Swarm Modes

- **Read-only review:** agents inspect code and return findings. They do not edit files.
- **Contract worker:** agents create or update TypeScript contracts, tests, docs, and diagnostics in assigned files only.
- **Implementation worker:** agents change runtime behavior in assigned modules only after contracts are merged.
- **Verifier:** agents run focused checks, bridge smoke tests, or regression scripts while implementation workers continue non-overlapping work.

### Phase 0 Agent Packets

Run these packets in parallel for the first swarm. Each packet must produce a short final report with changed files, tests run, risks, and follow-up dependencies.

1. **Agent A: Render Model And Geometry Contracts**
   - Owns: new render-model and geometry contract files, geometry tests, pure helper tests.
   - May read: `TimelineClipCanvas.tsx`, `TimelineTrack.tsx`, `Timeline.tsx`, timeline CSS, existing canvas worker.
   - Must not edit: playback/runtime/export files.
   - Deliverable: `TimelineRenderModel`, `TimelineGeometrySnapshot`, purity tests, and measured viewport contract.

2. **Agent B: ClipInteractionShell Contract**
   - Owns: shell prop types, shell module folder, shell parity test scaffolding.
   - May read: `TimelineTrack.tsx`, shell modules, context-menu files, and deleted `TimelineClip.tsx` history through `git show HEAD:...` when parity archaeology is needed.
   - Must not recreate the full `TimelineClip.tsx` body in Phase 0.
   - Deliverable: typed shell contract, mount-state model, active module slot names, and parity matrix.

3. **Agent C: Edit Operation Contracts**
   - Owns: edit operation types/tests and transaction interfaces.
   - May read: drag, trim, fade, keyframe, marquee, keyboard, transition hooks and slices.
   - Must not edit: hook behavior in Phase 0 except test-only fixtures.
   - Deliverable: `ResolvedClipMove`, fade/keyframe/transition transaction contracts, and legacy parity checklist.

4. **Agent D: Cache Scheduler Contracts**
   - Owns: scheduler interfaces, cache lane definitions, invalidation contract tests.
   - May read: thumbnail cache, thumbnail bitmap cache, waveform/spectrogram/loudness/beat/frequency caches, audio jobs.
   - Must not edit: `TimelineClipCanvas.tsx` cache behavior in Phase 0.
   - Deliverable: cache scheduler lane model, in-flight coalescing key contract, media deletion invalidation contract.

5. **Agent E: Runtime Policy Contracts**
   - Owns: runtime policy descriptor types, diagnostics shape, bridge-facing stats contract.
   - May read: `LayerBuilderService`, media runtime, playback loop, composition renderer, RAM preview, export preparation/seeking/building.
   - Must not edit: export/render behavior in Phase 0.
   - Deliverable: `TimelineRuntimeCoordinator` contract as policy registry and budget reporter, `RenderResourceDescriptor`, diagnostics schema.

6. **Agent F: Persistence And History Contracts**
   - Owns: project/render/history boundary docs, undo state contract tests, load-path audit notes.
   - May read: serialization, project load/save, history store, media-store project slices.
   - Must not edit: project load behavior in Phase 0 except isolated contract tests.
   - Deliverable: `HistoryTimelineEditState` contract, runtime rehydration adapter interface, legacy `projectSlice` decision record.

### Shared File Rules

- Shared contract files must be created before implementation files depend on them.
- Only one agent may own a shared contract file at a time. If another agent needs a type, they add a request to their report rather than editing the owner file.
- High-conflict files are single-owner per phase: `TimelineTrack.tsx`, `TimelineClipCanvas.tsx`, `Timeline.tsx`, `ClipInteractionShell` modules, timeline hooks, timeline store slices, `LayerBuilderService.ts`, export files, serialization files, and `historyStore.ts`.
- CSS files are also single-owner per phase because visual shell changes can silently break hit testing.
- Agents must not perform broad formatting or unrelated cleanup.
- Agents must not revert changes made by other agents or by the user.

### Merge Order

1. Merge Phase 0 contracts in this order: render model/geometry, shell contract, edit operations, cache scheduler, runtime policy, history/persistence.
2. Run focused type/lint checks after contract merge.
3. Phase 1 shell work is now post-retirement work: it must compile without importing or recreating `TimelineClip.tsx`.
4. Start Phase 2 only after cache scheduler interfaces compile and cache invalidation tests exist.
5. Start Phase 3 only after edit operation contracts and geometry hit-test data are merged.
6. Start Phase 4 only after shell, cache, interaction, and bridge playback parity pass.
7. Start Phase 5 runtime/export migration only after Phase 4 proves the timeline UI no longer depends on full DOM clip bodies.
8. Start Phase 6 worker work only after main-thread render-model parity is stable.

### Swarm Acceptance Checks

Each agent packet must finish with:

- changed file list
- LOC status from `npm run swarm:status`
- behavior changed or no behavior changed
- tests/checks run
- known risks
- required follow-up owner
- files intentionally not touched

Each phase integration must run:

- targeted TypeScript or lint checks for changed files
- relevant unit tests for changed contracts/modules
- AI bridge reload/playback smoke when timeline behavior changed
- `debugExport` smoke when runtime, nested comp, or export paths changed

### Regular Status Updates

During swarm work, the lead agent posts a status update at least every 30 minutes and after every agent packet lands.

Use:

```bash
npm run swarm:status
```

For long-running local monitoring, use:

```bash
npm run swarm:status:watch -- 60
```

Each status update must include:

- current phase and active packets
- files changed since `HEAD`
- new untracked files and their LOC
- tracked LOC added/deleted/net from `git diff --numstat HEAD`
- tests or bridge checks run since the previous update
- blockers or file-ownership conflicts

The status command reports tracked LOC deltas and untracked file LOC. It is a working-tree report, not a replacement for code review or tests.

### Conflict Rules

- If two agents need the same file, pause one packet and split the work into sequential sub-packets.
- If a contract shape changes after another agent has started implementation, update the contract first and ask dependent agents to rebase their local assumptions before editing.
- If an implementation needs behavior outside its packet, record it as a follow-up instead of widening scope.
- If a verifier finds a regression, stop new dependent implementation work until the owner packet fixes or explicitly defers the regression.

## Code Analysis Findings

### Current Canvas State

- The old `timelineCanvasClips` rollback flag is retired; `TimelineTrack` always renders `TimelineClipCanvas` as the visible clip body renderer.
- `TimelineTrack` renders DOM only through `ClipInteractionShell` overlays for active controls.
- Current DOM mounting is not selected-only; it mounts hovered, dragged, multi-drag, and trimmed clips. Re-adding selected-only DOM shells would regress select-all culling.
- The original removal target, full `TimelineClip` mounting inside `.timeline-canvas-dom-overlay`, is done for track rows.
- Legacy overlay CSS hiding hacks are gone; only shell-specific overlay rules remain.
- The former `TimelineTrack` full-renderer prop is gone, and `Timeline.tsx` no longer has a `renderLegacyClip` helper.
- Actual viewport measurement is now in the track row path: `TimelineTrack` measures `.track-clip-row` and uses it for culling, shell viewport geometry, and canvas viewport width.
- The current worker path is not feature-ready. `WorkerPlainClip` only carries minimal clip fields and lacks hover/selection, trim/source-extension visuals, thumbnails, waveforms, spectrogram resources, in/out/reverse handling, and active preview state.

### Retired TimelineClip Responsibility Map

- `TimelineClip.tsx` has been deleted; active behavior now lives in `ClipInteractionShell`, extracted feature modules, and canvas-backed row renderers.
- Passive body features are no longer allowed to re-enter a full DOM clip body. Remaining parity work should move through `TimelineClipCanvas`, shell modules, or cache/render services.
- `useClipContextMenu` only selects and stores `{ x, y, clipId }`; the first pure clip command model now lives in `utils/clipContextMenu.ts`, while DOM/browser/media side effects remain inside `TimelineContextMenu`. Track and empty timeline menus also have plain command-model builders.
- Processed waveform/spectrogram warming must stay outside the deleted clip component and move through timeline cache schedulers.
- `TimelineClipCanvas` no longer owns visible thumbnail DB loading, waveform artifact reads, spectrogram artifact reads, or source waveform generation. Those moved to timeline services with in-flight coalescing or generation scheduling; loudness, beat/onset, frequency/phase, and invalidation coverage now have service-side warmup/invalidation slices.

### Interaction Findings

- Drag has two contracts today: local `ClipDragState` and store-level `clipDragPreview` patches. Keep preview state, but move hit testing and geometry into `TimelineGeometrySnapshot`.
- Desktop body drag now commits through typed `move-clips`/`move-clips-resolved` operations for the covered hard cases; mobile and direct store callers still keep compatibility access to legacy `moveClip`.
- Trim canvas geometry now has plain-data lead/follower preview records; remaining work is broader UI parity around trim/fade/keyframe transaction flows.
- Generic keyframe transaction operations now execute through the operation kernel for numeric create/move/update/remove/easing/bezier/rotation/selection paths and path-value create/update. Selected clip-bar keyframe tick drag, expanded curve-editor keyframe/Bezier drags, and Mask/Text path-keyframe compatibility APIs are routed through those transactions.
- Fade transaction execution now routes through the operation kernel and `useClipFade`; remaining Phase 3 keyframe work is explicit shell/canvas parity coverage without reintroducing per-frame legacy mutation.
- Marquee selection is mostly data-driven for keyframe diamonds and exclusion helpers; broader empty-area/tool exclusion parity still needs follow-up geometry coverage.
- Keyboard delete and blend-mode cycling now route through operation contracts.
- Transition junction geometry plus typed transition apply/remove/update and preview/drop-clear operations are implemented with store-backed preview state.
- Preserve linked behavior explicitly: Alt unlinking, selected linked pairs avoiding double movement, linked groups, and operation `includeLinked` must match preview and commit behavior.

### Runtime Findings

- `LayerBuilderService` is currently a mixed boundary: store read, media hydration, layer build, slot-layer merge, transition overlap, nested recursion, and DOM sync delegation.
- `usePlaybackLoop` is still a React hook that owns transport clock behavior, queries DOM, mutates playhead state, and seeks media elements on loop boundaries.
- Live media hydration is duplicated across primary timeline lazy media, background layers, warm slot decks, composition rendering, thumbnail/render-target generation, export, and RAM preview.
- `mediaRuntime` is the correct direction, but it is still clip/source-shaped and writes runtime IDs back onto clip sources.
- Runtime needs a renderer resource descriptor, not only decode sessions. `LayerSource` already spans video frame/provider, HTML media, image/canvas, native decoder, nested composition texture, model, gaussian splat, motion/data payload, and runtime ids.
- Export is a separate runtime policy with dedicated clip preparation, seek, layer-build, audio, and cleanup paths.
- Nested composition, thumbnail, and render-target paths prepare media outside interactive playback.
- Export layer building and clip preparation have different nested-depth behavior, which is a parity risk.
- Multilayer program output and export output are currently different contracts. Define whether export includes active slot/background layers or only the editor timeline.
- Playback diagnostics are strong but HTML-video-biased. They need provider/session health, not only `source.videoElement` health.

### Cache Findings

- Thumbnails are correctly source-level: split, trim, and reverse are render-time transforms over cached source seconds.
- Visible thumbnail cache loading is now independent from playback via `timelineThumbnailDbWarmup` and `thumbnailCacheService.loadCachedForSource`.
- Missing visible thumbnail generation now resolves source URLs from media metadata before hydrated preview elements and runs through a controlled background queue.
- `thumbnailBitmapCache` now has decoded `ImageBitmap` LRU close/clear behavior plus source/URL invalidation.
- Source waveform generation is job-coalesced and canvas-visible source demand goes through `timelineSourceWaveformWarmup`. Waveform artifact reads now have in-flight coalescing through `timelineWaveformArtifactWarmup`; spectrogram artifact reads now have the same treatment through `timelineSpectrogramArtifactWarmup`; loudness, beat/onset, and frequency/phase artifact reads now warm through `timelineAudioAnalysisArtifactWarmup`.
- Current canvas spectral mode renders real tile-backed spectrogram rasters with a bounded canvas raster cache; stylized waveform drawing remains the fallback while tiles are missing.
- Audio cache lanes include waveform, spectrogram, loudness, beat/onset, frequency, and phase.
- Media deletion now has executable memory invalidation for thumbnail source entries, decoded bitmaps, audio analysis memory refs, and clip analysis jobs. Source replacement now recomputes or clears `fileHash`, clears stale source-derived media and clip audio refs, and guards stale thumbnail DB-load promises. Remaining cache work is richer source-fingerprint equivalence for unchanged replacements, same-prefix hash collision hardening, and any follow-up canvas drawing that consumes newly warmed loudness/beat/frequency/phase caches.

### Project/History Findings

- Project persistence mostly writes clean data through `SerializableClip`.
- Runtime fields still exist on live `TimelineClip`: `File`, media elements, WebCodecs/native decoders, canvases, and blob URLs.
- Top-level video/audio restore and composition-audio restore are now metadata-only during `serializationUtils.loadState()`; the old unreachable top-level eager media-element/WebCodecs branch has been retired. AddComp linked composition-audio creation is data-only, and split operations now strip ordinary video/audio/WebCodecs/native-decoder runtime objects from new clip-part sources as well as composition-audio runtime elements. Playback/export composition-audio mixdown-on-demand now uses the shared lazy/cache helper. Direct browser video/audio add, timeline video/audio paste, media relink/reload video/audio, and download completion video/audio paths are data-only after temporary metadata/file reattachment work. 3D user-action paths now prefer media-owned URL ownership when a media id exists. Allocator ownership for the resulting runtime resources remains separate.
- Nested video clips restored by `serializationUtils.loadState()` are now data-only for direct and sub-nested composition clips; nested image/model assets and remaining post-load nested asset paths can still create object URLs/elements until their runtime descriptors are migrated.
- Cleanup is not centrally owned for all runtime resources, especially image/model/gaussian blob URLs and nested resources.
- History snapshots now carry `timelineEditState?: HistoryTimelineEditState` as the serializable sidecar. Restore prefers that sidecar and rehydrates compatible live sources through scoped runtime reporting; old snapshots without the sidecar still use the guarded legacy fallback.
- Persisted history snapshots remain disabled until the legacy fallback path is either fully retired or proven safe for all old projects.
- Legacy `mediaStore/slices/projectSlice.ts` no longer owns save/load/list/delete. It keeps only the startup compatibility `initFromDB()` delegator to `legacyStartupRestore`, guarded by `projectSliceCompatibility.test.ts`.

## Refined Workstreams

### A. Render Model And Geometry

Owner count: 1 to 2 agents.

- Add `TimelineRenderModel` for clip and track visual state.
- Add `TimelineGeometrySnapshot` for hit testing and overlay placement.
- Include track lane rects, clip body rects, trim/fade handle rects, keyframe rows/diamonds, transition junctions, marquee exclusion zones, source-extension ghosts, and drop targets.
- Use one geometry resolver for canvas, overlays, marquee, drag, trim, fade, keyframes, transitions, markers, and split targets.
- Replace `window.innerWidth` culling with measured track-row viewport geometry.
  - Done for `TimelineTrack` clip culling, shell viewport geometry, and `TimelineClipCanvas.viewportWidth`.
- Normalize or remove `canvasContentWidth`/`contentWidth`.
- Remove duplicated geometry from `TimelineTrack`, `TimelineClipCanvas`, and shell modules.
- Add render-model tests asserting no `File`, DOM element, store action, or cache-warming callback references.

### B. Clip Interaction Shell

Owner count: 1 agent.

- Replace `renderClip` with a typed `ClipInteractionShell` render contract.
- Continue shell work without a full DOM clip fallback; any missing parity must be implemented as canvas drawing, shell controls, or service/cache support.
- Plumb `clipFade` into `TimelineTrack` shell mount rules before moving fade handles.
- Define mount rules for hover, drag, trim, fade, open menus, selected clips with visible keyframes, active audio/spectral/video/stem regions, and tool preview states.
- Move trim/fade/context/keyframe hit areas into shell modules.
- Keep only active DOM controls; canvas remains the visible body.
- Add shell parity tests for selected/open/fade/keyframe/audio/spectral/video/stem states.

### C. Specialized Active Modules

Owner count: 1 to 2 agents.

- Extract audio region editing.
- Extract spectral region editing.
- Extract video bake region editing.
- Extract stem switcher and stem job controls.
- Extract context-menu command builders from live DOM/runtime dependencies.
  - Done for first-pass clip/track/empty menu pure models: `utils/clipContextMenu.ts` derives target ids, linked affected ids, lock/link/unlink permissions, effect labels, and color clipboard placement; `utils/trackContextMenu.ts` derives track add/duplicate/delete/color commands; `utils/timelineEmptyContextMenu.ts` derives gap/view commands. Proxy/thumbnail/explorer/transcription side effects remain in `TimelineContextMenu` for the next menu slice.
- Add a parity matrix for trim/fade handles, keyframe ticks, audio region selection/gain/context menu/edit stack, spectral selection/image-layer drop/toolbar, video bake controls, stem switcher, and tool pointer affordances.
- Add context-menu command-model tests without DOM/media elements.
  - Done for first-pass clip/track/empty models through `clipContextMenu.test.ts`, `trackContextMenu.test.ts`, and `timelineEmptyContextMenu.test.ts`; React menu tests remain as integration parity.

### D. Edit Operation And Transaction Contracts

Owner count: 1 agent.

- Define `ResolvedClipMove` and convert body drag commit to typed `move-clips`.
- Match legacy `moveClip` behavior: snapping, resistance, fallback track creation, overlap trimming, linked clips, linked groups, and selected linked pairs.
- Add keyframe operation types for add/remove/move/update and curve-editor transactions.
- Add fade transactions with begin/update/commit/cancel and one history batch; generic keyframe transaction execution, path-value keyframe create/update, Mask/Text path-keyframe compatibility routing, `useClipFade` routing, selected clip-bar keyframe tick-drag routing, and expanded curve-editor keyframe/Bezier routing are implemented. Remaining work is explicit parity coverage.
- Add transition operations for apply/remove/update plus transition junction geometry.
- Route keyboard edit paths through operation contracts, including keyframe delete, clip delete, and blend-mode cycling.
- Keep preview and commit contracts aligned so undo/redo sees the final resolved operation, not many transient pointer updates.

### E. Cache Warm Scheduler

Owner count: 1 agent.

- Add timeline-level visible cache scheduler.
- Move all visible-range cache warming out of components.
- Scheduler lanes:
  - thumbnail DB load
  - thumbnail generation
  - thumbnail bitmap decode
  - waveform artifact load
  - source waveform generation
  - processed waveform derivation
  - spectrogram tile artifact load/generation
  - loudness envelope artifact load/generation
  - beat/onset artifact load/generation
  - frequency/phase artifact load/generation
- Add in-flight load maps for artifact reads by source/ref id.
- Keep source waveform generation coalescing and integrate it into scheduler budget reporting.
- Define media deletion/source replacement invalidation:
  - abort queued thumbnail generation for deleted sources
  - clear source thumbnail DB rows and `thumbnailCacheService` memory entries
  - close decoded entries in `thumbnailBitmapCache`
  - evict waveform, spectrogram, loudness, beat/onset, and frequency/phase memory entries by source/processed ref id
  - cancel queued `ClipAudioAnalysisJobService` work for deleted clips
  - preserve shared file-hash/project-path artifacts when another media file still references them

### F. Runtime Coordinator

Owner count: 1 to 2 agents.

- Introduce `TimelineRuntimeCoordinator` first as a policy registry and budget reporter, not an immediate allocator replacement.
- Policies: `interactive`, `background`, `slot-deck`, `composition-render`, `thumbnail`, `render-target`, `ram-preview`, and `export`.
- Add `RenderResourceDescriptor` parity with `LayerSource`: video frame/provider, HTML media, image/canvas, native decoder, nested composition texture, model, gaussian splat, motion/data payload, audio source/clock, runtime id, diagnostics, and memory cost.
- Migrate primary lazy media first, then background/slot decks, then composition-render/thumbnail/render-target paths. Export adapter comes last.
- Keep AI bridge playback diagnostics as a first-class contract.
- Add provider/session health diagnostics, not only HTML media element diagnostics.

### G. Serializable State And History

Owner count: 1 agent.

- Define separate contracts for project persistence (`CompositionTimelineData`), canvas render state (`TimelineRenderModel`), and undo state (`HistoryTimelineEditState`).
- Make persisted/edit state explicitly serializable.
- Move runtime state behind registry keys.
- Migrate history apply/restore so undo/redo rehydrates runtime through the runtime coordinator instead of directly restoring runtime-bearing `TimelineClip.source` objects.
- Make all project load/reload paths data-only for video/audio where possible.
- Audit all load paths: `projectLoad.ts`, `serializationUtils.loadState()`, project-load background nested restore, `mediaStore/init.ts`, and legacy `projectSlice.ts`.
- Centralize runtime cleanup for blob URLs, media elements, vector canvases, model/gaussian URLs, WebCodecs/native decoders, composition renderers, and nested resources.

### H. Dead Code And Legacy Route Retirement

Owner count: 1 agent for audits, then single-owner implementation slices.

- Track dead code and legacy route removal as an explicit workstream, not a cleanup afterthought.
- Delete or delegate obsolete code only after the replacement path has parity tests or a bridge smoke for the affected behavior.
- Current retirement candidates:
  - Full `TimelineClip` mounting inside `.timeline-canvas-dom-overlay` once shell modules cover active controls.
    - Done: canvas-mode `.timeline-canvas-dom-overlay` now renders only `ClipInteractionShell`; it never calls the legacy `renderLegacyClip()` fallback.
  - CSS hiding hacks under `.timeline-canvas-dom-overlay` after the full overlay is removed.
    - Done: removed the dead `.timeline-canvas-dom-overlay .timeline-clip` suppression block from `TimelineClip.css`; shell-specific keyframe rules remain.
  - Cache-warmup `useEffect` blocks in the former `TimelineClip.tsx` path after timeline services own thumbnail, waveform, spectrogram, and analysis artifact warmup.
    - Done for the deleted clip component; remaining cache parity work belongs to services/canvas paths.
  - Former `renderLegacyClip` fallback in composition-exit overlays after that path is either canvas/shell-backed or explicitly kept as a compatibility path.
    - Done: removed the full clip renderer prop from `TimelineTrack`; new track rows cannot call a legacy DOM clip renderer.
    - Done: new-track clip-drag previews now use lightweight `.timeline-clip-preview` markup instead of `TimelineClip`.
    - Done: composition-switch overlays now render `TimelineClipCanvas` rows; `Timeline.tsx` no longer imports or calls the `TimelineClip` component.
    - Done: deleted `src/components/timeline/TimelineClip.tsx`; remaining clip CSS is now shell/overlay-only and imported directly by `Timeline.tsx`.
    - Done: removed the former passive DOM hook/component stack plus app-orphaned utilities/tests that only supported the deleted clip body.
  - Direct timeline-load thumbnail generation through `restoreSourceThumbnails()`/`thumbnailCacheService.generateForSource()` once cache scheduler ownership is proven for visible demand and import/manual regenerate paths are explicitly separated.
    - Done for `serializationUtils.restoreSourceThumbnails()`: it now uses `thumbnailCacheService.loadCachedForSource()` only.
    - Done for silent project-load auto-relink: `fileManageSlice.updateTimelineClips()` and `relinkMedia.applyRelinkMatch()` now accept `generateThumbnails`, and `projectLoad.ts` passes `false` for both project-folder matches and the stored-handle fallback. Manual reload/relink paths keep the default `true`.
  - Legacy `mediaStore/slices/projectSlice.ts` load path: read-only audit found `projectSlice.loadProject(projectId)` has no app call sites and should be retired instead of extended.
    - Done: removed unused `saveProject`, `loadProject`, `getProjectList`, and `deleteProject` actions from `projectSlice.ts`; current save/load flows remain in `ProjectFileService`.
    - Done: extracted the remaining `initFromDB()` body to `mediaStore/legacyStartupRestore.ts`; `projectSlice.ts` keeps only the compatibility action that delegates to the helper.
  - History snapshots that keep runtime-bearing `TimelineClip.source` objects after `HistoryTimelineEditState` restore is implemented.
    - Audit result: project save is mostly on serializable data, but in-memory history still stores full `TimelineClip[]`/`Layer[]` snapshots. Next safe slice is adding `timelineEditState?: HistoryTimelineEditState` as a non-restoring sidecar to `StateSnapshot`, then proving `findHistoryStateBoundaryViolations(snapshot.timelineEditState) === []` with runtime-bearing clip/layer fixtures before changing undo/redo restore behavior.
    - Done for the non-restoring sidecar: `StateSnapshot` now carries `timelineEditState?: HistoryTimelineEditState`, created during capture and verified with runtime-bearing clip/layer fixtures. Restore now prefers the sidecar and uses a scoped runtime rehydrate adapter for compatible live sources.
    - Done for the first restore-side migration: `applySnapshot()` now restores timeline state from `HistoryTimelineEditState` when present, uses a pure restore mapper to rebuild timeline-store data, reuses only compatible current runtime sources, restores deleted clips as lazy data-only entries, and derives newly captured legacy `snapshot.timeline` payloads from the sidecar instead of retaining old DOM/File/decoder runtime payloads. The fallback to legacy `snapshot.timeline` remains only for old snapshots without a sidecar.
  - Unreachable top-level eager video/audio restore branch in `serializationUtils.loadState()`.
    - Done: the branch after `deferMediaElementRestore` was deleted together with its restore-time WebCodecs cache/warmup helpers; `serializationNestedRestore.test.ts` now asserts top-level video/audio restore does not create media elements or object URLs.
- Keep compatibility shims only when a concrete persisted project or manual workflow still needs them; otherwise remove the old path in the same phase that proves the replacement.

## Implementation Phases

### Phase 0: Contracts And Diagnostics First

- Land `TimelineRenderModel` and `TimelineGeometrySnapshot` interfaces.
- Define persistence boundary: project data vs render data vs undo data.
- Define `ClipInteractionShell` props, mount rules, command callbacks, and active module slots.
- Define edit operation contracts for move, trim preview followers, fade, keyframes, transitions, keyboard edits, and history batching.
- Define cache scheduler lanes and invalidation.
- Define runtime coordinator policies and resource descriptor shape.
- Add or expose timeline-canvas diagnostics: visible clip count, drawn clip count, thumbnail draw count, waveform draw count, DOM overlay count, DOM clip body count, cache scheduler counts, and live runtime resource counts.
- Add focused tests for model generation, geometry generation, and render-model purity.

### Phase 1: Build ClipInteractionShell As The Only DOM Clip Layer

- Continue `ClipInteractionShell` without a full `TimelineClip` fallback.
- Replace generic `renderClip` usage with typed shell props for new paths.
  - Done for the `TimelineTrack` public contract: no full clip renderer prop remains on track rows.
- Plumb `clipFade` into `TimelineTrack`.
- Mount shell for hover, drag, trim, fade, open menus, focused keyframes, audio/spectral/video/stem states, and tool previews.
- Keep active controls in shell modules; do not move behavior back into a full clip body.
  - Done for active trim handles and fade controls. Fade now uses shared curve geometry for DOM and canvas, positions shell handles from detected fade durations, draws fade curves in `TimelineClipCanvas`, and skips the legacy body for fade-only shells.
  - Done for selected keyframe clip-bar ticks: `ClipKeyframeTicks` now renders through `ClipInteractionShell`, routes grouped keyframe moves through `TimelineTrack`, and skips the legacy body for keyframe-only and mixed parity-shell states.
  - Done for audio-region selection/gain visuals, operation overlays, edit-stack controls, context-menu commands, move, resize, gain, fade-in, and fade-out interactions through `ClipAudioRegionControls`; audio-region-only shells now skip the legacy body.
  - Done for video-bake clip-region overlays and Bake/Unbake/Remove controls through `ClipVideoBakeControls`; video-bake-only shells now skip the legacy body.
  - Done for spectral-region selection/image-layer overlays, toolbar commands, Ctrl/Meta drag selection, clear, and image-layer drop through `ClipSpectralRegionControls`; spectral-region-only shells now skip the legacy body, and passive spectrogram display now renders through the canvas.
  - Done for stem-only active shells through `ClipStemControls`: active job progress and completed-stem source switcher render in the shell, preserve prewarm/source-switch callbacks, and no longer mount the legacy body in canvas mode.
- The old `TimelineClip` overlay is deleted; parity gaps must be fixed forward in shell/canvas modules.

### Phase 2: Cache And Specialized Module Extraction

- Move processed waveform and spectrogram warming out of `TimelineClip`.
  - Done for processed-waveform derivation and spectrogram tile generation via `timelineAudioArtifactGenerationWarmup`; both block during playback/drag preview and coalesce request keys.
- Move source waveform generation and thumbnail visible loads out of `TimelineClipCanvas`.
  - Done for visible thumbnail DB loads.
  - Done for source waveform generation demand via `timelineSourceWaveformWarmup`; it blocks generation during playback or drag preview and coalesces canvas/legacy-overlay requests.
  - Done for visible missing-thumbnail generation via `timelineThumbnailGenerationWarmup`; it resolves `MediaFile.url` before hydrated preview video URLs, skips ready/generating sources, blocks during playback/drag preview, coalesces by media id/file hash, and uses a small global generation cap.
  - Done from the thumbnail reload audit: added URL-based `thumbnailCacheService.generateForSourceUrl()` and coverage for URL-only generation, no-hydrated-video warmup, and generation concurrency limiting.
- Add cache scheduler and in-flight artifact load coalescing.
  - Done for visible thumbnail DB loads and waveform artifact reads.
  - Done for spectrogram artifact read coverage at the service boundary.
  - Done for decoded thumbnail bitmap invalidation.
  - Done for executable media cache invalidation of thumbnail source memory/persistent rows, decoded bitmap memory, waveform/spectrogram/loudness/beat/onset/frequency/phase memory refs, and clip audio-analysis jobs on media delete/source replacement.
  - Done for event-granular thumbnail notifications and visible-source-range redraw filtering.
  - Done for loudness, beat/onset, and frequency/phase artifact reads through `timelineAudioAnalysisArtifactWarmup`.
- Add canvas renderer contract for real spectrogram tile summaries.
  - Done: `TimelineClipCanvas` loads visible spectrogram refs through `timelineSpectrogramArtifactWarmup`, draws tile-backed rasters via `utils/spectrogramCanvas`, and falls back to stylized waveform drawing while tiles are missing.
- Extract audio, spectral, video-bake, and stem modules.
  - Done for the stem shell module as the most disjoint active-control extraction.
  - Done for the keyframe clip-bar shell module as the smallest remaining active-control extraction.
  - Done for the audio-region shell module: visuals, operation overlays, edit-stack controls, context-menu commands, move/resize/gain interactions, and audio-region-only legacy-body skip are in shell.
  - Done for the video-bake shell module: clip-region overlays, Bake/Unbake/Remove controls, and video-bake-only legacy-body skip are in shell.
  - Done for the spectral shell module: active selection overlays, image-layer overlays/drop, and toolbar controls are in shell; passive spectrogram drawing is now canvas-backed.
  - Done for lightweight passive transcript markers, sampled focus/motion/face analysis overlays, and status/progress badges in `TimelineClipCanvas`.
  - Done for nested/composition segment thumbnail strips, boundary markers, mixdown waveform/generation state, and composition outlines in `TimelineClipCanvas`.
  - Done for audio cache invalidation, media deletion artifact cleanup, relink/source-identity, and non-waveform artifact read warmups.
- Make source replacement and media deletion evict persistent, memory, and decoded bitmap caches.
  - Done for media delete and source-replacement memory cleanup through `timelineCacheInvalidation`; remaining work is richer source-fingerprint equivalence for unchanged replacements and same-prefix hash collision hardening.
  - Done for the relink/source-identity slice: file-backed reload/relink and stored-handle auto-relink recompute `MediaFile.fileHash`, native metadata-only relinks clear stale hashes, source-derived media fields and clip audio refs are cleared on replacement, replacement thumbnail generation receives the new hash, and stale thumbnail DB-load promises cannot repopulate memory after `clearSource()`.
  - Done for non-waveform artifact read warmups: visible geometry-aware canvas demand now primes loudness, beat/onset, frequency, and phase caches through `timelineAudioAnalysisArtifactWarmup`, with service tests for ref collection, cached fast path, kind-safe coalescing, and missing-result publication.

### Phase 3: Interaction Operations And Geometry Parity

- Convert body drag commit to typed `move-clips`.
  - Historical audit result: `MoveClipsOperation`, `ResolvedClipMove`, and `applyMoveClipsOperation()` existed before the UI adapter, but normal body drag still committed through `moveClip()` in `useClipDrag.handleMouseUp()`.
  - Done for the first resolver slice: `moveResolution.ts` emits `ResolvedClipMove[]`/`MoveClipsOperation` without mutating the store, preserves raw request vs resolved output, and covers linked, group, selected-pair, resistance, lock/compatibility, and fallback-track intent.
  - Done for the desktop UI adapter slice: body-drag commits route through typed operations using explicit resolved moves; warning cases no longer silently fall back to legacy `moveClip()` in the desktop drag adapter.
  - Done for fallback-track materialization: resolver fallback provisional IDs are rewritten inside `move-clips-resolved` by pure track planning, then committed with tracks, expanded tracks, clips, cleanup, duration update, and cache invalidation in one history batch.
  - Done for the overlap oracle slice: forced-overlap resolved moves now carry direct overlap candidates plus linked trim/delete propagation as plain data.
  - Done for the overlap apply slice: `moveOverlapTrim.ts` applies the resolved overlap plan as pure legacy-equivalent trim/delete output; `move-clips-resolved` applies move plus overlap trim/delete inside the operation kernel with cleanup, selection reconciliation, update duration, and one invalidation.
  - Done for selected linked-pair audit coverage: `createResolvedClipMoveOperationPlan()` now treats preserved selected linked-pair offsets as resolved-only, `useClipDrag` dispatches `move-clips-resolved` for that blocker, and tests assert both the routing condition and the preserved video/audio offset outcome.
- Add `ResolvedClipMove` parity tests against legacy behavior.
- Add linked/follower trim preview geometry.
  - Done for the first geometry-contract slice: `TimelineGeometrySnapshot.trimPreviews[]` can carry multiple lead/follower trim preview records, and each record includes `leadClipId` to associate linked/selected follower previews with the active trim lead. `timelineRenderModel.test.ts` asserts lead plus linked follower entries and keeps the snapshot structured-clone-safe.
- Add fade and keyframe preview/transaction contracts.
  - Done for the first ID contract slice: `TimelineFadeCurveGeometry` carries a stable `id` plus `handleRectIds` that can point to fade handle rects, and `TimelineKeyframeDiamondGeometry` carries `rectId` so `KeyframeGeometryReference.diamondRect` can resolve to render-model plain geometry independently of the domain keyframe id.
- Move keyframe marquee and empty-area exclusions to geometry data.
  - Done for the first exclusion-helper slice: `findTimelineMarqueeExclusionAtPoint()` and `findTimelineMarqueeExclusionsIntersectingRect()` evaluate keyframe-editor, clip-handle, active-control, header, and future exclusions from plain `TimelineGeometrySnapshot.marqueeExclusions` data.
  - Done for the first hook-adapter slice: keyframe marquee hit testing uses rendered keyframe row track/property data plus clip/keyframe data instead of querying `.keyframe-diamond` DOM nodes; a hook test covers selecting a keyframe without any diamond DOM present.
- Route keyboard edit paths through operation contracts.
  - Done for delete and blend-mode cycling: `useTimelineKeyboard` emits executable keyboard command operations, `applyTimelineEditOperation()` handles them, and tests cover keyframes-first delete, clip-only linked parity, mixed missing/locked keyframes, blend-mode application, and shortcut dispatch.
- Add keyframe transaction execution for drag/curve-editor parity.
  - Done for the first generic keyframe transaction kernel: `keyframe-transaction-begin`, `keyframe-transaction-update`, `keyframe-transaction-commit`, and `keyframe-transaction-cancel` are accepted by `applyTimelineEditOperation()`, with numeric create/move/update/remove/easing/bezier/rotation/selection operations applied in one history batch.
  - Done for Fade transaction execution and hook routing: `fade-transaction-update` / `commit` / `cancel` materialize the planned fade keyframes, preserve existing handles, and `useClipFade` now uses typed operations with one live-update/commit history batch. Selected clip-bar keyframe tick drag and expanded curve-editor keyframe/Bezier drags now emit typed `keyframe-transaction-*` operations with deferred live-update history batches. Path-value keyframe creation/update and Mask/Text compatibility API routing are executable through the generic kernel. Remaining work is explicit parity coverage.
- Add transition junction geometry and typed transition operations.
  - Done for the typed transition slice: `transition-apply`, `transition-remove`, `transition-update-duration`, `transition-preview-drop`, and `transition-clear-preview` are executable in `applyTimelineEditOperation()`, legacy store actions wrap apply/remove/update, transition drag dispatches preview/drop/clear contracts with junction geometry references, and tests cover apply, clamp, remove, update, locked-track rejection, structured-clone contracts, transition junction hit geometry, store-backed preview ghosts, blocked preview state, overlay propagation, and replay descriptors.
- Confirm linked clip and linked group parity.
  - Done for the first typed apply proof: resolver tests cover linked group inclusion and explicit skipped followers for `includeGroups:false`; operation tests cover linked group moves through `applyTimelineEditOperation()`, Alt-style group-skip behavior, and locked follower-track blocking before partial commit.
  - Done for the desktop drag commit adapter: `useClipDrag` now has a tested pure typed-commit decision helper and no longer falls back to desktop `addTrack()`/`moveClip()` for resolver warnings, typed apply failures, fallback-track moves, overlap trims, or selected linked-pair moves.

### Phase 4: Retire The Full TimelineClip Overlay

- Overlay deletion is done; remaining Phase 4 work is parity verification, stale selector removal, and fixing any regressions forward in shell/canvas modules.
- Delete full `TimelineClip` mounting from `.timeline-canvas-dom-overlay`.
  - Done: `TimelineTrack` no longer mounts `TimelineClip` inside the canvas overlay; `domClipBodyCount` is always 0 for canvas-mode tracks.
- Delete CSS hiding hacks only after replacement controls are active.
  - Done for the old hidden full-body overlay rules; shell-specific overlay rules remain.
- Keep former `TimelineClip` responsibilities only where they have been reassigned as shared renderer utilities or active shell modules.
- Verify select-all and large-project culling do not mount selected-only DOM clip bodies.

### Phase 5: Runtime And Persistence Boundary

- Introduce runtime coordinator policy registry and budget reporting.
  - Done for live resource reporting: `timelineRuntimeCoordinator` now retains plain resource descriptors, computes policy usage/budget pressure, and exposes bridge-safe stats.
- Migrate primary lazy media into the coordinator.
  - Done for descriptor/reporting ownership: primary `lazyMediaElements` video/audio HTML elements register interactive `html-media` resources on attach, refresh diagnostics while desired, and release exact resource ids on detach/prune/releaseAll.
  - Done for first allocator admission: `attachVideoElement()` and `attachAudioElement()` ask `TimelineRuntimeCoordinator.canRetainResource(...)` before `getLazySource(...)`, file-backed object URL creation, DOM element creation, and clip-source mutation. Over-budget clips stay data-only/retryable, with focused tests covering full HTML-media and audio-source budgets plus retry after release.
  - Done for file-backed lazy video/audio element URL ownership: when no durable media URL exists but a media id and file are available, the assigned `video.src` / `audio.src` is created through `mediaObjectUrlManager` with a per-clip/per-kind lazy key and revoked through that same manager on detach/prune/releaseAll. The old direct object URL path remains only as fallback for clips without a media id.
  - Done for direct browser video/audio add data-only behavior: imported video/audio clips and linked video audio no longer store `HTMLVideoElement`, `HTMLAudioElement`, or `WebCodecsPlayer` on clip sources after add; temporary metadata elements are released after probing.
  - Done for timeline video/audio paste data-only behavior: pasted video/audio clips reattach `File` and metadata without creating or storing DOM media elements, WebCodecs players, or video/audio blob URLs.
  - Done for media relink/reload video/audio data-only behavior: `updateTimelineClips()` reattaches the replacement `File` and source metadata without creating or storing DOM media elements, WebCodecs players, or video/audio blob URLs; thumbnail generation uses the media-owned URL.
  - Done for download completion video/audio data-only behavior: downloaded video plus linked audio clips store source metadata only, reuse import duration where possible, and warm thumbnails via the media-owned source URL.
  - Done for 3D user-action media-owned URL behavior: direct model/gaussian fallback, model paste, single spatial reload, and NativeHelper external-drop recovery prefer media-owned URLs when a media id exists.
  - Done for AddComp composition-audio placeholder creation: linked composition audio clips are created with plain `source: { type: 'audio', naturalDuration }`, silent waveform metadata, reciprocal links, and no eager mixdown/audio element allocation.
  - Done for split media-runtime data-only cleanup: legacy `splitClip`, bulk `split-at-times`, and shared clip-part source cloning strip `videoElement`, `audioElement`, `webCodecsPlayer`, and `nativeDecoder` from new video/audio clip-part sources while preserving data metadata such as `naturalDuration`, `mediaFileId`, `runtimeSourceId`, `runtimeSessionKey`, and `filePath`. This supersedes the earlier composition-audio-only cleanup.
  - Done for audio edit bake/unbake source cleanup: baked and restored audio clips no longer create or store `HTMLAudioElement`; they keep file/media identity, duration, waveform/analysis refs, and source metadata for lazy/runtime hydration.
  - Done for stem source-switch data-only cleanup: switching an audio clip to an extracted stem or back to the original source no longer creates/caches/stores `HTMLAudioElement`; stale source elements are disposed and the clip keeps file/media identity, duration, and source metadata for lazy/audio sync hydration.
  - Done for Vector runtime canvas admission: Lottie/Rive runtime preparation asks coordinator admission for the caller-owned `image-canvas` before creating the canvas/player, and background-layer, slot-deck, composition-render, and export call sites pass their real policy/owner IDs.
  - Done for video-bake proxy admission: composition video-bake proxy registration asks coordinator admission for the `composition-render` video resource before creating blob URLs or hidden `HTMLVideoElement`s, and releases on remove/clear/load failure.
  - Done for legacy JPEG proxy-frame cache admission: retained `HTMLImageElement` proxy frames are represented as one aggregate `thumbnail` policy `image-canvas` resource per media file, admission is checked before cache insertion, and LRU/media/full clears refresh or release the aggregate resource.
  - Done for decoded AudioBuffer cache admission: retained scrub/playback decoded audio buffers are represented as `interactive` policy `audio-source-clock` resources, admission is checked before cache insertion, and LRU/media/full/context clears release the retained resource.
  - Done for proxy VideoFrame cache admission: retained WebCodecs proxy frames are represented as one aggregate `interactive` policy `video-frame-provider` resource per media file, admission is checked before cache insertion, denied frames are closed, and LRU/media/full clears refresh or release the aggregate resource.
  - Done for playback/export composition-audio mixdown-on-demand: active playback requests and attaches composition audio only when needed through `compositionAudioMixdownCache.ts`, and audio export requests a buffer instead of decoding empty data-only placeholder files.
- Migrate background/slot deck hydration.
  - Done for descriptor/reporting ownership: cold background-layer and warm slot-deck hydration now report runtime bindings, HTML media elements, image elements, and text canvases into `timelineRuntimeCoordinator` with exact owner-scoped release. Existing media allocation, playback sync, slot pinning, and decode-session policies remain unchanged until allocator migration.
  - Done for allocator admission: background-layer and warm slot-deck video/audio paths plan runtime source/session ids and reserve runtime-binding plus HTML media resources before DOM element creation. Denial clears loading/prepared state without creating elements, and stale/error paths release reservations.
- Migrate composition-render, thumbnail, render-target, and RAM preview policies.
  - Done for composition-render descriptor/reporting ownership: non-active composition video/image/text/math/vector sources report owner-scoped resources under `composition-render`, release on dispose/invalidation, clean up stale in-flight loads, revoke owned blob URLs on error/invalidation, and avoid reporting active-composition pass-through media.
  - Done for composition-render video allocator admission: composition video sources reserve planned runtime-binding plus HTML video resources before object URL creation and detached `video` allocation. Denial resolves the pending source without creating browser media.
  - Done for thumbnail descriptor/reporting ownership: DB-load/generation/bitmap-decode jobs now report as real `job` resources, detached thumbnail videos/canvases report while generation owns them, decoded thumbnail bitmaps report while retained, and exact resource ids are released on completion, abort, source clear, memory eviction, and cache clear.
  - Done for render-target descriptor/reporting ownership: central render-target store actions report canvas-backed targets under the `render-target` policy and release exact target owners when canvases/windows deactivate or unregister. The first `gpu-texture` descriptor contract is now available for internal GPU resources; final allocator enforcement is still a separate ownership pass.
  - Done for GPU texture contract readiness: `gpu-texture` is now a first-class resource kind and counts against GPU texture/GPU byte budgets.
  - Done for RAM-preview descriptor/reporting ownership: run jobs, video/image/nested source resources, RAM-preview policy runtime sessions, aggregate CPU composite cache, and per-frame GPU cached playback textures now report/release under `ram-preview`, including same-key replacement, LRU eviction, clear, and device-loss cleanup.
  - Done for RAM-preview allocator admission: run jobs ask admission before engine setup, data-only image hydration asks admission before object URL / `new Image()` allocation, video runtime-binding/provider/html-media resources are reserved before `getRuntimeFrameProvider(...)`, verification uses a non-allocating provider peek, CPU composite cache asks admission before `ImageData` allocation and store mutation, and GPU frame cache asks admission before WebGPU texture creation/retention.
- Add export adapter last, with nested composition and active slot/background decisions settled.
  - Done for the first `FrameExporter`-owned adapter: export run jobs, output surface, `ClipPreparation` runtime bindings/providers/precise videos, and export preview bitmaps report under the `export` policy and release by run owner on success/failure/cancel.
  - Done for export allocator admission: run jobs, output surfaces, optional preview frames, export image elements, dedicated PRECISE video elements, source/processed/mix/master audio buffers, runtime bindings, FAST sequential WebCodecs providers, and parallel decoder/frame-buffer resources ask coordinator admission before the relevant heavy allocation or downstream work. Runtime bindings are planned before `bindSourceRuntimeForOwner()`, sequential providers are reserved before `File.arrayBuffer()` / `new WebCodecsPlayer()`, and parallel decoder/frame-buffer resources are reserved before `new ParallelDecodeManager()` / `ParallelDecodeManager.initialize()`.
  - Done for ParallelDecode detail reporting: `ParallelDecodeManager` exposes a bridge-plain runtime snapshot, and export reporting maps decoders to `native-decoder` plus decoded `VideoFrame` buffers to `video-frame-provider` heap pressure without mislabeling them as GPU textures.
  - Done for `AudioExportPipeline`: source/processed/mix/master buffers report as export `audio-source-clock` resources, extractor cache cleanup now runs through `finally`, raw export checks cancellation after master render, and WebCodecs audio encoding has a cancel path.
  - Done for active FFmpeg-frame rendering: `FFmpegFrameRenderer` owns optional export run reporting for FFmpeg video/GIF, browser GIF, and image-sequence paths, forwards its run id into `prepareClipsForExport(...)` so existing export admission gates apply during preparation, and releases reported job/resources if initialization fails; one-shot still-frame render and FFmpeg WASM internals are explicit non-goals for this coordinator slice.
  - Historical browser export smoke: `debugExport` produced a non-empty 1s MP4 and post-export stats showed no retained export resources, GPU textures, playback stalls, preview freezes, or WebGPU-lost symptoms. Rerun this in the final broad verification pass because later restore/menu/worker-contract slices have landed since that proof.
- Make nested comp restore/reload data-only where possible.
  - Done for top-level `loadState()` video/audio restore: ordinary video/audio clips now have explicit test coverage proving no eager media elements or object URLs are created.
  - Done for `loadState()` nested video restore: direct and sub-nested video clips now use data-only sources with media ids/file paths and do not create object URLs, video elements, or WebCodecs players during restore.
  - Done for `loadState()` nested audio restore: direct nested audio clips now use the shared data-only restored media source path and no longer allocate unused object URLs during timeline restore.
  - Done for internal `loadState()` nested builder removal: direct level-1 nested clips and deeper nested clips now delegate to `nestedCompositionLoader.ts:loadNestedClips(..., { restoreHooks })` instead of maintaining a local recursive builder.
  - Done for `loadState()` composition-audio restore: linked composition audio clips now restore from persisted plain data without audio elements, object URLs, mixdown buffers, or background mixdown generation.
  - Done for AddComp/Split/playback/export composition-audio no-runtime behavior: newly added composition clips create data-only linked audio placeholders, split parts keep composition audio and ordinary media runtime fields as data-only source metadata even if older runtime objects existed on the source, and playback/export hydrate composition mixdown audio only on demand.
  - Done for direct browser video/audio user-action add: browser add keeps video/audio source metadata data-only and leaves actual media element ownership to lazy runtime hydration.
  - Done for timeline video/audio user-action paste: paste keeps video/audio source metadata data-only and leaves actual media element ownership to lazy runtime hydration.
  - Done for media relink/reload video/audio user-action replacement: reload keeps video/audio source metadata data-only and leaves actual media element ownership to lazy runtime hydration.
  - Done for download completion video/audio user-action replacement: completion keeps downloaded video/audio source metadata data-only and leaves actual media element ownership to lazy runtime hydration.
  - Done for 3D user-action URL ownership: model/gaussian/avatar clip URL sources now avoid duplicate clip-owned or unmanaged object URLs when media-owned URLs are available.
  - Done for the first `projectLoad.ts` post-relink cleanup: `reloadNestedCompositionClips()` no longer keeps unreachable video/audio element branches after its data-only short-circuit. Its older nested image allocation branch has since been superseded by data-only nested image restore.
  - Done for the cross-file video/audio descriptor contract: `restoredMediaSource.ts` now provides `createDataOnlyRestoredMediaSource(...)`, and both `serializationUtils` plus `projectLoad.ts:reloadNestedCompositionClips()` use it for restored video/audio descriptors.
  - Done for post-relink coverage: `tests/unit/projectMediaPersistence.test.ts` now drives `loadProjectToStores()` through Raw-folder relink and asserts the rebuilt nested video/audio clips are data-only with no restore-time video/audio elements.
  - Done for the first shared nested-restore module slice: `nestedRestore.ts` now owns nested clip ids, motion clip restore, persisted video/audio state restoration, node graph cloning, and common media clip shell fields used by `serializationUtils`, `addCompClip`, and the delegated `projectLoad` post-relink path.
  - Done for project-load post-relink delegation: `reloadNestedCompositionClips()` now calls `loadNestedClips(...)`, preserves session/same-composition guards, writes nested boundaries, and has focused recursive/stale-session coverage.
  - Done for neutral nested loader ownership: `nestedCompositionLoader.ts` now owns `loadNestedClips(...)`, nested boundaries, clip segment helpers, and composition thumbnail generation for AddComp/project-load callers instead of keeping that orchestration in `clip/addCompClip.ts`.
  - Done for recursive nested keyframe collector convergence: `nestedCompositionLoader.ts` now exports the shared keyframe ID remapper used by both `loadNestedClips(...)` and `serializationUtils.loadState()`, including nested composition clips and sub-nested media clips.
  - Done for nested segment scheduling convergence: AddComp, nested refresh, and `serializationUtils.loadState()` now route clip segment build/apply scheduling through `nestedCompositionLoader.ts`, with focused stale-session coverage.
  - Done for nested boundary/thumbnail helper coverage: focused tests prove visible-video-only boundary calculation and forwarding of boundaries into composition thumbnail generation.
  - Done for nested keyframe merge/session-guard convergence: `mergeNestedClipKeyframes(...)` now owns preserving unrelated keyframes, empty-map no-op behavior, and stale-session blocking for both `loadNestedClips(...)` and `serializationUtils.loadState()`.
  - Done for neutral restore-hooks policy surface: `loadNestedClips(...)` now accepts `restoreHooks.runtimeReady` and `restoreHooks.mediaRelink`, covering runtime-ready events, opt-in cache invalidation override, opt-in `needsReload`, and opt-in fallback source metadata while preserving AddComp/project-load defaults.
  - Done for `serializationUtils.loadState()` nested delegation: the old local recursive `loadNestedCompositionClips(...)` builder is gone, loadState passes preview-wakeup/relink policy through `restoreHooks`, and focused tests cover preview wakeups, stale runtime completions, missing image/vector/model relink semantics, and combined AddComp/loadState/project-load restore coverage.
  - Done for top-level spatial restore dedupe: `serializationUtils.loadState()` now uses `isRestoredSpatialSourceType(...)` plus `applyManagedRestoredSpatialSource(...)` for model, gaussian avatar, and gaussian splat clips instead of three repeated branches.
  - Done for project-load missing-media policy guard: `tests/unit/projectMediaPersistence.test.ts` now asserts post-relink nested image/vector/model/gaussian assets with no browser file stay neutral placeholders and do not inherit loadState relink fallback hooks.
  - Done for post-relink populated fallback refresh: `reloadNestedCompositionClips()` now also rebuilds composition clips whose existing nested tree contains `needsReload` placeholders, so loadState-created fallback clips are not skipped after media relink.
  - Done for top-level image/vector relink and stale-runtime guards: `serializationUtils.loadState()` now keeps top-level missing image/vector media as non-loading relink placeholders, does not start image/vector runtime restore without a browser file, and ignores stale top-level Vector runtime completions without preview wakeups.
  - Done for top-level restore helper extraction: initial restored media-source selection and top-level image/vector/spatial runtime dispatch now use narrow helpers, including the shared spatial source applicator. Remaining restore work is runtime-hydrator/allocator ownership, not the old nested builder duplication.
  - Done for NativeHelper restore URL ownership cleanup: recovered top-level image files use media-scoped primary object URL ownership, while native video/audio restore stays data-only and no longer dereferences full files during timeline load.
  - Done for restore-scoped vector helper extraction: `vectorRuntimeRestore.ts` owns pending vector source setup, stale-generation guards, runtime manager preparation, cleanup, and patch conversion, while `nestedRestore.ts` keeps image/tree restore helpers.
  - Done for top-level stale image blob URL hardening: `serializationUtils.loadState()` now reuses primary media-owned blob URLs, rejects stale/unmanaged image blob URLs when a browser file is available, and replaces them with managed clip-owned image URLs.
  - Done for cached media thumbnail URL ownership: `mediaObjectUrlManager` now has a separate `thumbnail` key; project-load cached thumbnail restore creates managed URLs only when the batch applies them, and legacy startup plus `ensureFileThumbnail()` use media-owned thumbnail URLs for persisted thumbnail blobs.
  - Done for first interactive lazy image preview fallback and admission: `lazyImageElements.ts` can supply ready `HTMLImageElement`s for data-only image clips to `LayerBuilderService` without mutating `clip.source`, reports interactive image resources, releases owned file-backed URLs, wakes preview on load/error, asks coordinator admission before `new Image()` / file-backed object URL creation, and replaces stale same-clip image records when the image source changes.
  - Done for shared image hydrator admission: `imageRuntimeHydrator.ts` now accepts resource metadata and asks coordinator admission before `new Image()` for composition-render, background-layer, and warm slot-deck image loads. Denial resolves composition pending loads, clears background loading state, and avoids slot-deck `preparedClipCount` inflation.
  - Done for top-level data-only image restore and export image hydration: `serializationUtils.loadState()` writes managed `source.imageUrl` instead of eager top-level `Image` allocation, `ClipPreparation` prepares export image elements from `source.imageUrl` / media URLs / files, and export cleanup revokes only export-owned image blob URLs.
  - Done for nested/add/paste data-only image state: direct and sub-nested image clips now restore with managed `source.imageUrl` descriptors and immediate runtime-ready notifications instead of eager `imageElement` patching; direct add-image and image clipboard paste also write data-only `source.imageUrl` while keeping any required `Image` object local to thumbnail/native-scale preparation.
  - Remaining: final broad build/lint/test/browser pass; the old loadState nested builder is no longer an active duplication.
- Move history snapshots toward `HistoryTimelineEditState`.
  - Done for capture-side sidecar generation and boundary tests.
  - Done for first restore-side migration: undo/redo/jump/branch restore prefer sidecar-derived timeline state, new snapshots derive their legacy timeline payload from plain sidecar data, and deleted/non-resident clips restore as lazy data-only entries.
  - Done for runtime rehydrate reporting: after restore, compatible live sources are reported to the interactive runtime coordinator through scoped `history-rehydrate:*` owners, stale history-rehydrate reports are released, and data-only lazy reload entries remain unreported until the normal lazy media/render adapters prepare them.
- Retire legacy `projectSlice.ts` or delegate it to the unified project-load contract with tests.
  - Done: unused legacy IndexedDB project save/load/list/delete actions are retired, `initFromDB()` remains only as a startup compatibility delegator to `legacyStartupRestore`, and `projectSliceCompatibility.test.ts` guards both the delegation and the absence of retired actions.

### Phase 6: Worker And Large-Project Hardening

- Redesign OffscreenCanvas worker API only after main-thread render-model parity.
  - Done for worker protocol/runtime fallback: resolved geometry, selected/hovered state, request ids, ready/drawn/error acks, diagnostics, and recoverable main-thread fallback are implemented.
- Worker message must carry resolved geometry plus transferable/prepared thumbnail, waveform, and spectrogram resources.
  - Done for waveform, spectrogram, bounded thumbnail-strip, and passive decoration resource slices: eligible non-spectral audio clips can send prepared normalized waveform columns, eligible spectral audio clips can send bounded rasterized spectrogram values, otherwise-worker-eligible source-video clips can send fresh per-draw thumbnail strip `ImageBitmap`s without transferring cache-owned bitmaps, and simple passive status/progress badges, bounded transcript markers, and sampled analysis overlays can cross as plain transferable data.
  - Done for worker wire contract cleanup: `timelineClipCanvasWorkerContract.ts` is the single shared type surface for clip resources, draw/init messages, and worker outgoing acks/errors; the model, worker, and React owner no longer maintain duplicate local unions for those payloads.
- Support hover/selection/trim/source-extension visuals before enabling `timelineCanvasWorker` by default.
  - Done for hover/selection in the worker payload, source-timed and reversed thumbnail strips, simple passive badge/progress overlays, transcript markers, sampled focus/motion/face analysis overlays, reversed passive badge parity, active trim/source-extension ghost visuals, fade curve resources, ordinary active drag/drag-preview geometry, Slip/Slide resolved geometry/source-range parity, and composition visuals.
- Add large-project diagnostics and perf budgets.
  - Done for large-project diagnostics and worker draw/resource-byte totals in `getStats.timelineCanvas`.
- Add regression tests or debug scripts for reload, scroll, zoom, select-all, marquee, and playback.
  - Done for real-media worker-positive hook coverage: the verification runner now has a `workerPositiveLive` step that opens the configured worker-positive target, warms cached thumbnail bitmaps, and requires worker-rendered tracks without fallback, pending, or error tracks.
  - Done for focused browser proof: the restored 3-video comp `1780725925034-snfho0roz` passed forced-worker rendering with 3 eligible worker tracks, zero fallback/pending/error tracks, warmed thumbnail bitmaps, and a clean frame-loop estimate.
  - Historical proof exists for reload, large scroll/zoom/select-all, marquee, playback, playhead, Blade, thumbnail reload, export-preview parity, RAM, spectral, worker-solid, and worker-live-fallback smokes plus full build/lint/test gates. These results predate the latest restore/menu/worker-contract/worker-positive hook/import-fallback slices and must be rerun for final readiness. A local ignored `fixtures/torture-media/manifest.local.json` now exists, so the final runner should include Torture on this machine.

## Verification Requirements

Every implementation batch must be checked with targeted tests plus real browser behavior:

- idle reload stats through the AI bridge
- real playback simulation through the AI bridge after timeline changes
- timeline thumbnail visibility after reload before playback
- scroll/zoom/select-all smoke on a large project
- targeted TypeScript/lint checks for touched files
- targeted fade shell/canvas parity tests: `ClipInteractionShell.contract.test.tsx`, `TimelineTrack.test.tsx`, and `fadeCurvePath.test.ts`
- targeted thumbnail cache event tests: `thumbnailCacheService.test.ts`, `timelineThumbnailDbWarmup.test.ts`, and `timelineThumbnailGenerationWarmup.test.ts`
- targeted keyframe shell parity tests: `ClipInteractionShell.contract.test.tsx` and `TimelineTrack.test.tsx`
- targeted audio-region shell visual, command, edit-stack, operation-overlay, and legacy-skip tests: `ClipInteractionShell.contract.test.tsx` and `TimelineTrack.test.tsx`

Required new or expanded checks:

- Unit tests: `serialization.test.ts`, `projectMediaPersistence.test.ts`, `projectTimelineSaveGuard.test.ts`, `historyStore.test.ts`, `TimelineTrack.test.tsx`, plus new tests for nested comp `loadState()` runtime deferral and legacy `projectSlice` compatibility/delegation.
- Shell parity tests for selected/open/fade/keyframe/audio/spectral/video/stem states.
- Context-menu command-model tests without DOM/media elements.
- Render-model tests asserting no `File`, DOM element, store action, media element, object URL, or cache-warming callback references.
- Reload-before-playback hydrates visible thumbnail DB entries without creating video elements.
- Multiple visible clips sharing the same audio artifact ref perform one in-flight artifact read.
- Deleting media evicts thumbnail source memory, decoded bitmap memory, and all audio analysis memory caches.
- Canvas spectral display parity is covered by `timelineSpectrogramCanvas.test.ts`; browser spectral-mode playback smoke is covered by the full timeline-canvas runner.
- Fast and precise `debugExport` with two-level nested comps, same-track transitions, masks inside nested comps, and active slot/background layers.
- Preview/export frame comparison for multilayer program output.
- RAM preview smoke for video and nested comp clips.
- AI bridge smoke tools: `reloadApp`, `getStatsHistory`, `getStats`, `getPlaybackTrace`, `simulateScrub`, `simulatePlayback`, `simulatePlaybackPath`, and `debugExport`.

Before normal commit or merge, use the repo-required full chain:

```bash
npm run build
npm run lint
npm run test
```

## Remaining Decisions

- Whether track grid/background state remains DOM/CSS or moves to a separate background canvas.
- Whether active overlays are one shell with feature modules or separate shells per tool mode.
- Whether export renders the editor timeline only, or the current program output including active slot/background layers.
- How much remaining stray runtime allocator ownership outside the now-gated export/RAM/background/slot/composition paths moves into `TimelineRuntimeCoordinator`; export admission is implemented and now needs final live parity proof.
- Whether the spectrogram raster cache should move into the timeline worker once the worker API carries resolved geometry and prepared resources.

## Initial Decision

A full refactor is worthwhile. The current hybrid architecture is a useful stepping stone, but the maintainable target is a source-cache-backed canvas timeline with active DOM shells, typed edit operations, explicit cache scheduling, and strict runtime policies.
